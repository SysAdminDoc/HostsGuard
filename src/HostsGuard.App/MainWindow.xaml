<Window x:Class="HostsGuard.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        xmlns:vm="clr-namespace:HostsGuard.App.ViewModels"
        x:Name="Root"
        Style="{StaticResource Hg.Window}"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:svc="clr-namespace:HostsGuard.App.Services"
        mc:Ignorable="d"
        Title="{svc:Loc Key=Xaml_HostsGuard_bf992a7e}" Height="920" Width="1520"
        MinHeight="760" MinWidth="1180">
    <Window.Resources>
        <svc:PointsTextConverter x:Key="PointsConverter" />
        <svc:SeriesBrushConverter x:Key="SeriesBrush" />
        <svc:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVis" />
        <svc:EmptyStateVisibilityConverter x:Key="EmptyStateVis" />
        <svc:ObjectPresentBooleanConverter x:Key="ObjectPresentBool" />
        <Style x:Key="Hg.LockPasswordBox" TargetType="PasswordBox" BasedOn="{StaticResource {x:Type PasswordBox}}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Grid>
                            <Border x:Name="FocusRing"
                                    BorderBrush="{DynamicResource Hg.Focus}"
                                    BorderThickness="2"
                                    CornerRadius="8"
                                    Margin="-2"
                                    Opacity="0"
                                    IsHitTestVisible="False" />
                            <Border x:Name="Chrome"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6">
                                <Grid>
                                    <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" />
                                    <TextBlock x:Name="Hint"
                                               Margin="8,0"
                                               VerticalAlignment="Center"
                                               IsHitTestVisible="False"
                                               Visibility="Collapsed"
                                               Foreground="{DynamicResource Hg.Dim}"
                                               Text="{Binding Path=(svc:PasswordBoxHelper.Watermark), RelativeSource={RelativeSource TemplatedParent}}"
                                               TextTrimming="CharacterEllipsis" />
                                </Grid>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="svc:PasswordBoxHelper.IsEmpty" Value="True">
                                <Setter TargetName="Hint" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource Hg.S2}" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource Hg.Focus}" />
                                <Setter TargetName="FocusRing" Property="Opacity" Value="1" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <tb:TaskbarIcon x:Name="Tray"
                        IconSource="pack://application:,,,/HostsGuard.App;component/Assets/icon.ico"
                        ToolTipText="HostsGuard"
                        TrayMouseDoubleClick="OnTrayOpen">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu Opened="OnTrayMenuOpened">
                    <MenuItem Header="{svc:Loc Key=Xaml_Open_HostsGuard_0cf1d264}" Click="OnTrayOpen" />
                    <Separator />
                    <MenuItem Header="{svc:Loc Key=Xaml_Filtering_mode_10d184c2}">
                        <MenuItem x:Name="TrayModeNormal" Header="{svc:Loc Key=Xaml_Normal_â_enforce_silently_6f006f4f}" Tag="normal" Click="OnTrayMode" />
                        <MenuItem x:Name="TrayModeNotify" Header="{svc:Loc Key=Xaml_Notify_â_prompt_on_new_connections_1dd99427}" Tag="notify" Click="OnTrayMode" />
                        <MenuItem x:Name="TrayModeLearning" Header="{svc:Loc Key=Xaml_Learning_â_auto_allow_and_record_6c277dd9}" Tag="learning" Click="OnTrayMode" />
                        <MenuItem Header="{svc:Loc Key=Xaml_Learning_â_15_min_then_lock_66553a49}" Tag="learning:15" Click="OnTrayMode" />
                        <MenuItem Header="{svc:Loc Key=Xaml_Learning_â_1_hour_then_lock_468ea740}" Tag="learning:60" Click="OnTrayMode" />
                    </MenuItem>
                    <MenuItem Header="{svc:Loc Key=Xaml_Global_outbound_90a53e2a}">
                        <MenuItem Header="{svc:Loc Key=Xaml_Block_all_outbound_b110b88e}" Tag="block-all" Click="OnTrayGlobalMode" />
                        <MenuItem Header="{svc:Loc Key=Xaml_Allow_all_outbound_4a4d0c05}" Tag="allow-all" Click="OnTrayGlobalMode" />
                        <MenuItem Header="{svc:Loc Key=Xaml_Pause_enforcement_8c943b55}">
                            <MenuItem Header="{svc:Loc Key=Xaml_5_minutes_34e17f47}" Tag="5" Click="OnTrayPauseEnforcement" />
                            <MenuItem Header="{svc:Loc Key=Xaml_15_minutes_899761ff}" Tag="15" Click="OnTrayPauseEnforcement" />
                            <MenuItem Header="{svc:Loc Key=Xaml_1_hour_f030c3d6}" Tag="60" Click="OnTrayPauseEnforcement" />
                        </MenuItem>
                    </MenuItem>
                    <Separator />
                    <MenuItem Header="{svc:Loc Key=Xaml_Exit_f83b6fe3}" Click="OnTrayExit" />
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>

        <DockPanel>
            <DockPanel.LayoutTransform>
                <ScaleTransform ScaleX="{Binding UiScale}" ScaleY="{Binding UiScale}" />
            </DockPanel.LayoutTransform>

            <!-- Menu bar -->
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="{svc:Loc Key=Menu_File, Default=File}">
                    <MenuItem Header="{svc:Loc Key=Xaml_Import_hosts_fileâ_fcc431cd}" Command="{Binding ImportHostsFileCommand}"
                              ToolTip="{svc:Loc Key=Xaml_Replace_the_live_hosts_file_with_b6081ef3}" />
                    <MenuItem Header="{svc:Loc Key=Xaml_Export_hosts_fileâ_cb20a33b}" Command="{Binding ExportHostsFileCommand}" />
                    <MenuItem Header="{svc:Loc Key=Xaml_Export_managed_domains_JSON_â_f2f0acf7}" Command="{Binding ExportDomainsCommand}" />
                    <MenuItem Header="{svc:Loc Key=Xaml_Export_AI_knowledge_c58d27a3}" Command="{Binding Tools.ExportAiKnowledgeCommand}" />
                    <Separator />
                    <MenuItem Header="{svc:Loc Key=Xaml_Export_policy_JSON_â_2b2d496c}" Command="{Binding ExportPolicyCommand}"
                              ToolTip="{svc:Loc Key=Xaml_Save_the_whole_policy_â_domains_f8586b5d}" />
                    <MenuItem Header="{svc:Loc Key=Xaml_Import_policy_JSON_â_f74619f8}" Command="{Binding ImportPolicyCommand}"
                              ToolTip="{svc:Loc Key=Xaml_Reconstruct_the_policy_from_an_exported_d0d84c0e}" />
                    <MenuItem Header="{svc:Loc Key=Xaml_Restore_policy_checkpoint_74b7d23a, Default=Restore policy checkpoint}" Command="{Binding RestorePolicyCheckpointCommand}"
                              ToolTip="{svc:Loc Key=Xaml_Restore_the_latest_policy_state_captured_74b7d23a, Default=Restore the latest policy state captured before an import}" />
                    <Separator />
                    <MenuItem Header="{svc:Loc Key=Xaml_Save_hosts_file_d57ecafc}" Command="{Binding RawHosts.SaveCommand}"
                              ToolTip="{svc:Loc Key=Xaml_Write_unsaved_Raw_Editor_changes_to_bdb2f9f1}" />
                    <MenuItem Header="{svc:Loc Key=Xaml_Backup_hosts_file_b631b0d3}" Command="{Binding Tools.BackupHostsCommand}" />
                    <Separator />
                    <MenuItem Header="{svc:Loc Key=Xaml_Exit_f83b6fe3}" Click="OnTrayExit" />
                </MenuItem>
                <MenuItem Header="{svc:Loc Key=Menu_View, Default=View}">
                    <MenuItem Header="{svc:Loc Key=Xaml_Group_by_root_activity_db0a88c0}" IsCheckable="True" IsChecked="{Binding Activity.GroupByRoot}" />
                    <MenuItem Header="{svc:Loc Key=Xaml_Hide_blocked_activity_c4c8a89e}" IsCheckable="True" IsChecked="{Binding Activity.HideBlocked}" />
                    <MenuItem Header="{svc:Loc Key=Xaml_Show_hidden_activity_28f65185}" IsCheckable="True" IsChecked="{Binding Activity.ShowHidden}" />
                    <Separator />
                    <MenuItem Header="{svc:Loc Key=Xaml_Group_by_app_connections_731338df}" IsCheckable="True" IsChecked="{Binding FwActivity.GroupByApp}" />
                    <MenuItem Header="{svc:Loc Key=Xaml_Resolve_IPs_connections_79845b86}" IsCheckable="True" IsChecked="{Binding FwActivity.ResolveIps}" />
                    <Separator />
                    <MenuItem Header="{svc:Loc Key=Xaml_UI_scale_b24ae33d}" ItemsSource="{x:Static vm:MainViewModel.UiScaleChoices}">
                        <MenuItem.ItemContainerStyle>
                            <Style TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
                                <Setter Property="Header" Value="{Binding}" />
                                <Setter Property="HeaderStringFormat" Value="{}{0}%" />
                                <Setter Property="Command"
                                        Value="{Binding DataContext.SetScaleCommand, RelativeSource={RelativeSource AncestorType=Menu}}" />
                                <Setter Property="CommandParameter" Value="{Binding}" />
                            </Style>
                        </MenuItem.ItemContainerStyle>
                    </MenuItem>
                    <MenuItem Header="{svc:Loc Key=Xaml_Toggle_theme_9b0eaf14}" Command="{Binding ToggleThemeCommand}" />
                    <MenuItem x:Name="LanguageMenu"
                              Header="{svc:Loc Key=Menu_Language, Default=Language}"
                              ItemsSource="{x:Static svc:AppConfigStore.LanguageOptions}">
                        <MenuItem.ItemContainerStyle>
                            <Style TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
                                <Setter Property="Header" Value="{Binding Name}" />
                                <EventSetter Event="Click" Handler="OnLanguageMenuClick" />
                            </Style>
                        </MenuItem.ItemContainerStyle>
                    </MenuItem>
                    <Separator />
                    <MenuItem Header="{svc:Loc Key=Xaml_Reset_view_80d8db7a}" Command="{Binding ResetViewCommand}"
                              ToolTip="{svc:Loc Key=Xaml_Clear_every_filter_and_toggle_back_ef9c2076}" />
                    <MenuItem Header="{svc:Loc Key=Xaml_Refresh_all_bc959b88}" Command="{Binding RefreshAllCommand}" />
                </MenuItem>
                <MenuItem Header="{svc:Loc Key=Xaml_Tools_c32e69db}">
                    <MenuItem Header="{svc:Loc Key=Xaml_Run_diagnostics_2d1e4905}" Command="{Binding RunDiagnosticsCommand}" />
                    <MenuItem Header="{svc:Loc Key=Xaml_Flush_DNS_15214212}" Command="{Binding Tools.FlushDnsCommand}" />
                    <MenuItem Header="{svc:Loc Key=Xaml_Export_support_bundle_5b5b35e2}" Command="{Binding Tools.ExportBundleCommand}" />
                    <Separator />
                    <MenuItem Header="{svc:Loc Key=Xaml_Check_for_updates_736b9062}" Command="{Binding CheckForUpdatesCommand}" />
                </MenuItem>
                <MenuItem Header="{svc:Loc Key=Menu_Help, Default=Help}">
                    <MenuItem Header="{svc:Loc Key=Xaml_Check_for_updates_736b9062}" Command="{Binding CheckForUpdatesCommand}" />
                    <MenuItem Header="{svc:Loc Key=Xaml_Open_GitHub_repository_76131fab}" Click="OnOpenGitHub" />
                    <MenuItem Header="{svc:Loc Key=Xaml_About_HostsGuard_5f701f9a}" Click="OnAbout" />
                </MenuItem>
            </Menu>

            <Border DockPanel.Dock="Top" Style="{StaticResource Hg.TopBand}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <Path Width="17" Height="17" Stretch="Uniform"
                              Data="M 8 2 A 6 6 0 1 1 8 14 A 6 6 0 1 1 8 2 M 5 8 H 11"
                              Stroke="{DynamicResource Hg.Dim}"
                              StrokeThickness="1.7"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"
                              StrokeLineJoin="Round"
                              Margin="0,0,9,0" />
                        <TextBlock Text="{svc:Loc Key=TopBand_System}" Style="{StaticResource Hg.SubText}" VerticalAlignment="Center" Margin="0,0,18,0" />
                        <Path Width="17" Height="17" Stretch="Uniform"
                              Data="M 8 1 L 14 3.5 V 8 C 14 11 11.5 13 8 15 C 4.5 13 2 11 2 8 V 3.5 Z M 5 8 L 7 10 L 11 5.5"
                              Stroke="{DynamicResource Hg.Green}"
                              Fill="Transparent"
                              StrokeThickness="1.7"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"
                              StrokeLineJoin="Round"
                              Margin="0,0,8,0" />
                        <TextBlock Text="{svc:Loc Key=TopBand_SafePosture}" Foreground="{DynamicResource Hg.Green}" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,16,0" />
                        <TextBlock Text="{Binding FilteringModeTitle}" Foreground="{DynamicResource Hg.Green}" VerticalAlignment="Center" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Style="{StaticResource Hg.SafeButton}"
                                Command="{Binding RestoreSafeNetworkPostureCommand}"
                                IsEnabled="{Binding IsConnected}"
                                ToolTip="{svc:Loc Key=TopBand_RestoreSafeTip}"
                                AutomationProperties.Name="{svc:Loc Key=TopBand_RestoreSafe}">
                            <StackPanel Orientation="Horizontal">
                                <Path Style="{StaticResource Hg.ButtonGlyphPath}"
                                      Stroke="{DynamicResource Hg.Green}"
                                      Data="M 4 8 A 4 4 0 0 1 11 5 M 11 5 H 8 M 11 5 V 2 M 12 8 A 4 4 0 0 1 5 11 M 5 11 H 8 M 5 11 V 14" />
                                <TextBlock Text="{svc:Loc Key=TopBand_RestoreSafe}" Margin="8,0,0,0"
                                           MaxWidth="180" TextWrapping="Wrap" TextAlignment="Center"
                                           Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" />
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource Hg.TopBandButton}"
                                Command="{Binding Tools.FlushDnsCommand}"
                                IsEnabled="{Binding IsConnected}"
                                Margin="8,0,0,0"
                                ToolTip="{svc:Loc Key=Xaml_Flush_DNS_15214212}"
                                AutomationProperties.Name="{svc:Loc Key=Xaml_Flush_DNS_15214212}">
                            <StackPanel Orientation="Horizontal">
                                <Path Style="{StaticResource Hg.ButtonGlyphPath}"
                                      Data="M 4 8 A 4 4 0 0 1 11 5 M 11 5 H 8 M 11 5 V 2 M 12 8 A 4 4 0 0 1 5 11 M 5 11 H 8 M 5 11 V 14" />
                                <TextBlock Text="{svc:Loc Key=Xaml_Flush_DNS_15214212}" Margin="8,0,0,0"
                                           MaxWidth="100" TextWrapping="Wrap" TextAlignment="Center"
                                           Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" />
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource Hg.TopBandButton}"
                                Command="{Binding RunDiagnosticsCommand}"
                                IsEnabled="{Binding IsConnected}"
                                Margin="8,0,0,0"
                                ToolTip="{svc:Loc Key=Xaml_Run_diagnostics_2d1e4905}"
                                AutomationProperties.Name="{svc:Loc Key=Xaml_Run_diagnostics_2d1e4905}">
                            <Path Style="{StaticResource Hg.ButtonGlyphPath}"
                                  Data="M 8 2 A 6 6 0 1 1 8 14 A 6 6 0 1 1 8 2 M 8 5 A 3 3 0 1 0 8 11 A 3 3 0 1 0 8 5" />
                        </Button>
                        <Button Style="{StaticResource Hg.TopBandButton}"
                                Command="{Binding CheckForUpdatesCommand}"
                                Margin="8,0,0,0"
                                ToolTip="{svc:Loc Key=Xaml_Check_for_updates_736b9062}"
                                AutomationProperties.Name="{svc:Loc Key=Xaml_Check_for_updates_736b9062}">
                            <Path Style="{StaticResource Hg.ButtonGlyphPath}"
                                  Data="M 8 2 V 4 M 8 12 V 14 M 2 8 H 4 M 12 8 H 14 M 4 4 L 5.5 5.5 M 10.5 10.5 L 12 12 M 12 4 L 10.5 5.5 M 5.5 10.5 L 4 12" />
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Status bar -->
            <StatusBar DockPanel.Dock="Bottom">
                <StatusBarItem>
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="9" Height="9" Margin="4,0,7,0" VerticalAlignment="Center">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="{DynamicResource Hg.Red}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                            <Setter Property="Fill" Value="{DynamicResource Hg.Green}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock Text="{Binding ConnectionText}"
                                   Style="{StaticResource Hg.SubText}"
                                   AutomationProperties.LiveSetting="Polite" />
                    </StackPanel>
                </StatusBarItem>
                <Separator />
                <StatusBarItem>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Style="{StaticResource Hg.SubText}"
                                   Text="{Binding HostsBlocked, Mode=OneWay, StringFormat='Hosts file: {0}'}"
                                   ToolTip="{svc:Loc Key=Xaml_Blocked_entries_currently_written_to_the_13a1cafc}" />
                        <TextBlock Style="{StaticResource Hg.SubText}" Margin="14,0,0,0"
                                   Text="{Binding DbBlocked, Mode=OneWay, StringFormat='Blocked: {0}'}"
                                   ToolTip="{svc:Loc Key=Xaml_Managed_domains_set_to_blocked_ce919f47}" />
                        <TextBlock Style="{StaticResource Hg.SubText}" Margin="14,0,0,0"
                                   Text="{Binding DbAllowed, Mode=OneWay, StringFormat='Allowed: {0}'}"
                                   ToolTip="{svc:Loc Key=Xaml_Managed_domains_set_to_allowed_â_2b3fb277}" />
                    </StackPanel>
                </StatusBarItem>
                <Separator />
                <StatusBarItem>
                    <TextBlock Text="{Binding FilteringModeText}" Style="{StaticResource Hg.SubText}"
                               AutomationProperties.LiveSetting="Polite"
                               ToolTip="{svc:Loc Key=Xaml_Filtering_mode_â_switch_from_the_340a5c61}" />
                </StatusBarItem>
                <StatusBarItem HorizontalAlignment="Right">
                    <TextBlock Text="{Binding ServiceVersion, StringFormat='service {0}'}"
                               Style="{StaticResource Hg.DimText}" />
                </StatusBarItem>
            </StatusBar>

            <!-- Main workbench -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" MinWidth="264" MaxWidth="340" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource Hg.SideRail}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <ScrollViewer Grid.Row="0"
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled"
                                      Padding="0,0,2,0">
                            <StackPanel>
                                <Grid Margin="4,4,4,12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Style="{StaticResource Hg.BrandMark}">
                                        <Viewbox Stretch="Uniform">
                                            <Grid Width="32" Height="36">
                                                <Path Data="M 16 2 L 29 7 V 17 C 29 24 23.5 29.5 16 34 C 8.5 29.5 3 24 3 17 V 7 Z"
                                                      Stroke="{DynamicResource Hg.Teal}"
                                                      Fill="Transparent"
                                                      StrokeThickness="2.4"
                                                      StrokeStartLineCap="Round"
                                                      StrokeEndLineCap="Round"
                                                      StrokeLineJoin="Round" />
                                                <Path Data="M 10 17 L 14 21 L 22.5 12"
                                                      Stroke="{DynamicResource Hg.Green}"
                                                      StrokeThickness="2.6"
                                                      StrokeStartLineCap="Round"
                                                      StrokeEndLineCap="Round"
                                                      StrokeLineJoin="Round" />
                                            </Grid>
                                        </Viewbox>
                                    </Border>
                                    <StackPanel Grid.Column="1"
                                                Margin="12,0,0,0"
                                                VerticalAlignment="Center">
                                        <TextBlock Text="{svc:Loc Key=Xaml_HostsGuard_bf992a7e}"
                                                   Style="{StaticResource Hg.BrandName}" />
                                        <TextBlock Text="{Binding AppVersion, StringFormat='v{0}'}"
                                                   Style="{StaticResource Hg.BrandVersion}" />
                                    </StackPanel>
                                </Grid>

                                <Border Style="{StaticResource Hg.RailDivider}" />

                                <Border Style="{StaticResource Hg.RailTile}">
                                    <StackPanel>
                                        <TextBlock Text="{svc:Loc Key=Xaml_Service_health_49b31512}" Style="{StaticResource Hg.Kicker}" />
                                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                            <Ellipse Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center">
                                                <Ellipse.Style>
                                                    <Style TargetType="Ellipse">
                                                        <Setter Property="Fill" Value="{DynamicResource Hg.Red}" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                                                <Setter Property="Fill" Value="{DynamicResource Hg.Green}" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Ellipse.Style>
                                            </Ellipse>
                                            <TextBlock Text="{Binding ConnectionStateTitle}"
                                                       Style="{StaticResource Hg.RailValue}"
                                                       Foreground="{DynamicResource Hg.Green}" />
                                        </StackPanel>
                                        <TextBlock Text="{svc:Loc Key=Xaml_HostsGuardSvc_cfea64c8}" Style="{StaticResource Hg.DimText}" Margin="0,8,0,0" />
                                        <TextBlock Text="{svc:Loc Key=Xaml_Running_73989d9c}"
                                                   Visibility="{Binding IsConnected, Converter={StaticResource BoolToVis}}"
                                                   Style="{StaticResource Hg.RailActionText}"
                                                   Foreground="{DynamicResource Hg.Green}" />
                                        <TextBlock Text="{Binding ServiceVersion, StringFormat='v{0}', TargetNullValue={svc:Loc Key=Xaml_ServiceOfflineTargetNull}}"
                                                   Style="{StaticResource Hg.DimText}" />
                                        <Button Content="{svc:Loc Key=Xaml_Reconnect_6988b16a}"
                                                Command="{Binding ConnectCommand}"
                                                Visibility="{Binding IsConnected, Converter={StaticResource InverseBoolToVis}}"
                                                Margin="0,10,0,0"
                                                HorizontalAlignment="Stretch" />
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource Hg.RailTile}">
                                    <StackPanel>
                                        <TextBlock Text="{svc:Loc Key=Xaml_Filtering_mode_10d184c2}" Style="{StaticResource Hg.Kicker}" />
                                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                            <Path Style="{StaticResource Hg.RailGlyphPath}"
                                                  Data="M 8 2 L 14 8 L 8 14 L 2 8 Z"
                                                  Stroke="{DynamicResource Hg.Teal}" />
                                            <TextBlock Text="{Binding FilteringModeTitle}"
                                                       Style="{StaticResource Hg.RailValue}"
                                                       Foreground="{DynamicResource Hg.Teal}" />
                                        </StackPanel>
                                        <TextBlock Text="{Binding FilteringModeText}"
                                                   Style="{StaticResource Hg.HelperText}"
                                                   Margin="0,6,0,10" />
                                        <Button Content="{svc:Loc Key=Xaml_Change_mode_2266c567}"
                                                Command="{Binding SetFilteringModeCommand}"
                                                CommandParameter="notify"
                                                HorizontalAlignment="Stretch"
                                                Padding="7,3"
                                                ToolTip="{svc:Loc Key=Xaml_Switch_to_Notify_mode_143de68d}" />
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource Hg.RailTile}">
                                    <StackPanel>
                                        <TextBlock Text="{svc:Loc Key=Xaml_Global_posture_3b00dca1}" Style="{StaticResource Hg.Kicker}" />
                                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                            <Path Style="{StaticResource Hg.RailGlyphPath}"
                                                  Data="M 8 2 L 14 8 L 8 14 L 2 8 Z M 5 8 H 11"
                                                  Stroke="{DynamicResource Hg.Teal}" />
                                            <TextBlock Text="{Binding EnforcementPauseTitle}"
                                                       Style="{StaticResource Hg.RailValue}"
                                                       Foreground="{DynamicResource Hg.Teal}" />
                                        </StackPanel>
                                        <TextBlock Text="{Binding EnforcementPauseText}" Style="{StaticResource Hg.DimText}" Margin="0,6,0,10" />
                                        <Button
                                                Command="{Binding RestoreSafeNetworkPostureCommand}"
                                                IsEnabled="{Binding IsConnected}"
                                                Style="{StaticResource Hg.SafeButton}"
                                                HorizontalAlignment="Stretch"
                                                Margin="0,0,0,8"
                                                ToolTip="{svc:Loc Key=TopBand_RestoreSafeTip}">
                                            <TextBlock Text="{svc:Loc Key=TopBand_RestoreSafe}" TextWrapping="Wrap" TextAlignment="Center" />
                                        </Button>
                                        <Button
                                                Command="{Binding SetGlobalModeCommand}"
                                                CommandParameter="block-all"
                                                Style="{StaticResource Hg.DangerButton}"
                                                HorizontalAlignment="Stretch"
                                                Padding="7,3"
                                                ToolTip="{svc:Loc Key=Xaml_Block_new_outbound_traffic_unless_an_9797ef1d}">
                                            <TextBlock Text="{svc:Loc Key=Xaml_Block_all_outbound_b110b88e}" TextWrapping="Wrap" TextAlignment="Center" />
                                        </Button>
                                        <TextBlock Text="{svc:Loc Key=Xaml_Pause_enforcement_8c943b55}" Style="{StaticResource Hg.HelperText}" Margin="0,8,0,4" />
                                        <WrapPanel>
                                            <Button
                                                    Command="{Binding PauseEnforcementCommand}"
                                                    CommandParameter="5"
                                                    Padding="7,3"
                                                    Margin="0,0,6,6"><TextBlock Text="{svc:Loc Key=Xaml_5_minutes_34e17f47}" TextWrapping="Wrap" /></Button>
                                            <Button
                                                    Command="{Binding PauseEnforcementCommand}"
                                                    CommandParameter="15"
                                                    Padding="7,3"
                                                    Margin="0,0,6,6"><TextBlock Text="{svc:Loc Key=Xaml_15_minutes_899761ff}" TextWrapping="Wrap" /></Button>
                                            <Button
                                                    Command="{Binding PauseEnforcementCommand}"
                                                    CommandParameter="60"
                                                    Padding="7,3"
                                                    Margin="0,0,0,6"><TextBlock Text="{svc:Loc Key=Xaml_1_hour_f030c3d6}" TextWrapping="Wrap" /></Button>
                                        </WrapPanel>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource Hg.RailTile}">
                                    <StackPanel>
                                        <TextBlock Text="{svc:Loc Key=Xaml_Alert_inbox_2be348dd}" Style="{StaticResource Hg.Kicker}" />
                                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                            <Path Style="{StaticResource Hg.RailGlyphPath}"
                                                  Data="M 8 2 L 14 13 H 2 Z M 8 6 V 9 M 8 11 H 8"
                                                  Stroke="{DynamicResource Hg.Peach}" />
                                            <TextBlock Text="{Binding Alerts.UnreadTitle}"
                                                       Style="{StaticResource Hg.RailValue}"
                                                       Foreground="{DynamicResource Hg.Peach}" />
                                        </StackPanel>
                                        <TextBlock Text="{Binding Alerts.UnreadText}" Style="{StaticResource Hg.DimText}" Margin="0,6,0,10" />
                                        <Button Content="{svc:Loc Key=Xaml_Refresh_56e3badc}"
                                                Command="{Binding Alerts.RefreshCommand}"
                                                HorizontalAlignment="Stretch"
                                                Padding="7,3"
                                                ToolTip="{svc:Loc Key=Xaml_Stateful_low_volume_alerts_for_security_relevant_97175abd}" />
                                    </StackPanel>
                                </Border>

                            </StackPanel>
                        </ScrollViewer>

                        <StackPanel Grid.Row="1">
                            <Border Style="{StaticResource Hg.RailDivider}" Margin="0,6,0,8" />
                            <UniformGrid Columns="3">
                                <Button Width="38"
                                        Height="32"
                                        MinHeight="32"
                                        Padding="0"
                                        Command="{Binding RefreshAllCommand}"
                                        ToolTip="{svc:Loc Key=Xaml_Refresh_all_bc959b88}"
                                        AutomationProperties.Name="{svc:Loc Key=Xaml_Refresh_all_bc959b88}">
                                    <Path Style="{StaticResource Hg.ButtonGlyphPath}"
                                          Data="M 4 8 A 4 4 0 0 1 11 5 M 11 5 H 8 M 11 5 V 2 M 12 8 A 4 4 0 0 1 5 11 M 5 11 H 8 M 5 11 V 14" />
                                </Button>
                                <Button Width="38"
                                        Height="32"
                                        MinHeight="32"
                                        Padding="0"
                                        Command="{Binding ToggleThemeCommand}"
                                        Margin="6,0,0,0"
                                        ToolTip="{svc:Loc Key=Xaml_Switch_theme_9a9a20fa}"
                                        AutomationProperties.Name="{svc:Loc Key=Xaml_Switch_theme_9a9a20fa}">
                                    <Path Style="{StaticResource Hg.ButtonGlyphPath}"
                                          Data="M 11 2 A 6 6 0 1 0 14 11 A 5 5 0 0 1 11 2" />
                                </Button>
                                <Button Width="38"
                                        Height="32"
                                        MinHeight="32"
                                        Padding="0"
                                        Command="{Binding ResetViewCommand}"
                                        Margin="6,0,0,0"
                                        ToolTip="{svc:Loc Key=Xaml_Reset_view_80d8db7a}"
                                        AutomationProperties.Name="{svc:Loc Key=Xaml_Reset_view_80d8db7a}">
                                    <Path Style="{StaticResource Hg.ButtonGlyphPath}"
                                          Data="M 4 5 H 12 M 4 8 H 12 M 4 11 H 12" />
                                </Button>
                            </UniformGrid>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource Hg.ShellSurface}">
                    <Grid>
            <TabControl x:Name="MainTabs"
                        Style="{StaticResource Hg.PrimaryTabs}"
                        ItemContainerStyle="{StaticResource Hg.PrimaryTabItem}"
                        Margin="0"
                        IsEnabled="{Binding IsConnected}">
                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <Path Style="{StaticResource Hg.MainTabIconPath}"
                                  Data="M 8 2 A 6 6 0 1 1 8 14 A 6 6 0 1 1 8 2 M 8 5 A 3 3 0 1 0 8 11 A 3 3 0 1 0 8 5"
                                  Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=TabItem}}" />
                            <TextBlock Text="{svc:Loc Key=Tab_HostsActivity, Default=Hosts Activity}"
                                       TextWrapping="Wrap" TextAlignment="Center" MaxWidth="100" />
                        </StackPanel>
                    </TabItem.Header>
                    <Grid Margin="4" DataContext="{Binding Activity}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" MinHeight="180" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Style="{StaticResource Hg.CommandBar}" Margin="0,0,0,8">
                        <WrapPanel VerticalAlignment="Center">
                            <TextBox Text="{Binding Filter, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource Hg.HintBox}"
                                     Width="160"
                                     Tag="{svc:Loc Key=Xaml_SearchDomainsHint}"
                                     AutomationProperties.Name="{svc:Loc Key=Xaml_Search_DNS_activity_c131610b}"
                                     ToolTip="{svc:Loc Key=Xaml_Filters_as_you_type_supports_field_900279c6}" />
                            <Button
                                    Command="{Binding ResearchPurposesCommand}"
                                    Padding="10,5"
                                    Margin="6,0,0,0"
                                    ToolTip="{svc:Loc Key=Xaml_Have_DeepSeek_research_what_each_domain_68c1899f}">
                                <TextBlock Text="{svc:Loc Key=Xaml_AI_purposes_9aa2d27e}" TextWrapping="Wrap" />
                            </Button>
                            <Button Content="{svc:Loc Key=Xaml_Block_82dd2cdf}"
                                    Style="{StaticResource Hg.DangerButton}"
                                    Padding="10,5"
                                    Command="{Binding BlockSelectedCommand}"
                                    CommandParameter="{Binding SelectedItems, ElementName=ActivityGrid}"
                                    IsEnabled="{Binding SelectedItem, ElementName=ActivityGrid, Converter={StaticResource ObjectPresentBool}}"
                                    Margin="6,0,0,0" />
                            <Button Content="{svc:Loc Key=Xaml_Allow_3ad0e369}"
                                    Style="{StaticResource Hg.SuccessButton}"
                                    Padding="10,5"
                                    Command="{Binding AllowSelectedCommand}"
                                    CommandParameter="{Binding SelectedItems, ElementName=ActivityGrid}"
                                    IsEnabled="{Binding SelectedItem, ElementName=ActivityGrid, Converter={StaticResource ObjectPresentBool}}"
                                    Margin="6,0,8,0" />
                            <CheckBox Content="{svc:Loc Key=Xaml_Reverse_DNS_9fc5dae3}" Margin="6,0,0,0"
                                      VerticalAlignment="Center"
                                      IsChecked="{Binding HideReverseDns}"
                                      AutomationProperties.Name="{svc:Loc Key=Xaml_Hide_reverse_DNS_PTR_lookups_c24359ac}"
                                      ToolTip="{svc:Loc Key=Xaml_Hide_reverse_DNS_PTR_lookups_in_667e1814}" />
                            <CheckBox Content="{svc:Loc Key=Xaml_Group_root_b54b32ae}" Margin="6,0,0,0"
                                      VerticalAlignment="Center"
                                      IsChecked="{Binding GroupByRoot}"
                                      AutomationProperties.Name="{svc:Loc Key=Xaml_Group_feed_rows_by_root_domain_5a130da0}"
                                      ToolTip="{svc:Loc Key=Xaml_Collapse_subdomains_under_their_root_ca66b5fb}" />
                            <CheckBox Content="{svc:Loc Key=Xaml_Blocked_99613c74}" Margin="6,0,0,0"
                                      VerticalAlignment="Center"
                                      IsChecked="{Binding BlockedOnly}"
                                      AutomationProperties.Name="{svc:Loc Key=Xaml_Show_only_blocked_domains_0ccd7e40}" />
                            <CheckBox Margin="6,0,0,0"
                                      VerticalAlignment="Center"
                                      IsChecked="{Binding HideBlocked}"
                                      AutomationProperties.Name="{svc:Loc Key=Xaml_Hide_already_blocked_domains_c28c44e2}">
                                <TextBlock Text="{svc:Loc Key=Xaml_Hide_blocked_3a8dacc1}" TextWrapping="Wrap" MaxWidth="110" />
                            </CheckBox>
                            <CheckBox Content="{svc:Loc Key=Xaml_Hidden_d4c2792a}" Margin="6,0,0,0"
                                      VerticalAlignment="Center"
                                      IsChecked="{Binding ShowHidden}" />
                            <Button Content="{svc:Loc Key=Xaml_Refresh_56e3badc}"
                                    Command="{Binding RefreshCommand}"
                                    Padding="10,5"
                                    Margin="6,0,0,0" />
                        </WrapPanel>
                        </Border>

                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="3*" />
                                <ColumnDefinition Width="2*" MinWidth="250" MaxWidth="360" />
                            </Grid.ColumnDefinitions>

                        <DataGrid Grid.Column="0" x:Name="ActivityGrid" ItemsSource="{Binding RowsView}"
                                  SelectedIndex="0"
                                  AutomationProperties.Name="{svc:Loc Key=Xaml_DNS_activity_feed_3e3cfef1}"
                                  IsVisibleChanged="OnGridVisible">
                            <DataGrid.GroupStyle>
                                <!-- Flat section headers: a styled band per root with the
                                     normal grid rows directly beneath - same layout as the
                                     ungrouped feed, just sectioned. Right-click hides the root. -->
                                <GroupStyle>
                                    <GroupStyle.ContainerStyle>
                                        <Style TargetType="{x:Type GroupItem}">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="{x:Type GroupItem}">
                                                        <StackPanel>
                                                            <Border Background="{DynamicResource Hg.Mantle}"
                                                                    BorderBrush="{DynamicResource Hg.S0}"
                                                                    BorderThickness="0,1,0,1"
                                                                    Padding="8,4" Cursor="Hand"
                                                                    Tag="{Binding RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                                    ToolTip="{svc:Loc Key=Xaml_Right_click_to_hide_this_group_8b651c7a}">
                                                                <Border.ContextMenu>
                                                                    <ContextMenu>
                                                                        <MenuItem Header="{svc:Loc Key=Xaml_Hide_this_group_cc5d662f}"
                                                                                  Command="{Binding PlacementTarget.Tag.DataContext.HideGroupCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                                                  CommandParameter="{Binding Name}"
                                                                                  ToolTip="{svc:Loc Key=Xaml_Hide_every_domain_currently_listed_under_28de8cef}" />
                                                                    </ContextMenu>
                                                                </Border.ContextMenu>
                                                                <StackPanel Orientation="Horizontal">
                                                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold"
                                                                               Foreground="{DynamicResource Hg.Blue}" />
                                                                    <TextBlock Text="{Binding ItemCount, StringFormat=' - {0} domains'}"
                                                                               Style="{StaticResource Hg.DimText}" />
                                                                </StackPanel>
                                                            </Border>
                                                            <ItemsPresenter />
                                                        </StackPanel>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </GroupStyle.ContainerStyle>
                                </GroupStyle>
                            </DataGrid.GroupStyle>
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Setter Property="Foreground" Value="{DynamicResource Hg.Text}" />
                                    <Setter Property="MinHeight" Value="32" />
                                    <Setter Property="ToolTip" Value="{Binding Reason}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Status}" Value="blocked">
                                            <Setter Property="Background" Value="{DynamicResource Hg.DangerSoft}" />
                                        </DataTrigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource Hg.RowHover}" />
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource Hg.Sel}" />
                                        </Trigger>
                                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource Hg.Sel}" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>
                            <DataGrid.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="{svc:Loc Key=Xaml_Block_82dd2cdf}"
                                              Command="{Binding PlacementTarget.DataContext.BlockSelectedCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.SelectedItems, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              ToolTip="{svc:Loc Key=Xaml_Block_every_selected_domain_multi_select_01215dea}" />
                                    <MenuItem Header="{svc:Loc Key=Xaml_Block_root_6c8978ac}"
                                              Command="{Binding PlacementTarget.DataContext.BlockRootCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.SelectedItem.Domain, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                    <MenuItem Header="{svc:Loc Key=Xaml_Allow_3ad0e369}"
                                              Command="{Binding PlacementTarget.DataContext.AllowSelectedCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.SelectedItems, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              ToolTip="{svc:Loc Key=Xaml_Allow_every_selected_domain_â_remove_85f98590}" />
                                    <MenuItem Header="{svc:Loc Key=Xaml_Unblock_remove_from_hosts_da345a55}"
                                              Command="{Binding PlacementTarget.DataContext.UnblockSelectedCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.SelectedItems, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              ToolTip="{svc:Loc Key=Xaml_Remove_the_selected_domains_0_0_f3c604ab}" />
                                    <MenuItem Header="{svc:Loc Key=Xaml_Temp_allow_dcb5b93d}">
                                        <MenuItem Header="{svc:Loc Key=Xaml_15_minutes_899761ff}"
                                                  Command="{Binding PlacementTarget.DataContext.TempAllow15Command, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding PlacementTarget.SelectedItem.Domain, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                        <MenuItem Header="{svc:Loc Key=Xaml_1_hour_f030c3d6}"
                                                  Command="{Binding PlacementTarget.DataContext.TempAllow60Command, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding PlacementTarget.SelectedItem.Domain, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                        <MenuItem Header="{svc:Loc Key=Xaml_8_hours_191800b7}"
                                                  Command="{Binding PlacementTarget.DataContext.TempAllow480Command, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding PlacementTarget.SelectedItem.Domain, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                    </MenuItem>
                                    <MenuItem Header="{svc:Loc Key=Xaml_Temp_block_e7a19c42}">
                                        <MenuItem Header="{svc:Loc Key=Xaml_15_minutes_899761ff}"
                                                  Command="{Binding PlacementTarget.DataContext.TempBlock15Command, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding PlacementTarget.SelectedItem.Domain, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                        <MenuItem Header="{svc:Loc Key=Xaml_1_hour_f030c3d6}"
                                                  Command="{Binding PlacementTarget.DataContext.TempBlock60Command, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding PlacementTarget.SelectedItem.Domain, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                        <MenuItem Header="{svc:Loc Key=Xaml_8_hours_191800b7}"
                                                  Command="{Binding PlacementTarget.DataContext.TempBlock480Command, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding PlacementTarget.SelectedItem.Domain, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                    </MenuItem>
                                    <Separator />
                                    <MenuItem Header="{svc:Loc Key=Xaml_Hide_domain_s_5312c84d}"
                                              Command="{Binding PlacementTarget.DataContext.HideSelectedCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.SelectedItems, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              ToolTip="{svc:Loc Key=Xaml_Hide_every_selected_domain_from_the_c9919958}" />
                                    <MenuItem Header="{svc:Loc Key=Xaml_Hide_root_768c2f5a}"
                                              Command="{Binding PlacementTarget.DataContext.HideRootCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.SelectedItem.Domain, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              ToolTip="{svc:Loc Key=Xaml_Hide_the_whole_root_including_future_f3fb6200}" />
                                    <MenuItem Header="{svc:Loc Key=Xaml_Hide_this_group_cc5d662f}"
                                              Command="{Binding PlacementTarget.DataContext.HideGroupCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.SelectedItem.Root, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              ToolTip="{svc:Loc Key=Xaml_Hide_every_domain_currently_listed_under_bd945a60}" />
                                    <MenuItem Header="{svc:Loc Key=Xaml_Unhide_root_ffc85337}"
                                              Command="{Binding PlacementTarget.DataContext.UnhideRootCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.SelectedItem.Domain, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                    <Separator />
                                    <MenuItem Header="{svc:Loc Key=Xaml_Fix_purposeâ_226cec91}"
                                              Command="{Binding PlacementTarget.DataContext.FixPurposeCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              ToolTip="{svc:Loc Key=Xaml_Correct_this_domain_s_purpose_â_0a507959}" />
                                    <MenuItem Header="{svc:Loc Key=Xaml_Fix_categoryâ_2b0ad390}"
                                              Command="{Binding PlacementTarget.DataContext.FixCategoryCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              ToolTip="{svc:Loc Key=Xaml_Correct_this_domain_s_category_â_c06fdadd}" />
                                    <Separator />
                                    <MenuItem Header="{svc:Loc Key=Xaml_Research_online_0fdb1e04}">
                                        <MenuItem Header="{svc:Loc Key=Xaml_Google_2b681c0a}"
                                                  Command="{Binding PlacementTarget.DataContext.ResearchGoogleCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding PlacementTarget.SelectedItem.Domain, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                        <MenuItem Header="{svc:Loc Key=Xaml_VirusTotal_d38e68d0}"
                                                  Command="{Binding PlacementTarget.DataContext.ResearchVirusTotalCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding PlacementTarget.SelectedItem.Domain, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                        <MenuItem Header="{svc:Loc Key=Xaml_who_is_42562da6}"
                                                  Command="{Binding PlacementTarget.DataContext.ResearchWhoisCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding PlacementTarget.SelectedItem.Domain, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                    </MenuItem>
                                </ContextMenu>
                            </DataGrid.ContextMenu>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_Domain_9b10914d}" Binding="{Binding Domain}" Width="170">
                                    <DataGridTextColumn.ElementStyle>
                                        <!-- Yellow = undecided domain the reference blocklists would
                                             block: a candidate worth reviewing. -->
                                        <Style TargetType="TextBlock">
                                            <Setter Property="ToolTip" Value="{Binding BlocklistsTip}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding BlocklistsText}" Value="">
                                                    <Setter Property="ToolTip" Value="{x:Null}" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsBlockCandidate}" Value="True">
                                                    <Setter Property="Foreground" Value="{DynamicResource Hg.Yellow}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_Purpose_a0fb821b}" Binding="{Binding Purpose}" Width="135">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{DynamicResource Hg.Dim}" />
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_Status_bae7d5be}" Binding="{Binding StatusText}" Width="80">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{DynamicResource Hg.Dim}" />
                                            <Style.Triggers>
                                                <!-- Blocked stands out red so an over-block is easy to spot when troubleshooting. -->
                                                <DataTrigger Binding="{Binding Status}" Value="allowed">
                                                    <Setter Property="Foreground" Value="{DynamicResource Hg.Green}" />
                                                    <Setter Property="FontWeight" Value="SemiBold" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="blocked">
                                                    <Setter Property="Foreground" Value="{DynamicResource Hg.Red}" />
                                                    <Setter Property="FontWeight" Value="SemiBold" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="">
                                                    <Setter Property="Foreground" Value="{DynamicResource Hg.Sub}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_Process_12f4c13a}" Binding="{Binding Process}" Width="95" />
                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_Hits_a6179b4c}" Binding="{Binding Hits}" Width="45" />
                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_Data_e5e429bc}" Binding="{Binding DataText}" Width="64" IsReadOnly="True" />
                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_New_c2a1f0e3}" Binding="{Binding NewText}" Width="44" IsReadOnly="True">
                                    <DataGridTextColumn.ElementStyle>
                                        <!-- Green "NEW" = first observed on this machine within the window. -->
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{DynamicResource Hg.Green}" />
                                            <Setter Property="FontWeight" Value="SemiBold" />
                                            <Setter Property="FontSize" Value="10" />
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTemplateColumn Header="{svc:Loc Key=Xaml_24h_02779596}" Width="96">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Viewbox Width="84" Height="18" Stretch="Fill" HorizontalAlignment="Left"
                                                     ToolTip="{Binding SparklineTip}">
                                                <Canvas Width="120" Height="20">
                                                    <Polyline Points="{Binding SparklinePoints, Converter={StaticResource PointsConverter}}"
                                                              Stroke="{DynamicResource Hg.Series1}"
                                                              StrokeThickness="1.2" StrokeLineJoin="Round" />
                                                </Canvas>
                                            </Viewbox>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_Last_seen_56462f81}" Binding="{Binding LastSeenText}" Width="75" />
                            </DataGrid.Columns>
                        </DataGrid>

                        <Border Grid.Column="0"
                                Style="{StaticResource Hg.EmptyState}"
                                Visibility="{Binding Rows.Count, Converter={StaticResource EmptyStateVis}}">
                            <StackPanel>
                                <TextBlock Text="{svc:Loc Key=Xaml_No_DNS_activity_yet_edff6649}"
                                           Style="{StaticResource Hg.SectionTitle}"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,4" />
                                <TextBlock Text="{svc:Loc Key=Xaml_HostsGuard_will_fill_this_feed_as_6cba4cd0}"
                                           Style="{StaticResource Hg.EmptyText}" />
                            </StackPanel>
                        </Border>

                            <Border Grid.Column="1"
                                    Style="{StaticResource Hg.Inspector}"
                                    Margin="10,0,0,0"
                                    DataContext="{Binding SelectedItem, ElementName=ActivityGrid}">
                                <Grid>
                                    <StackPanel>
                                        <StackPanel.Style>
                                            <Style TargetType="StackPanel">
                                                <Setter Property="Visibility" Value="Visible" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding}" Value="{x:Null}">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <DockPanel LastChildFill="True">
                                            <Button DockPanel.Dock="Right"
                                                    Style="{StaticResource Hg.CloseIconButton}"
                                                    ToolTip="{svc:Loc Key=Xaml_Clear_selection_247fd631}"
                                                    AutomationProperties.Name="{svc:Loc Key=Xaml_Clear_selection_247fd631}"
                                                    Click="OnClearActivitySelection">
                                                <Path Style="{StaticResource Hg.ButtonGlyphPath}"
                                                      Data="M 4 4 L 12 12 M 12 4 L 4 12"
                                                      Stroke="{DynamicResource Hg.Sub}" />
                                            </Button>
                                            <TextBlock DockPanel.Dock="Left"
                                                       Text="{svc:Loc Key=Xaml_DNS_3ddc21b1}"
                                                       Foreground="{DynamicResource Hg.Teal}"
                                                       FontSize="11"
                                                       FontWeight="SemiBold"
                                                       Margin="0,0,8,0"
                                                       VerticalAlignment="Center" />
                                            <TextBlock Text="{Binding Domain, TargetNullValue={svc:Loc Key=Xaml_SelectDnsRowTargetNull}}"
                                                       FontSize="16"
                                                       FontWeight="SemiBold"
                                                       TextWrapping="Wrap" />
                                        </DockPanel>
                                        <TextBlock Text="{Binding Root, StringFormat='Root: {0}', TargetNullValue={svc:Loc Key=Xaml_DnsRowActionMenuTargetNull}}"
                                                   Style="{StaticResource Hg.DimText}"
                                                   Margin="0,5,0,0"
                                                   TextWrapping="Wrap" />

                                        <Grid Margin="0,12,0,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <Border Grid.Column="0" Style="{StaticResource Hg.Badge}" Margin="0,0,6,0">
                                                <TextBlock Text="{Binding Status, TargetNullValue={svc:Loc Key=Xaml_NoStatusTargetNull}}"
                                                           Style="{StaticResource Hg.DimText}" />
                                            </Border>
                                            <Border Grid.Column="1" Style="{StaticResource Hg.Badge}" Margin="6,0,0,0">
                                                <TextBlock Text="{Binding BlocklistsText, TargetNullValue={svc:Loc Key=Xaml_NoListHitsTargetNull}}"
                                                           Style="{StaticResource Hg.DimText}" />
                                            </Border>
                                        </Grid>

                                        <DockPanel Margin="0,10,0,3" LastChildFill="True">
                                            <Button DockPanel.Dock="Right"
                                                    Command="{Binding DataContext.ResearchPurposesCommand, ElementName=ActivityGrid}"
                                                    Padding="8,2"
                                                    MinHeight="24">
                                                <TextBlock Text="{svc:Loc Key=Xaml_AI_analyze_b541cf78}" TextWrapping="Wrap" TextAlignment="Center" />
                                            </Button>
                                            <TextBlock Text="{svc:Loc Key=Xaml_Purpose_a0fb821b}" Style="{StaticResource Hg.InspectorLabel}" Margin="0" />
                                        </DockPanel>
                                        <TextBlock Text="{Binding Purpose, TargetNullValue={svc:Loc Key=Xaml_NotClassifiedYetTargetNull}}"
                                                   Style="{StaticResource Hg.InspectorValue}" />

                                        <UniformGrid Columns="2" Margin="0,12,0,0">
                                            <Border Style="{StaticResource Hg.MetricTile}">
                                                <StackPanel>
                                                    <TextBlock Text="{svc:Loc Key=Xaml_Hits_a6179b4c}" Style="{StaticResource Hg.Kicker}" />
                                                    <TextBlock Text="{Binding Hits}" Style="{StaticResource Hg.MetricValue}" />
                                                </StackPanel>
                                            </Border>
                                            <Border Style="{StaticResource Hg.MetricTile}" Margin="0,0,0,8">
                                                <StackPanel>
                                                    <TextBlock Text="{svc:Loc Key=Xaml_Last_seen_56462f81}" Style="{StaticResource Hg.Kicker}" />
                                                    <TextBlock Text="{Binding LastSeenText, TargetNullValue='-'}"
                                                               Style="{StaticResource Hg.InspectorValue}" />
                                                </StackPanel>
                                            </Border>
                                        </UniformGrid>

                                        <TextBlock Text="{svc:Loc Key=Xaml_Actions_c3cd636a}" Style="{StaticResource Hg.InspectorLabel}" />
                                        <StackPanel>
                                            <Button
                                                    Style="{StaticResource Hg.SuccessButton}"
                                                    Margin="0,0,0,6"
                                                    HorizontalAlignment="Stretch"
                                                    Command="{Binding DataContext.AllowSelectedCommand, ElementName=ActivityGrid}"
                                                    CommandParameter="{Binding SelectedItems, ElementName=ActivityGrid}"
                                                    IsEnabled="{Binding SelectedItem, ElementName=ActivityGrid, Converter={StaticResource ObjectPresentBool}}">
                                                <TextBlock Text="{svc:Loc Key=Xaml_Allow_domain_2c91d71a}" TextWrapping="Wrap" TextAlignment="Center" />
                                            </Button>
                                            <Button
                                                    Style="{StaticResource Hg.DangerButton}"
                                                    Margin="0,0,0,6"
                                                    HorizontalAlignment="Stretch"
                                                    Command="{Binding DataContext.BlockSelectedCommand, ElementName=ActivityGrid}"
                                                    CommandParameter="{Binding SelectedItems, ElementName=ActivityGrid}"
                                                    IsEnabled="{Binding SelectedItem, ElementName=ActivityGrid, Converter={StaticResource ObjectPresentBool}}">
                                                <TextBlock Text="{svc:Loc Key=Xaml_Block_domain_9fcd1d15}" TextWrapping="Wrap" TextAlignment="Center" />
                                            </Button>
                                            <Button
                                                    Margin="0,0,0,6"
                                                    HorizontalAlignment="Stretch"
                                                    Command="{Binding DataContext.TempAllow60Command, ElementName=ActivityGrid}"
                                                    CommandParameter="{Binding Domain}"
                                                    IsEnabled="{Binding SelectedItem, ElementName=ActivityGrid, Converter={StaticResource ObjectPresentBool}}">
                                                <TextBlock Text="{svc:Loc Key=Xaml_Temp_allow_69178873}" TextWrapping="Wrap" TextAlignment="Center" />
                                            </Button>
                                            <Button
                                                    Margin="0,0,0,6"
                                                    HorizontalAlignment="Stretch"
                                                    Command="{Binding DataContext.HideSelectedCommand, ElementName=ActivityGrid}"
                                                    CommandParameter="{Binding SelectedItems, ElementName=ActivityGrid}"
                                                    IsEnabled="{Binding SelectedItem, ElementName=ActivityGrid, Converter={StaticResource ObjectPresentBool}}">
                                                <TextBlock Text="{svc:Loc Key=Xaml_Hide_domain_5dfed914}" TextWrapping="Wrap" TextAlignment="Center" />
                                            </Button>
                                            <Button Content="{svc:Loc Key=Xaml_Research_be601df2}"
                                                    HorizontalAlignment="Stretch"
                                                    Command="{Binding DataContext.ResearchGoogleCommand, ElementName=ActivityGrid}"
                                                    CommandParameter="{Binding Domain}"
                                                    IsEnabled="{Binding SelectedItem, ElementName=ActivityGrid, Converter={StaticResource ObjectPresentBool}}" />
                                        </StackPanel>
                                    </StackPanel>

                                    <Border Style="{StaticResource Hg.EmptyState}"
                                            Visibility="{Binding Converter={StaticResource EmptyStateVis}}">
                                        <StackPanel>
                                            <TextBlock Text="{svc:Loc Key=Xaml_Select_a_domain_ecec97fc}"
                                                       Style="{StaticResource Hg.SectionTitle}"
                                                       HorizontalAlignment="Center"
                                                       Margin="0,0,0,4" />
                                            <TextBlock Text="{svc:Loc Key=Xaml_The_inspector_keeps_row_details_and_19beece8}"
                                                       Style="{StaticResource Hg.EmptyText}" />
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </Border>
                        </Grid>

                        <TextBlock Grid.Row="2" Margin="2,6,0,0"
                                   Text="{Binding StatusText}"
                                   Style="{StaticResource Hg.DimText}"
                                   AutomationProperties.LiveSetting="Polite" />
                    </Grid>
                </TabItem>

                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <Path Style="{StaticResource Hg.MainTabIconPath}"
                                  Data="M 8 2 L 14 13 H 2 Z M 8 6 V 9 M 8 11 H 8"
                                  Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=TabItem}}" />
                            <TextBlock Text="{svc:Loc Key=Tab_Alerts, Default=Alerts}" />
                        </StackPanel>
                    </TabItem.Header>
                    <Grid Margin="4" DataContext="{Binding Alerts}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Style="{StaticResource Hg.CommandBar}" Margin="0,0,0,8">
                            <WrapPanel VerticalAlignment="Center">
                                <TextBox Text="{Binding TypeFilter, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource Hg.HintBox}"
                                         MinWidth="160" Margin="0,0,8,6"
                                         Tag="{svc:Loc Key=Xaml_Type_filter_6e3b3ab7}"
                                         AutomationProperties.Name="{svc:Loc Key=Xaml_Type_filter_6e3b3ab7}"
                                         ToolTip="{svc:Loc Key=Xaml_Alert_type_exact_filter_095a0f53}" />
                                <CheckBox Content="{svc:Loc Key=Xaml_Surface_only_5f216e59}" Margin="0,0,8,6"
                                          VerticalAlignment="Center"
                                          IsChecked="{Binding SurfaceOnly}"
                                          ToolTip="{svc:Loc Key=Xaml_Show_only_alert_types_configured_to_baa98b18}" />
                                <CheckBox Margin="0,0,8,6" VerticalAlignment="Center"
                                          IsChecked="{Binding IncludeRead}"
                                          ToolTip="{svc:Loc Key=Xaml_Include_acknowledged_alerts_c79eea1f}">
                                    <TextBlock Text="{svc:Loc Key=Xaml_Include_read_23a80646}" TextWrapping="Wrap" MaxWidth="120" />
                                </CheckBox>
                                <Button Command="{Binding AckCommand}"
                                        CommandParameter="{Binding SelectedItem, ElementName=AlertsGrid}"
                                        Margin="0,0,8,6">
                                    <TextBlock Text="{svc:Loc Key=Xaml_Acknowledge_selected_01567827}" TextWrapping="Wrap" TextAlignment="Center" />
                                </Button>
                                <Button Command="{Binding AckAllCommand}" Margin="0,0,8,6">
                                    <TextBlock Text="{svc:Loc Key=Xaml_Acknowledge_all_40e0718f}" TextWrapping="Wrap" TextAlignment="Center" />
                                </Button>
                                <Button Content="{svc:Loc Key=Xaml_Refresh_56e3badc}"
                                        Command="{Binding RefreshCommand}" Margin="8,0,0,0" />
                            </WrapPanel>
                        </Border>

                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="400" />
                            </Grid.ColumnDefinitions>

                            <DataGrid Grid.Column="0" x:Name="AlertsGrid" ItemsSource="{Binding Alerts}"
                                      SelectedItem="{Binding SelectedAlert, Mode=TwoWay}"
                                      AutomationProperties.Name="{svc:Loc Key=Xaml_Low_volume_alerts_cad2ca46}"
                                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                      IsVisibleChanged="OnGridVisible">
                                <DataGrid.RowStyle>
                                    <Style TargetType="DataGridRow">
                                        <Setter Property="Foreground" Value="{DynamicResource Hg.Text}" />
                                        <Setter Property="MinHeight" Value="32" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsRead}" Value="True">
                                                <Setter Property="Opacity" Value="0.62" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Severity}" Value="critical">
                                                <Setter Property="Background" Value="{DynamicResource Hg.DangerRow}" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Severity}" Value="warning">
                                                <Setter Property="Background" Value="{DynamicResource Hg.WarnRow}" />
                                            </DataTrigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource Hg.RowHover}" />
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource Hg.Sel}" />
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGrid.RowStyle>
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="{svc:Loc Key=Xaml_When_769bb19e}" Binding="{Binding UpdatedText}" Width="118" />
                                    <DataGridTextColumn Header="{svc:Loc Key=Xaml_Severity_2de3cb85}" Binding="{Binding Severity}" Width="82">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="FontWeight" Value="SemiBold" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Severity}" Value="warning">
                                                        <Setter Property="Foreground" Value="{DynamicResource Hg.Yellow}" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Severity}" Value="critical">
                                                        <Setter Property="Foreground" Value="{DynamicResource Hg.Red}" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTextColumn Header="{svc:Loc Key=Xaml_Title_b78a3223}" Binding="{Binding Title}" Width="150" />
                                    <DataGridTextColumn Header="{svc:Loc Key=Xaml_Subject_8d183dbd}" Binding="{Binding Subject}" Width="1.2*" />
                                    <DataGridTextColumn Header="{svc:Loc Key=Xaml_Details_dc3decbb}" Binding="{Binding Details}" Width="1.8*" />
                                    <DataGridTextColumn Header="{svc:Loc Key=Xaml_Process_12f4c13a}" Binding="{Binding Process}" Width="1*" />
                                    <DataGridTextColumn Header="{svc:Loc Key=Xaml_Type_4f6f5f0b}" Binding="{Binding Type}" Width="110" />
                                    <DataGridCheckBoxColumn Header="{svc:Loc Key=Xaml_Read_d8e2f72f}" Binding="{Binding IsRead}" Width="54" IsReadOnly="True" />
                                </DataGrid.Columns>
                            </DataGrid>

                            <Border Grid.Column="0"
                                    Style="{StaticResource Hg.EmptyState}"
                                    Visibility="{Binding Alerts.Count, Converter={StaticResource EmptyStateVis}}">
                                <StackPanel>
                                    <TextBlock Text="{svc:Loc Key=Xaml_No_alerts_yet_69542c8a}"
                                               Style="{StaticResource Hg.SectionTitle}"
                                               HorizontalAlignment="Center"
                                               Margin="0,0,0,4" />
                                    <TextBlock Text="{svc:Loc Key=Xaml_Alerts_will_appear_here_when_3f0d73ac}"
                                               Style="{StaticResource Hg.EmptyText}" />
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="1" Style="{StaticResource Hg.Card}" Margin="10,0,0,0">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Text="{svc:Loc Key=Xaml_Alert_type_settings_8e403573}" FontWeight="SemiBold" />
                                    <Border Grid.Row="1" Margin="0,8,0,2" Style="{StaticResource Hg.StatusStrip}"
                                            Visibility="{Binding SelectedAlert.IsIdnHomograph, Converter={StaticResource BoolToVis}}"
                                            AutomationProperties.Name="{svc:Loc Key=IdnHomograph_AlertExplainerName}">
                                        <StackPanel>
                                            <TextBlock Text="{svc:Loc Key=IdnHomograph_AlertExplainerTitle}"
                                                       FontWeight="SemiBold" />
                                            <TextBlock Text="{svc:Loc Key=IdnHomograph_ObservedLabel}"
                                                       Style="{StaticResource Hg.FieldLabel}" Margin="0,7,0,2" />
                                            <TextBlock Text="{Binding SelectedAlert.Subject}" TextWrapping="Wrap"
                                                       AutomationProperties.Name="{svc:Loc Key=IdnHomograph_ObservedName}" />
                                            <TextBlock Text="{svc:Loc Key=IdnHomograph_EvidenceLabel}"
                                                       Style="{StaticResource Hg.FieldLabel}" Margin="0,7,0,2" />
                                            <TextBlock Text="{Binding SelectedAlert.Details}" TextWrapping="Wrap"
                                                       AutomationProperties.Name="{svc:Loc Key=IdnHomograph_EvidenceName}" />
                                            <TextBlock Text="{svc:Loc Key=IdnHomograph_NoBlockAlert}"
                                                       Style="{StaticResource Hg.HelperText}" Margin="0,7,0,0"
                                                       TextWrapping="Wrap" />
                                        </StackPanel>
                                    </Border>
                                    <Border Grid.Row="1" Margin="0,8,0,2" Style="{StaticResource Hg.StatusStrip}"
                                            Visibility="{Binding SelectedAlert.HasDgaEvidence, Converter={StaticResource BoolToVis}}"
                                            AutomationProperties.Name="{svc:Loc Key=Dga_AlertExplainerName}">
                                        <StackPanel>
                                            <TextBlock Text="{svc:Loc Key=Dga_AlertExplainerTitle}"
                                                       FontWeight="SemiBold" TextWrapping="Wrap" />
                                            <TextBlock Text="{svc:Loc Key=Dga_DefaultOffAlertOnly}"
                                                       Style="{StaticResource Hg.HelperText}" Margin="0,4,0,8"
                                                       TextWrapping="Wrap" />
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto" />
                                                    <RowDefinition Height="Auto" />
                                                    <RowDefinition Height="Auto" />
                                                    <RowDefinition Height="Auto" />
                                                    <RowDefinition Height="Auto" />
                                                    <RowDefinition Height="Auto" />
                                                    <RowDefinition Height="Auto" />
                                                </Grid.RowDefinitions>
                                                <TextBlock Grid.Row="0" Text="{svc:Loc Key=Dga_Label}" Style="{StaticResource Hg.FieldLabel}" Margin="0,0,12,4" />
                                                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SelectedAlert.DgaRegistrableLabel}" TextWrapping="Wrap"
                                                           AutomationProperties.Name="{svc:Loc Key=Dga_LabelName}" />
                                                <TextBlock Grid.Row="1" Text="{svc:Loc Key=Dga_Score}" Style="{StaticResource Hg.FieldLabel}" Margin="0,0,12,4" />
                                                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedAlert.DgaScoreText}" TextWrapping="Wrap"
                                                           AutomationProperties.Name="{svc:Loc Key=Dga_ScoreName}" />
                                                <TextBlock Grid.Row="2" Text="{svc:Loc Key=Dga_Entropy}" Style="{StaticResource Hg.FieldLabel}" Margin="0,0,12,4" />
                                                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SelectedAlert.DgaEntropyText}" TextWrapping="Wrap" />
                                                <TextBlock Grid.Row="3" Text="{svc:Loc Key=Dga_VowelRatio}" Style="{StaticResource Hg.FieldLabel}" Margin="0,0,12,4" />
                                                <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding SelectedAlert.DgaVowelRatioText}" TextWrapping="Wrap" />
                                                <TextBlock Grid.Row="4" Text="{svc:Loc Key=Dga_DigitRatio}" Style="{StaticResource Hg.FieldLabel}" Margin="0,0,12,4" />
                                                <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding SelectedAlert.DgaDigitRatioText}" TextWrapping="Wrap" />
                                                <TextBlock Grid.Row="5" Text="{svc:Loc Key=Dga_ConsonantRun}" Style="{StaticResource Hg.FieldLabel}" Margin="0,0,12,4" />
                                                <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding SelectedAlert.DgaConsonantRunText}" TextWrapping="Wrap" />
                                                <TextBlock Grid.Row="6" Text="{svc:Loc Key=Dga_Reason}" Style="{StaticResource Hg.FieldLabel}" Margin="0,0,12,0" />
                                                <TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding SelectedAlert.DgaReason}" TextWrapping="Wrap"
                                                           AutomationProperties.Name="{svc:Loc Key=Dga_ReasonName}" />
                                            </Grid>
                                        </StackPanel>
                                    </Border>
                                    <DataGrid Grid.Row="2" ItemsSource="{Binding AlertTypes}" Margin="0,8,0,0"
                                              AutomationProperties.Name="{svc:Loc Key=Xaml_Alert_type_settings_8e403573}">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="{svc:Loc Key=Xaml_Type_4f6f5f0b}" Binding="{Binding Label}" Width="*" />
                                            <DataGridCheckBoxColumn Header="{svc:Loc Key=Xaml_Surface_5e1f39da}" Binding="{Binding Surface}" Width="64" />
                                            <DataGridTextColumn Header="{svc:Loc Key=Xaml_Unread_alerts_0f4f2d44}" Binding="{Binding Unread}" Width="100" />
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </Grid>
                            </Border>
                        </Grid>

                        <TextBlock Grid.Row="2" Margin="2,6,0,0"
                                   Text="{Binding StatusText}"
                                   Style="{StaticResource Hg.DimText}"
                                   AutomationProperties.LiveSetting="Polite" />
                    </Grid>
                </TabItem>

                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <Path Style="{StaticResource Hg.MainTabIconPath}"
                                  Data="M 4 2 H 10 L 13 5 V 14 H 4 Z M 10 2 V 5 H 13 M 6 8 H 11 M 6 11 H 10"
                                  Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=TabItem}}" />
                            <TextBlock Text="{svc:Loc Key=Tab_HostsFile, Default=Hosts File}" />
                        </StackPanel>
                    </TabItem.Header>
                    <TabControl x:Name="HostsTabs" Margin="0,4,0,0"
                                Style="{StaticResource Hg.PrimaryTabs}"
                                ItemContainerStyle="{StaticResource Hg.SecondaryTabItem}">
                        <TabItem Header="{svc:Loc Key=Tab_ManagedDomains, Default=Managed Domains}">
                            <Grid Margin="4" DataContext="{Binding Hosts}">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <Border Grid.Row="0" Style="{StaticResource Hg.CommandBar}" Margin="0,0,0,8">
                                <DockPanel>
                                    <Button DockPanel.Dock="Right" Content="{svc:Loc Key=Xaml_Refresh_56e3badc}"
                                            Command="{Binding RefreshCommand}" Margin="8,0,0,0" />
                                    <Button DockPanel.Dock="Right" Content="{svc:Loc Key=Xaml_AI_Categorize_27b3527f}"
                                            Command="{Binding AiCategorizeCommand}" Margin="8,0,0,0"
                                            ToolTip="{svc:Loc Key=Xaml_Have_DeepSeek_categorize_every_hosts_file_3b37a22a}" />
                                    <Button DockPanel.Dock="Right" Content="{svc:Loc Key=Xaml_Block_82dd2cdf}"
                                            Style="{StaticResource Hg.AccentButton}"
                                            Command="{Binding BlockCommand}" Margin="8,0,0,0"
                                            ToolTip="{svc:Loc Key=Xaml_Enter_a_domain_then_add_it_67b6f1e2}" />
                                    <TextBox DockPanel.Dock="Right" Width="220" Margin="8,0,0,0"
                                             Text="{Binding NewDomain, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource Hg.HintBox}"
                                             Tag="{svc:Loc Key=Xaml_Domain_to_block_31b7bab9}"
                                             AutomationProperties.Name="{svc:Loc Key=Xaml_Domain_to_block_31b7bab9}"
                                             ToolTip="{svc:Loc Key=Xaml_Domain_to_block_31b7bab9}" />
                                    <ComboBox DockPanel.Dock="Right" Width="120" Margin="8,0,0,0"
                                              ItemsSource="{x:Static vm:HostsViewModel.StatusFilters}"
                                              AutomationProperties.Name="{svc:Loc Key=Xaml_Managed_domain_status_filter_c1837b19}"
                                              SelectedItem="{Binding StatusFilter}" />
                                    <TextBox Text="{Binding Filter, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource Hg.HintBox}"
                                             Tag="{svc:Loc Key=Xaml_SearchManagedDomainsHint}"
                                             AutomationProperties.Name="{svc:Loc Key=Xaml_Search_managed_domains_f5421c94}"
                                             ToolTip="{svc:Loc Key=Xaml_Filters_as_you_type_â_supports_a2c98d15}" />
                                </DockPanel>
                                </Border>

                                <DataGrid Grid.Row="1" x:Name="DomainsGrid" ItemsSource="{Binding Domains}"
                                          AutomationProperties.Name="{svc:Loc Key=Xaml_Managed_domains_a7b9edcb}"
                                          IsVisibleChanged="OnGridVisible">
                                    <DataGrid.RowStyle>
                                        <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                                            <Setter Property="ToolTip" Value="{Binding Reason}" />
                                        </Style>
                                    </DataGrid.RowStyle>
                                    <DataGrid.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="{svc:Loc Key=Xaml_Block_selected_d487fbba}"
                                                      Command="{Binding PlacementTarget.DataContext.BlockSelectedCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding PlacementTarget.SelectedItems, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                            <MenuItem Header="{svc:Loc Key=Xaml_Allow_selected_7bc4fcf5}"
                                                      Command="{Binding PlacementTarget.DataContext.AllowSelectedCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding PlacementTarget.SelectedItems, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                            <MenuItem Header="{svc:Loc Key=Xaml_Remove_selected_f38b8091}"
                                                      Command="{Binding PlacementTarget.DataContext.RemoveSelectedCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding PlacementTarget.SelectedItems, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                            <Separator />
                                            <MenuItem Header="{svc:Loc Key=Xaml_Block_root_6c8978ac}"
                                                      Command="{Binding PlacementTarget.DataContext.BlockRootCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding PlacementTarget.SelectedItem.Domain, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                            <Separator />
                                            <MenuItem Header="{svc:Loc Key=Xaml_Research_online_0fdb1e04}">
                                                <MenuItem Header="{svc:Loc Key=Xaml_Google_2b681c0a}"
                                                          Command="{Binding PlacementTarget.DataContext.ResearchGoogleCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                          CommandParameter="{Binding PlacementTarget.SelectedItem.Domain, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                                <MenuItem Header="{svc:Loc Key=Xaml_VirusTotal_d38e68d0}"
                                                          Command="{Binding PlacementTarget.DataContext.ResearchVirusTotalCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                          CommandParameter="{Binding PlacementTarget.SelectedItem.Domain, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                                <MenuItem Header="{svc:Loc Key=Xaml_who_is_42562da6}"
                                                          Command="{Binding PlacementTarget.DataContext.ResearchWhoisCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                          CommandParameter="{Binding PlacementTarget.SelectedItem.Domain, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                            </MenuItem>
                                        </ContextMenu>
                                    </DataGrid.ContextMenu>
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="{svc:Loc Key=Xaml_Domain_9b10914d}" Binding="{Binding Domain}" Width="2*" />
                                        <DataGridTextColumn Header="{svc:Loc Key=Xaml_Status_bae7d5be}" Binding="{Binding Status}" Width="110" />
                                        <DataGridTextColumn Header="{svc:Loc Key=Xaml_Category_a3c686e7}" Binding="{Binding Category}" Width="140" />
                                        <DataGridTextColumn Header="{svc:Loc Key=Xaml_Source_6da13add}" Binding="{Binding Source}" Width="110" />
                                        <DataGridTextColumn Header="{svc:Loc Key=Xaml_Reason_f219cc06}" Binding="{Binding Reason}" Width="2*" />
                                        <DataGridTextColumn Header="{svc:Loc Key=Xaml_Hits_a6179b4c}" Binding="{Binding Hits}" Width="70" />
                                        <DataGridTemplateColumn Header="{svc:Loc Key=Xaml_Actions_c3cd636a}" Width="170">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal">
                                                        <Button Content="{svc:Loc Key=Xaml_Allow_3ad0e369}" Padding="8,2" Margin="0,0,6,0"
                                                                Command="{Binding DataContext.AllowCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                                CommandParameter="{Binding Domain}" />
                                                        <Button Content="{svc:Loc Key=Xaml_Remove_e963907d}" Padding="8,2"
                                                                Style="{StaticResource Hg.DangerButton}"
                                                                Command="{Binding DataContext.UnblockCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                                CommandParameter="{Binding Domain}" />
                                                    </StackPanel>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>

                                <Border Grid.Row="1"
                                        Style="{StaticResource Hg.EmptyState}"
                                        Visibility="{Binding Domains.Count, Converter={StaticResource EmptyStateVis}}">
                                    <StackPanel>
                                        <TextBlock Text="{svc:Loc Key=Xaml_No_managed_domains_90d3bbe6}"
                                                   Style="{StaticResource Hg.SectionTitle}"
                                                   HorizontalAlignment="Center"
                                                   Margin="0,0,0,4" />
                                        <TextBlock Text="{svc:Loc Key=Xaml_Add_a_domain_above_or_import_5ac9415e}"
                                                   Style="{StaticResource Hg.EmptyText}" />
                                    </StackPanel>
                                </Border>

                                <TextBlock Grid.Row="2" Margin="2,6,0,0"
                                           Text="{Binding StatusText}"
                                           Style="{StaticResource Hg.DimText}"
                                           AutomationProperties.LiveSetting="Polite" />
                            </Grid>
                        </TabItem>

                        <TabItem Header="{svc:Loc Key=Tab_Blocklists, Default=Blocklists}">
                            <Grid Margin="4" DataContext="{Binding Blocklists}">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <DataGrid Grid.Row="0" ItemsSource="{Binding Sources}"
                                          AutomationProperties.Name="{svc:Loc Key=Xaml_Blocklist_sources_47c4f37d}">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="{svc:Loc Key=Xaml_Category_a3c686e7}" Binding="{Binding Category}" Width="120" />
                                        <DataGridTextColumn Header="{svc:Loc Key=Xaml_Name_709a2322}" Binding="{Binding Name}" Width="*">
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="ToolTip" Value="{Binding GalleryTip}" />
                                                </Style>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>
                                        <DataGridTextColumn Header="{svc:Loc Key=BlockGallery_Tags}" Binding="{Binding Tags}" Width="150">
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="ToolTip" Value="{Binding GalleryTip}" />
                                                    <Setter Property="Foreground" Value="{DynamicResource Hg.Sub}" />
                                                </Style>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>
                                        <DataGridTextColumn Header="{svc:Loc Key=BlockGallery_License}" Binding="{Binding License}" Width="86" />
                                        <DataGridCheckBoxColumn Header="{svc:Loc Key=Xaml_Subscribed_dd1242a8}" Binding="{Binding Subscribed}" Width="90" IsReadOnly="True" />
                                        <DataGridCheckBoxColumn Header="{svc:Loc Key=Xaml_Enabled_df174a3f}" Binding="{Binding Enabled}" Width="72" IsReadOnly="True" />
                                        <DataGridTextColumn Header="{svc:Loc Key=Xaml_Domains_a0d641b3}" Binding="{Binding DomainCount}" Width="90" />
                                        <DataGridTextColumn Header="{svc:Loc Key=Xaml_Owned_b62ff5cc}" Binding="{Binding OwnedDomainCount}" Width="70" />
                                        <DataGridTextColumn Header="{svc:Loc Key=Xaml_Hits_30d_d0f4cbfd}" Binding="{Binding Hits30d}" Width="80" />
                                        <DataGridTextColumn Header="{svc:Loc Key=Xaml_Last_refresh_53cadc51}" Binding="{Binding LastRefresh}" Width="160" />
                                        <DataGridTextColumn Header="{svc:Loc Key=Xaml_Health_3703cd21}" Binding="{Binding HealthSummary}" Width="180">
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{DynamicResource Hg.Peach}" />
                                                    <Setter Property="ToolTip" Value="{Binding HealthSummary}" />
                                                </Style>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>
                                        <DataGridTemplateColumn Header="{svc:Loc Key=Xaml_Actions_c3cd636a}" Width="400">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal">
                                                        <Button Content="{svc:Loc Key=Xaml_Preview_f1fbb2b4}" Padding="8,2" Margin="0,0,6,0"
                                                                Command="{Binding DataContext.PreviewCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                                CommandParameter="{Binding}" />
                                                        <Button Content="{svc:Loc Key=Xaml_Import_d6fbc9d2}" Padding="8,2" Margin="0,0,6,0"
                                                                Style="{StaticResource Hg.AccentButton}"
                                                                Command="{Binding DataContext.ImportCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                                CommandParameter="{Binding}" />
                                                        <Button Content="{Binding ToggleLabel}" Padding="8,2" Margin="0,0,6,0"
                                                                Visibility="{Binding Subscribed, Converter={StaticResource BoolToVis}}"
                                                                Command="{Binding DataContext.ToggleEnabledCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                                CommandParameter="{Binding}" />
                                                        <Button Content="{svc:Loc Key=Xaml_Restore_3cbe6d6b}" Padding="8,2" Margin="0,0,6,0"
                                                                Visibility="{Binding CanRollback, Converter={StaticResource BoolToVis}}"
                                                                Command="{Binding DataContext.RollbackCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                                CommandParameter="{Binding}" />
                                                        <Button Content="{svc:Loc Key=Xaml_Remove_e963907d}" Padding="8,2"
                                                                Visibility="{Binding Subscribed, Converter={StaticResource BoolToVis}}"
                                                                Command="{Binding DataContext.UnsubscribeCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                                CommandParameter="{Binding}" />
                                                        <Button Content="{svc:Loc Key=BlockGallery_Homepage}" Padding="8,2" Margin="6,0,0,0"
                                                                ToolTip="{Binding Homepage}"
                                                                Command="{Binding DataContext.OpenHomepageCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                                CommandParameter="{Binding}">
                                                            <Button.Style>
                                                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding Homepage}" Value="">
                                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Button.Style>
                                                        </Button>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>

                                <Border Grid.Row="0"
                                        Style="{StaticResource Hg.EmptyState}"
                                        Visibility="{Binding Sources.Count, Converter={StaticResource EmptyStateVis}}">
                                    <StackPanel>
                                        <TextBlock Text="{svc:Loc Key=Xaml_No_blocklist_sources_2d426058}"
                                                   Style="{StaticResource Hg.SectionTitle}"
                                                   HorizontalAlignment="Center"
                                                   Margin="0,0,0,4" />
                                        <TextBlock Text="{svc:Loc Key=Xaml_The_service_did_not_return_the_e87954a3}"
                                                   Style="{StaticResource Hg.EmptyText}" />
                                    </StackPanel>
                                </Border>

                                <Border Grid.Row="1" Style="{StaticResource Hg.Card}" Margin="0,8,0,0"
                                        Visibility="{Binding HasConnectivityWarnings, Converter={StaticResource BoolToVis}}"
                                        AutomationProperties.Name="{svc:Loc Key=Ncsi_WarningName}">
                                    <StackPanel>
                                        <DockPanel LastChildFill="False">
                                            <Button DockPanel.Dock="Right" Content="{svc:Loc Key=Ncsi_RecoverAction}"
                                                    Command="{Binding RecoverWindowsConnectivityCommand}"
                                                    Style="{StaticResource Hg.AccentButton}" Margin="10,0,0,0"
                                                    AutomationProperties.Name="{svc:Loc Key=Ncsi_RecoverActionName}"
                                                    ToolTip="{svc:Loc Key=Ncsi_RecoverTip}" />
                                            <StackPanel>
                                                <TextBlock Text="{svc:Loc Key=Ncsi_WarningTitle}"
                                                           Style="{StaticResource Hg.SectionTitle}" />
                                                <TextBlock Text="{svc:Loc Key=Ncsi_WarningHelp}"
                                                           Style="{StaticResource Hg.HelperText}" Margin="0,3,0,0"
                                                           TextWrapping="Wrap" />
                                            </StackPanel>
                                        </DockPanel>
                                        <ItemsControl ItemsSource="{Binding ConnectivityWarnings}" Margin="0,8,0,0">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Style="{StaticResource Hg.StatusStrip}" Margin="0,0,8,6" MinWidth="300">
                                                        <StackPanel>
                                                            <DockPanel>
                                                                <TextBlock DockPanel.Dock="Right" Text="{Binding StateText}"
                                                                           Style="{StaticResource Hg.DimText}" Margin="8,0,0,0" />
                                                                <TextBlock Text="{Binding Domain}" FontWeight="SemiBold" />
                                                            </DockPanel>
                                                            <TextBlock Text="{Binding DisplayReason}" Style="{StaticResource Hg.SubText}"
                                                                       TextWrapping="Wrap" ToolTip="{Binding Reason}" />
                                                            <TextBlock Text="{Binding DisplayMetadata}" Style="{StaticResource Hg.DimText}" />
                                                        </StackPanel>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                        <TextBlock Text="{Binding ConnectivityWarningStatus}"
                                                   Style="{StaticResource Hg.DimText}"
                                                   AutomationProperties.LiveSetting="Polite" />
                                    </StackPanel>
                                </Border>

                                <Border Grid.Row="2" Style="{StaticResource Hg.Card}" Margin="0,8,0,0">
                                    <DockPanel>
                                        <Button DockPanel.Dock="Right" Content="{svc:Loc Key=Xaml_Save_apply_allowlists_c3ce8fa2}"
                                                Style="{StaticResource Hg.AccentButton}"
                                                Command="{Binding SaveAllowlistsCommand}" Margin="8,0,0,0"
                                                VerticalAlignment="Top" />
                                        <Button DockPanel.Dock="Right" Content="{svc:Loc Key=Xaml_Refresh_all_subscriptions_2a8a52db}"
                                                Command="{Binding RefreshAllListsCommand}" Margin="8,0,0,0"
                                                VerticalAlignment="Top" />
                                        <StackPanel>
                                            <TextBlock Text="{svc:Loc Key=Xaml_Allowlist_URLs_one_per_line_â_67b8a98c}"
                                                       Style="{StaticResource Hg.SubText}" Margin="0,0,0,4" />
                                            <TextBox Text="{Binding AllowlistUrlsText, UpdateSourceTrigger=PropertyChanged}"
                                                     AutomationProperties.Name="{svc:Loc Key=Xaml_Allowlist_subscription_URLs_ef7fa97d}"
                                                     AcceptsReturn="True" MinHeight="52" MaxHeight="110"
                                                     VerticalScrollBarVisibility="Auto" FontFamily="Consolas" />
                                        </StackPanel>
                                    </DockPanel>
                                </Border>

                                <TextBlock Grid.Row="3" Margin="2,6,0,0"
                                           Text="{Binding StatusText}"
                                           Style="{StaticResource Hg.DimText}"
                                           AutomationProperties.LiveSetting="Polite" />
                            </Grid>
                        </TabItem>

                        <TabItem Header="{svc:Loc Key=Tab_RawEditor, Default=Raw Editor}">
                            <Grid Margin="4" DataContext="{Binding RawHosts}">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <Border Grid.Row="0" Style="{StaticResource Hg.CommandBar}" Margin="0,0,0,8">
                                <DockPanel LastChildFill="False">
                                    <Button DockPanel.Dock="Right" Content="{svc:Loc Key=Xaml_Save_efc007a3}"
                                            Style="{StaticResource Hg.AccentButton}"
                                            Command="{Binding SaveCommand}" IsEnabled="{Binding IsDirty}"
                                            ToolTip="{svc:Loc Key=Xaml_Write_the_edited_text_back_to_11af2855}" />
                                    <Button DockPanel.Dock="Right" Content="{svc:Loc Key=Xaml_Reload_cce71553}"
                                            Command="{Binding LoadCommand}" Margin="0,0,8,0"
                                            ToolTip="{svc:Loc Key=Xaml_Discard_edits_and_reload_the_hosts_7d7d2853}" />
                                    <Button DockPanel.Dock="Right" Content="{svc:Loc Key=Xaml_AI_Categorize_27b3527f}"
                                            Command="{Binding AiCategorizeCommand}" Margin="0,0,8,0"
                                            ToolTip="{svc:Loc Key=Xaml_Have_DeepSeek_categorize_every_hosts_file_3b37a22a}" />
                                    <TextBlock DockPanel.Dock="Right" Margin="0,0,12,0" VerticalAlignment="Center"
                                               Text="{svc:Loc Key=Xaml_â_Unsaved_changes_617137b6}" Foreground="{DynamicResource Hg.Peach}"
                                               Visibility="{Binding IsDirty, Converter={StaticResource BoolToVis}}" />
                                </DockPanel>
                                </Border>

                                <TextBox Grid.Row="1"
                                         Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}"
                                         AutomationProperties.Name="{svc:Loc Key=Xaml_Raw_hosts_file_editor_131f9bad}"
                                         AcceptsReturn="True" AcceptsTab="True"
                                         FontFamily="Consolas"
                                         VerticalScrollBarVisibility="Auto"
                                         HorizontalScrollBarVisibility="Auto" />

                                <TextBlock Grid.Row="2" Margin="2,6,0,0"
                                           Text="{Binding StatusText}"
                                           Style="{StaticResource Hg.DimText}"
                                           AutomationProperties.LiveSetting="Polite" />
                            </Grid>
                        </TabItem>
                    </TabControl>
                </TabItem>

                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <Path Style="{StaticResource Hg.MainTabIconPath}"
                                  Data="M 3 11 C 4.5 6 6 4 8 4 S 11.5 6 13 3 M 3 13 H 13 M 5 9 H 11"
                                  Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=TabItem}}" />
                            <TextBlock Text="{svc:Loc Key=Tab_FirewallActivity, Default=Firewall Activity}" />
                        </StackPanel>
                    </TabItem.Header>
                    <Grid Margin="4" DataContext="{Binding FwActivity}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" MinHeight="220" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <!-- Modes: lockdown = default-outbound Block; learning/observe persist to config -->
                        <Border Grid.Row="0" Style="{StaticResource Hg.CommandBar}" Margin="0,0,0,8">
                        <WrapPanel>
                            <CheckBox DockPanel.Dock="Left" Content="{svc:Loc Key=Xaml_Lockdown_8ba04349}" Margin="0,0,14,0" VerticalAlignment="Center"
                                      IsChecked="{Binding Lockdown}"
                                      ToolTip="{svc:Loc Key=Xaml_Set_the_default_outbound_action_to_26ed8581}" />
                            <CheckBox DockPanel.Dock="Left" Content="{svc:Loc Key=Xaml_Learning_1016fcb2}" Margin="0,0,14,0" VerticalAlignment="Center"
                                      IsChecked="{Binding LearningMode}"
                                      ToolTip="{svc:Loc Key=Xaml_Surface_trust_prompts_for_processes_making_2f0de78c}" />
                            <CheckBox DockPanel.Dock="Left" Content="{svc:Loc Key=Xaml_Observe_c99a2bcf}" Margin="0,0,14,0" VerticalAlignment="Center"
                                      IsChecked="{Binding ObserveMode}"
                                      ToolTip="{svc:Loc Key=Xaml_Record_new_processes_silently_instead_of_e83fd128}" />
                            <CheckBox DockPanel.Dock="Left" Margin="0,0,14,0" VerticalAlignment="Center"
                                      IsChecked="{Binding DataContext.ChildInherit, RelativeSource={RelativeSource AncestorType=Window}}"
                                      AutomationProperties.Name="{svc:Loc Key=Xaml_Child_process_auto_allow_b6f484ea}"
                                      ToolTip="{svc:Loc Key=Xaml_Child_process_auto_allow_a_direct_f67d601b}">
                                <TextBlock Text="{svc:Loc Key=Xaml_Inherit_to_children_86cbf611}" TextWrapping="Wrap" />
                            </CheckBox>
                            <CheckBox DockPanel.Dock="Left" Margin="0,0,14,0" VerticalAlignment="Center"
                                      IsChecked="{Binding DataContext.InboundConsent, RelativeSource={RelativeSource AncestorType=Window}}"
                                      AutomationProperties.Name="{svc:Loc Key=Xaml_Inbound_connection_consent_d2906bf6}"
                                      ToolTip="{svc:Loc Key=Xaml_Inbound_connection_consent_also_prompt_on_2905613f}">
                                <TextBlock Text="{svc:Loc Key=Xaml_Inbound_prompts_a42a5e5c}" TextWrapping="Wrap" />
                            </CheckBox>
                            <TextBlock DockPanel.Dock="Right" Text="{Binding PostureText}"
                                       Style="{StaticResource Hg.DimText}" VerticalAlignment="Center"
                                       TextWrapping="Wrap" MaxWidth="520" />
                            <CheckBox DockPanel.Dock="Right" Margin="10,0,0,0" VerticalAlignment="Center"
                                      IsChecked="{Binding FlowTeardownEnabled}"
                                      AutomationProperties.Name="{svc:Loc Key=Xaml_Close_matching_IPv4_TCP_flows_on_block_b80df497}"
                                      ToolTip="{svc:Loc Key=Xaml_Opt_in_closes_matching_established_899aa84d}">
                                <TextBlock Text="{svc:Loc Key=Xaml_Close_TCP_on_block_b15d5697}" TextWrapping="Wrap" />
                            </CheckBox>
                            <Button DockPanel.Dock="Right" Content="{svc:Loc Key=Xaml_AI_identify_f32080d7}" Margin="10,0,0,0" Padding="10,3"
                                    Command="{Binding IdentifyConnectionsCommand}"
                                    ToolTip="{svc:Loc Key=Xaml_Have_DeepSeek_explain_what_each_connection_bf26cd35}" />
                            <CheckBox DockPanel.Dock="Right" Content="{svc:Loc Key=Xaml_Resolve_IPs_263bfa86}" Margin="10,0,0,0" VerticalAlignment="Center"
                                      IsChecked="{Binding ResolveIps}"
                                      AutomationProperties.Name="{svc:Loc Key=Xaml_Resolve_remote_IPs_with_reverse_DNS_037e338f}"
                                      ToolTip="{svc:Loc Key=Xaml_Reverse_DNS_remote_IPs_the_live_91aea662}" />
                            <CheckBox DockPanel.Dock="Right" Margin="10,0,10,0" VerticalAlignment="Center"
                                      IsChecked="{Binding GroupByApp}"
                                      AutomationProperties.Name="{svc:Loc Key=Xaml_Group_connections_by_app_8a45d006}"
                                      ToolTip="{svc:Loc Key=Xaml_Group_the_live_connections_under_their_5f76fe48}">
                                <TextBlock Text="{svc:Loc Key=Xaml_Group_by_app_8d9fabe2}" TextWrapping="Wrap" />
                            </CheckBox>
                            <CheckBox DockPanel.Dock="Right" Margin="10,0,0,0" VerticalAlignment="Center"
                                      IsChecked="{Binding GroupByCountry}"
                                      AutomationProperties.Name="{svc:Loc Key=Xaml_Group_connections_by_country_faf82ca8}"
                                      ToolTip="{svc:Loc Key=Xaml_Group_the_live_connections_by_remote_925f45c1}">
                                <TextBlock Text="{svc:Loc Key=Xaml_Group_by_country_20629707}" TextWrapping="Wrap" />
                            </CheckBox>
                            <TextBox Margin="0,0,0,0" VerticalAlignment="Center"
                                     Text="{Binding Filter, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource Hg.HintBox}"
                                     MinWidth="260"
                                     Tag="{svc:Loc Key=Xaml_SearchConnectionsHint}"
                                     AutomationProperties.Name="{svc:Loc Key=Xaml_Search_live_connections_0c00b205}"
                                     ToolTip="{svc:Loc Key=Xaml_Search_process_chrome_port_443_country_7a34001a}" />
                            <TextBox Width="220" Margin="10,0,0,0" VerticalAlignment="Center"
                                     Text="{Binding ExplainInput, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource Hg.HintBox}"
                                     Tag="{svc:Loc Key=Xaml_ExplainConnectionHint}"
                                     AutomationProperties.Name="{svc:Loc Key=Xaml_Decision_explainer_target_bb8fba97}"
                                     ToolTip="{svc:Loc Key=Xaml_Explain_the_current_rule_decision_for_04e02131}" />
                            <Button Margin="6,0,0,0" Padding="10,3"
                                    Command="{Binding ExplainInputCommand}"
                                    ToolTip="{svc:Loc Key=Xaml_Show_the_ordered_hosts_firewall_trust_7b7cc959}">
                                <TextBlock Text="{svc:Loc Key=Xaml_Explain_55cbfd1b}" TextWrapping="Wrap" />
                            </Button>
                        </WrapPanel>
                        </Border>

                        <Grid Grid.Row="1" MinHeight="180">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="248" />
                            </Grid.ColumnDefinitions>

                        <DataGrid Grid.Column="0" x:Name="ConnectionsGrid" ItemsSource="{Binding ConnectionsView}"
                                  AutomationProperties.Name="{svc:Loc Key=Xaml_Live_firewall_connections_9f7d7451}"
                                  IsVisibleChanged="OnGridVisible">
                            <DataGrid.GroupStyle>
                                <GroupStyle>
                                    <GroupStyle.ContainerStyle>
                                        <Style TargetType="{x:Type GroupItem}">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="{x:Type GroupItem}">
                                                        <Expander IsExpanded="True" Margin="0,2,0,0">
                                                            <Expander.Header>
                                                                <StackPanel Orientation="Horizontal">
                                                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                                                                    <TextBlock Text="{Binding ItemCount, StringFormat=' ({0})'}"
                                                                               Style="{StaticResource Hg.DimText}" />
                                                                </StackPanel>
                                                            </Expander.Header>
                                                            <ItemsPresenter />
                                                        </Expander>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </GroupStyle.ContainerStyle>
                                </GroupStyle>
                            </DataGrid.GroupStyle>
                            <DataGrid.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="{svc:Loc Key=Xaml_Block_this_site_domain_99db658c}"
                                              Command="{Binding PlacementTarget.DataContext.BlockSiteCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              ToolTip="{svc:Loc Key=Xaml_Block_the_resolved_domain_via_the_9b0f5c9b}" />
                                    <MenuItem Header="{svc:Loc Key=Xaml_Block_site_for_this_app_firewall_154a0d67}"
                                              Command="{Binding PlacementTarget.DataContext.BlockDomainFirewallCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              ToolTip="{svc:Loc Key=Xaml_Create_a_per_app_domain_firewall_rule_154a0d67}" />
                                    <MenuItem Header="{svc:Loc Key=Xaml_Allow_this_site_domain_7a372753}"
                                              Command="{Binding PlacementTarget.DataContext.AllowSiteCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              ToolTip="{svc:Loc Key=Xaml_Whitelist_the_resolved_domain_via_the_8decb985}" />
                                    <MenuItem Header="{svc:Loc Key=Xaml_Block_remote_IP_5e8f9ae4}"
                                              Command="{Binding PlacementTarget.DataContext.QuickBlockIpCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.SelectedItem.RemoteAddr, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                    <MenuItem Header="{svc:Loc Key=Xaml_Block_program_accc5df3}"
                                              Command="{Binding PlacementTarget.DataContext.QuickBlockProcessCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                    <MenuItem Header="{svc:Loc Key=Xaml_Close_connection_78b5d6bf}"
                                              Command="{Binding PlacementTarget.DataContext.CloseConnectionCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              ToolTip="{svc:Loc Key=Xaml_Immediately_close_this_established_IPv4_TCP_b0037bc9}" />
                                    <MenuItem Header="{svc:Loc Key=Xaml_Block_app_scope_5000247e}">
                                        <MenuItem Header="{svc:Loc Key=Xaml_Internet_public_IPs_6e6ca14f}"
                                                  Command="{Binding PlacementTarget.DataContext.BlockScopeCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding PlacementTarget.SelectedItem.Pid, StringFormat='internet|{0}', RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                        <MenuItem Header="{svc:Loc Key=Xaml_LAN_private_ranges_03642d23}"
                                                  Command="{Binding PlacementTarget.DataContext.BlockScopeCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding PlacementTarget.SelectedItem.Pid, StringFormat='lan|{0}', RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                        <MenuItem Header="{svc:Loc Key=Xaml_Localhost_loopback_60eafa07}"
                                                  Command="{Binding PlacementTarget.DataContext.BlockScopeCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding PlacementTarget.SelectedItem.Pid, StringFormat='localhost|{0}', RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                        <MenuItem Header="{svc:Loc Key=Xaml_Inbound_6b6e7903}"
                                                  Command="{Binding PlacementTarget.DataContext.BlockScopeCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding PlacementTarget.SelectedItem.Pid, StringFormat='inbound|{0}', RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                    </MenuItem>
                                    <Separator />
                                    <MenuItem Header="{svc:Loc Key=Xaml_Explain_decision_3c236b8e}"
                                              Command="{Binding PlacementTarget.DataContext.ExplainSelectedCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              ToolTip="{svc:Loc Key=Xaml_Show_the_ordered_hosts_firewall_trust_1ac7557b}" />
                                    <MenuItem Header="{svc:Loc Key=Xaml_Explain_look_up_connection_30154a2f}"
                                              ToolTip="{svc:Loc Key=Xaml_Look_up_the_connection_s_resolved_247f9d9a}">
                                        <MenuItem Header="{svc:Loc Key=Xaml_VirusTotal_site_298ecc2b}"
                                                  Command="{Binding PlacementTarget.DataContext.ResearchVirusTotalCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding PlacementTarget.SelectedItem.ResearchTarget, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                        <MenuItem Header="{svc:Loc Key=Xaml_who_is_site_09ff0b33}"
                                                  Command="{Binding PlacementTarget.DataContext.ResearchWhoisCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding PlacementTarget.SelectedItem.ResearchTarget, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                        <MenuItem Header="{svc:Loc Key=Xaml_Google_2b681c0a}"
                                                  Command="{Binding PlacementTarget.DataContext.ResearchGoogleCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding PlacementTarget.SelectedItem.ResearchTarget, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                        <MenuItem Header="{svc:Loc Key=Xaml_AbuseIPDB_IP_4666004f}"
                                                  Command="{Binding PlacementTarget.DataContext.ResearchAbuseIpdbCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding PlacementTarget.SelectedItem.RemoteAddr, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                    </MenuItem>
                                </ContextMenu>
                            </DataGrid.ContextMenu>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_Process_12f4c13a}" Binding="{Binding Process}" Width="90" />
                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_Service_329cb8b6}" Binding="{Binding Service}" Width="72" />
                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_PID_dde57e82}" Binding="{Binding Pid}" Width="42" />
                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_Proto_24131f3e}" Binding="{Binding Protocol}" Width="44" />
                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_Remote_c93f6536}" Binding="{Binding RemoteAddr}" Width="100" />
                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_Site_5b6825d3}" Binding="{Binding Host}" Width="*" MinWidth="90">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="ToolTip" Value="Domain this IP was resolved as (live DNS)" />
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_Info_4b631f69}" Binding="{Binding Info}" Width="*" MinWidth="90">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{DynamicResource Hg.Teal}" />
                                            <Setter Property="ToolTip" Value="{Binding Info}" />
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_Port_fe035157}" Binding="{Binding RemotePort}" Width="42" />
                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_State_a7250206}" Binding="{Binding State}" Width="66" />
                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_Country_d523ebbd}" Binding="{Binding Country}" Width="48" />
                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_ASN_b4d90f17}" Binding="{Binding Asn}" Width="150" />
                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_FW_913a8d78}" Binding="{Binding FwStatus}" Width="50" />
                            </DataGrid.Columns>
                        </DataGrid>

                        <Border Grid.Column="0"
                                Style="{StaticResource Hg.EmptyState}"
                                Visibility="{Binding Rows.Count, Converter={StaticResource EmptyStateVis}}">
                            <StackPanel>
                                <TextBlock Text="{svc:Loc Key=Xaml_No_live_connections_ecd1ff86}"
                                           Style="{StaticResource Hg.SectionTitle}"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,4" />
                                <TextBlock Text="{svc:Loc Key=Xaml_Connections_appear_here_when_the_monitor_79a98f2a}"
                                           Style="{StaticResource Hg.EmptyText}" />
                            </StackPanel>
                        </Border>

                            <Border Grid.Column="1"
                                    Style="{StaticResource Hg.Inspector}"
                                    Margin="10,0,0,0"
                                    DataContext="{Binding SelectedItem, ElementName=ConnectionsGrid}">
                                <Grid>
                                    <StackPanel>
                                        <StackPanel.Style>
                                            <Style TargetType="StackPanel">
                                                <Setter Property="Visibility" Value="Visible" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding}" Value="{x:Null}">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <TextBlock Text="{svc:Loc Key=Xaml_Selected_connection_c9d01f2f}" Style="{StaticResource Hg.Kicker}" />
                                        <TextBlock Text="{Binding Process, TargetNullValue={svc:Loc Key=Xaml_SelectConnectionTargetNull}}"
                                                   FontSize="16"
                                                   FontWeight="SemiBold"
                                                   TextWrapping="Wrap"
                                                   Margin="0,4,0,0" />
                                        <TextBlock Text="{Binding ResearchTarget, StringFormat={svc:Loc Key=Xaml_ConnectionTargetLabel}, TargetNullValue={svc:Loc Key=Xaml_ChooseLiveRowTargetNull}}"
                                                   Style="{StaticResource Hg.DimText}"
                                                   Margin="0,3,0,0"
                                                   TextWrapping="Wrap" />

                                        <Grid Margin="0,12,0,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <Border Grid.Column="0" Style="{StaticResource Hg.Badge}" Margin="0,0,6,0">
                                                <TextBlock Text="{Binding State, TargetNullValue={svc:Loc Key=Xaml_NoStateTargetNull}}"
                                                           Style="{StaticResource Hg.DimText}" />
                                            </Border>
                                            <Border Grid.Column="1" Style="{StaticResource Hg.Badge}" Margin="6,0,0,0">
                                                <TextBlock Text="{Binding FwStatus, TargetNullValue={svc:Loc Key=Xaml_NoFwFlagTargetNull}}"
                                                           Style="{StaticResource Hg.DimText}" />
                                            </Border>
                                        </Grid>

                                        <TextBlock Text="{svc:Loc Key=Xaml_Remote_c93f6536}" Style="{StaticResource Hg.InspectorLabel}" />
                                        <TextBlock Style="{StaticResource Hg.InspectorValue}">
                                            <Run Text="{Binding Protocol, TargetNullValue='-'}" />
                                            <Run Text=" " />
                                            <Run Text="{Binding RemoteAddr, TargetNullValue='-'}" />
                                            <Run Text=":" />
                                            <Run Text="{Binding RemotePort}" />
                                        </TextBlock>

                                        <TextBlock Text="{svc:Loc Key=Xaml_Resolved_site_ec0dc722}" Style="{StaticResource Hg.InspectorLabel}" />
                                        <TextBlock Text="{Binding Host, TargetNullValue={svc:Loc Key=Xaml_NoDnsMatchTargetNull}}"
                                                   Style="{StaticResource Hg.InspectorValue}" />

                                        <TextBlock Text="{svc:Loc Key=Xaml_Service_location_bd11579c}" Style="{StaticResource Hg.InspectorLabel}" />
                                        <TextBlock Text="{Binding Service, TargetNullValue={svc:Loc Key=Xaml_NoServiceAttributionTargetNull}}"
                                                   Style="{StaticResource Hg.InspectorValue}" />
                                        <TextBlock Text="{Binding Country, StringFormat={svc:Loc Key=Xaml_CountryLabel}, TargetNullValue={svc:Loc Key=Xaml_CountryUnknownTargetNull}}"
                                                   Style="{StaticResource Hg.DimText}"
                                                   Margin="0,4,0,0" />

                                        <TextBlock Text="{svc:Loc Key=Xaml_Info_4b631f69}" Style="{StaticResource Hg.InspectorLabel}" />
                                        <TextBlock Text="{Binding Info, TargetNullValue={svc:Loc Key=Xaml_RunAiIdentifyTargetNull}}"
                                                   Style="{StaticResource Hg.InspectorValue}"
                                                   Foreground="{DynamicResource Hg.Teal}"
                                                   MaxHeight="72" />

                                        <TextBlock Text="{svc:Loc Key=Xaml_Decision_7f59a1f1}" Style="{StaticResource Hg.InspectorLabel}" />
                                        <TextBlock Text="{Binding DataContext.DecisionSummary, ElementName=ConnectionsGrid}"
                                                   Style="{StaticResource Hg.InspectorValue}"
                                                   TextWrapping="Wrap"
                                                   MaxHeight="54" />
                                        <TextBlock Text="{Binding DataContext.DecisionNextAction, ElementName=ConnectionsGrid}"
                                                   Style="{StaticResource Hg.DimText}"
                                                   TextWrapping="Wrap"
                                                   Margin="0,3,0,0"
                                                   MaxHeight="48" />
                                        <ScrollViewer MaxHeight="126" VerticalScrollBarVisibility="Auto" Margin="0,6,0,0">
                                            <ItemsControl ItemsSource="{Binding DataContext.DecisionChain, ElementName=ConnectionsGrid}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Margin="0,0,0,6">
                                                            <TextBlock Text="{Binding Header}"
                                                                       FontWeight="SemiBold"
                                                                       Foreground="{DynamicResource Hg.Text}" />
                                                            <TextBlock Text="{Binding Owner}"
                                                                       Style="{StaticResource Hg.DimText}"
                                                                       TextWrapping="Wrap" />
                                                            <TextBlock Text="{Binding Detail}"
                                                                       Style="{StaticResource Hg.DimText}"
                                                                       TextWrapping="Wrap" />
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>

                                        <UniformGrid Columns="2" Margin="0,12,0,0">
                                            <Border Style="{StaticResource Hg.MetricTile}">
                                                <StackPanel>
                                                    <TextBlock Text="{svc:Loc Key=Xaml_PID_dde57e82}" Style="{StaticResource Hg.Kicker}" />
                                                    <TextBlock Text="{Binding Pid}" Style="{StaticResource Hg.MetricValue}" />
                                                </StackPanel>
                                            </Border>
                                            <Border Style="{StaticResource Hg.MetricTile}" Margin="0,0,0,8">
                                                <StackPanel>
                                                    <TextBlock Text="{svc:Loc Key=Xaml_LOCAL_9be34046}" Style="{StaticResource Hg.Kicker}" />
                                                    <TextBlock Text="{Binding LocalPort}"
                                                               Style="{StaticResource Hg.InspectorValue}" />
                                                </StackPanel>
                                            </Border>
                                        </UniformGrid>

                                        <TextBlock Text="{svc:Loc Key=Xaml_Actions_c3cd636a}" Style="{StaticResource Hg.InspectorLabel}" />
                                        <WrapPanel>
                                            <Button Content="{svc:Loc Key=Xaml_Block_site_4721aec9}"
                                                    Style="{StaticResource Hg.ActionChip}"
                                                    Margin="0,0,6,6"
                                                    Command="{Binding DataContext.BlockSiteCommand, ElementName=ConnectionsGrid}"
                                                    CommandParameter="{Binding}"
                                                    IsEnabled="{Binding SelectedItem, ElementName=ConnectionsGrid, Converter={StaticResource ObjectPresentBool}}" />
                                            <Button Content="{svc:Loc Key=Xaml_Block_site_app_154a0d67}"
                                                    Style="{StaticResource Hg.ActionChip}"
                                                    Margin="0,0,6,6"
                                                    Command="{Binding DataContext.BlockDomainFirewallCommand, ElementName=ConnectionsGrid}"
                                                    CommandParameter="{Binding}"
                                                    ToolTip="{svc:Loc Key=Xaml_Create_a_per_app_domain_firewall_rule_154a0d67}"
                                                    IsEnabled="{Binding SelectedItem, ElementName=ConnectionsGrid, Converter={StaticResource ObjectPresentBool}}" />
                                            <Button Content="{svc:Loc Key=Xaml_Allow_site_6217275e}"
                                                    Style="{StaticResource Hg.ActionChip}"
                                                    Margin="0,0,6,6"
                                                    Command="{Binding DataContext.AllowSiteCommand, ElementName=ConnectionsGrid}"
                                                    CommandParameter="{Binding}"
                                                    IsEnabled="{Binding SelectedItem, ElementName=ConnectionsGrid, Converter={StaticResource ObjectPresentBool}}" />
                                            <Button Content="{svc:Loc Key=Xaml_Block_IP_907a3ec9}"
                                                    Style="{StaticResource Hg.ActionChip}"
                                                    Margin="0,0,6,6"
                                                    Command="{Binding DataContext.QuickBlockIpCommand, ElementName=ConnectionsGrid}"
                                                    CommandParameter="{Binding RemoteAddr}"
                                                    IsEnabled="{Binding SelectedItem, ElementName=ConnectionsGrid, Converter={StaticResource ObjectPresentBool}}" />
                                            <Button Content="{svc:Loc Key=Xaml_Block_app_5cb3c2de}"
                                                    Style="{StaticResource Hg.ActionChip}"
                                                    Margin="0,0,6,6"
                                                    Command="{Binding DataContext.QuickBlockProcessCommand, ElementName=ConnectionsGrid}"
                                                    CommandParameter="{Binding}"
                                                    IsEnabled="{Binding SelectedItem, ElementName=ConnectionsGrid, Converter={StaticResource ObjectPresentBool}}" />
                                            <Button Content="{svc:Loc Key=Xaml_Explain_55cbfd1b}"
                                                    Style="{StaticResource Hg.ActionChip}"
                                                    Margin="0,0,6,6"
                                                    Command="{Binding DataContext.ExplainSelectedCommand, ElementName=ConnectionsGrid}"
                                                    CommandParameter="{Binding}"
                                                    IsEnabled="{Binding SelectedItem, ElementName=ConnectionsGrid, Converter={StaticResource ObjectPresentBool}}" />
                                        </WrapPanel>
                                    </StackPanel>

                                    <Border Style="{StaticResource Hg.EmptyState}"
                                            Visibility="{Binding Converter={StaticResource EmptyStateVis}}">
                                        <StackPanel>
                                            <TextBlock Text="{svc:Loc Key=Xaml_Select_a_connection_29e8cc33}"
                                                       Style="{StaticResource Hg.SectionTitle}"
                                                       HorizontalAlignment="Center"
                                                       Margin="0,0,0,4" />
                                            <TextBlock Text="{svc:Loc Key=Xaml_The_inspector_keeps_endpoint_identity_service_59af2ece}"
                                                       Style="{StaticResource Hg.EmptyText}" />
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </Border>
                        </Grid>

                        <!-- Per-app activity timeline (new connections per minute) -->
                        <Border Grid.Row="2" Style="{StaticResource Hg.Card}" Margin="0,8,0,0">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{svc:Loc Key=Xaml_Per_app_activity_217dcab5}"
                                               FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,0,12,0" />
                                    <TextBlock Grid.Column="1" Text="{Binding TimelineStatus}"
                                               Style="{StaticResource Hg.DimText}" TextWrapping="Wrap"
                                               TextAlignment="Right" />
                                </Grid>
                                <Grid Height="86" Margin="0,6,0,0" ClipToBounds="True">
                                    <ItemsControl ItemsSource="{Binding Timeline}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <Grid />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Viewbox Stretch="Fill">
                                                    <Canvas Width="600" Height="100">
                                                        <Polyline Points="{Binding PointsText, Converter={StaticResource PointsConverter}}"
                                                                  Stroke="{Binding ColorIndex, Converter={StaticResource SeriesBrush}}"
                                                                  StrokeThickness="1.6" StrokeLineJoin="Round" />
                                                    </Canvas>
                                                </Viewbox>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Grid>
                                <ItemsControl ItemsSource="{Binding Timeline}" Margin="0,4,0,0">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel Orientation="Horizontal" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" Margin="0,0,14,0">
                                                <Rectangle Width="10" Height="3" VerticalAlignment="Center" Margin="0,0,5,0"
                                                           Fill="{Binding ColorIndex, Converter={StaticResource SeriesBrush}}" />
                                                <TextBlock Text="{Binding Name}" Style="{StaticResource Hg.DimText}" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>

                        <!-- Recent consent decisions with re-decide (WFCP-021) + learning review (NET-074) -->
                        <Border Grid.Row="3" Style="{StaticResource Hg.Card}" Margin="0,8,0,0" MaxHeight="300">
                            <StackPanel>
                                <DockPanel LastChildFill="False">
                                    <TextBlock DockPanel.Dock="Left" Text="{svc:Loc Key=Xaml_Learning_review_e304e7cd}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                    <Button DockPanel.Dock="Right" Padding="10,2" Margin="6,0,0,0"
                                            Command="{Binding DiscardAllLearnedCommand}"
                                            ToolTip="{svc:Loc Key=Xaml_Remove_every_auto_allow_â_apps_163401e7}">
                                        <TextBlock Text="{svc:Loc Key=Xaml_Discard_all_5b034df8}" TextWrapping="Wrap" />
                                    </Button>
                                    <Button DockPanel.Dock="Right" Content="{svc:Loc Key=Xaml_Keep_all_cbfb83ff}" Padding="10,2" Margin="6,0,0,0"
                                            Command="{Binding PromoteAllLearnedCommand}"
                                            ToolTip="{svc:Loc Key=Xaml_Promote_every_auto_allow_to_a_d9f8f8a6}" />
                                    <Button DockPanel.Dock="Right" Content="{svc:Loc Key=Xaml_Refresh_56e3badc}" Padding="10,2"
                                            Command="{Binding LoadLearnedCommand}" />
                                </DockPanel>
                                <DataGrid ItemsSource="{Binding Learned}" MaxHeight="130" Margin="0,6,0,0"
                                          AutomationProperties.Name="{svc:Loc Key=Xaml_Learning_mode_auto_allow_decisions_f7003411}">
                                    <DataGrid.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="{svc:Loc Key=Xaml_Keep_permanent_allow_2b3d33a6}"
                                                      Command="{Binding PlacementTarget.DataContext.PromoteLearnedCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                            <MenuItem Header="{svc:Loc Key=Xaml_Reverse_permanent_block_1a84420b}"
                                                      Command="{Binding PlacementTarget.DataContext.BlockLearnedCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                            <MenuItem Header="{svc:Loc Key=Xaml_Discard_prompt_again_638eb4d7}"
                                                      Command="{Binding PlacementTarget.DataContext.DiscardLearnedCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                        </ContextMenu>
                                    </DataGrid.ContextMenu>
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="{svc:Loc Key=Xaml_Application_b291beb8}" Binding="{Binding Application}" Width="2*" />
                                        <DataGridTextColumn Header="{svc:Loc Key=Xaml_Service_329cb8b6}" Binding="{Binding ServiceName}" Width="120" />
                                        <DataGridTextColumn Header="{svc:Loc Key=Xaml_Dir_84c6b9ac}" Binding="{Binding Direction}" Width="46" />
                                        <DataGridTextColumn Header="{svc:Loc Key=Xaml_Rule_78e1790e}" Binding="{Binding RuleName}" Width="*" />
                                    </DataGrid.Columns>
                                </DataGrid>
                                <TextBlock Text="{Binding LearnedStatus}" Style="{StaticResource Hg.DimText}" Margin="0,4,0,6" />

                                <DockPanel LastChildFill="False">
                                    <TextBlock DockPanel.Dock="Left" Text="{svc:Loc Key=Xaml_Recent_connection_decisions_29dbabdf}" FontWeight="SemiBold" />
                                    <Button DockPanel.Dock="Right" Content="{svc:Loc Key=Xaml_Refresh_56e3badc}"
                                            Command="{Binding LoadConsentHistoryCommand}" Padding="10,2" />
                                    <CheckBox DockPanel.Dock="Right" VerticalAlignment="Center" Margin="0,0,12,0"
                                              IsChecked="{Binding SoundOnBlock}"
                                              AutomationProperties.Name="{svc:Loc Key=Xaml_Play_a_sound_when_a_connection_2bc200d4}"
                                              ToolTip="{svc:Loc Key=Xaml_Play_a_system_sound_when_a_1ab7ad53}">
                                        <TextBlock Text="{svc:Loc Key=Xaml_Sound_on_block_08d731f1}" TextWrapping="Wrap" />
                                    </CheckBox>
                                </DockPanel>
                                <ItemsControl ItemsSource="{Binding TopTriggered}" Margin="0,4,0,0">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate><WrapPanel Orientation="Horizontal" /></ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource Hg.Badge}" Margin="0,0,6,0">
                                                <TextBlock Text="{Binding}" Style="{StaticResource Hg.DimText}" />
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <DataGrid ItemsSource="{Binding ConsentHistory}" MaxHeight="150" Margin="0,6,0,0"
                                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                          AutomationProperties.Name="{svc:Loc Key=Xaml_Recent_connection_decisions_29dbabdf}">
                                    <DataGrid.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="{svc:Loc Key=Xaml_Allow_permanently_26badf25}"
                                                      Command="{Binding PlacementTarget.DataContext.ReAllowCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                            <MenuItem Header="{svc:Loc Key=Xaml_Block_permanently_723b38dc}"
                                                      Command="{Binding PlacementTarget.DataContext.ReBlockCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                        </ContextMenu>
                                    </DataGrid.ContextMenu>
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="{svc:Loc Key=Xaml_When_769bb19e}" Binding="{Binding DecidedAtText}" Width="110" />
                                        <DataGridTextColumn Header="{svc:Loc Key=Xaml_Application_b291beb8}" Binding="{Binding Application}" Width="1.6*">
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                                                </Style>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>
                                        <DataGridTextColumn Header="{svc:Loc Key=Xaml_Dir_84c6b9ac}" Binding="{Binding Direction}" Width="46" />
                                        <DataGridTextColumn Header="{svc:Loc Key=Xaml_Remote_c93f6536}" Binding="{Binding RemoteAddress}" Width="*">
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                                                </Style>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>
                                        <DataGridTextColumn Header="{svc:Loc Key=Xaml_Proto_24131f3e}" Binding="{Binding Protocol}" Width="56" />
                                        <DataGridTextColumn Header="{svc:Loc Key=Xaml_Verdict_7f6e5a6e}" Binding="{Binding Verdict}" Width="70" />
                                        <DataGridTextColumn Header="{svc:Loc Key=Xaml_Cause_FilterCause, Default=Cause}" Binding="{Binding OriginBadge}" Width="110">
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                                                    <Setter Property="ToolTip" Value="{Binding FilterOwner}" />
                                                </Style>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>
                                        <DataGridTextColumn Header="{svc:Loc Key=Xaml_Origin_44ab85a2}" Binding="{Binding FilterOriginText}" Width="150">
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                                                    <Setter Property="ToolTip" Value="{Binding FilterOriginText}" />
                                                </Style>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>
                                        <DataGridTextColumn Header="{svc:Loc Key=Xaml_Interface_Wfp, Default=Interface}" Binding="{Binding InterfaceText}" Width="118">
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                                                    <Setter Property="ToolTip" Value="{Binding InterfaceText}" />
                                                </Style>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>
                                        <DataGridTextColumn Header="{svc:Loc Key=Xaml_Scope_4651a34e}" Binding="{Binding Scope}" Width="86" />
                                    </DataGrid.Columns>
                                </DataGrid>
                                <TextBlock Text="{svc:Loc Key=Xaml_No_decisions_yet_â_switch_to_da4e0810}"
                                           Style="{StaticResource Hg.DimText}" Margin="0,4,0,0"
                                           Visibility="{Binding ConsentHistory.Count, Converter={StaticResource EmptyStateVis}}" />
                            </StackPanel>
                        </Border>

                        <!-- Effective local-listener exposure. Snapshot analysis; never claims external reachability. -->
                        <Expander Grid.Row="4" Margin="0,8,0,0" IsExpanded="False"
                                  AutomationProperties.Name="{svc:Loc Key=ListenerExposure_Name}">
                            <Expander.Header>
                                <TextBlock Text="{svc:Loc Key=ListenerExposure_Title}" TextWrapping="Wrap" />
                            </Expander.Header>
                            <Border Style="{StaticResource Hg.Card}">
                                <StackPanel>
                                    <DockPanel LastChildFill="False">
                                        <Button DockPanel.Dock="Right" Content="{svc:Loc Key=ListenerExposure_Refresh}"
                                                Command="{Binding LoadListenersCommand}" Padding="10,2"
                                                AutomationProperties.Name="{svc:Loc Key=ListenerExposure_RefreshName}" />
                                        <TextBox Width="260" Margin="0,0,8,0"
                                                 Text="{Binding ListenerFilter, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=ListenerExposure_SearchHint}"
                                                 AutomationProperties.Name="{svc:Loc Key=ListenerExposure_SearchName}"
                                                 ToolTip="{svc:Loc Key=ListenerExposure_SearchTip}" />
                                        <TextBlock Text="{Binding ListenerStatus}" Style="{StaticResource Hg.DimText}"
                                                   Margin="4,0,0,0" VerticalAlignment="Center"
                                                   AutomationProperties.LiveSetting="Polite" />
                                    </DockPanel>
                                    <TextBlock Text="{svc:Loc Key=ListenerExposure_Disclaimer}"
                                               Style="{StaticResource Hg.DimText}" Margin="0,6,0,4"
                                               TextWrapping="Wrap" />
                                    <Grid>
                                        <DataGrid ItemsSource="{Binding ListenerView}" MaxHeight="220"
                                                  IsReadOnly="True" CanUserSortColumns="True"
                                                  AutomationProperties.Name="{svc:Loc Key=ListenerExposure_TableName}">
                                            <DataGrid.Columns>
                                                <DataGridTextColumn Header="{svc:Loc Key=ListenerExposure_Risk}" Binding="{Binding Risk}" Width="72" />
                                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_Proto_24131f3e}" Binding="{Binding Protocol}" Width="54" />
                                                <DataGridTextColumn Header="{svc:Loc Key=ListenerExposure_Endpoint}" Binding="{Binding Endpoint}" Width="138" />
                                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_Process_12f4c13a}" Binding="{Binding Process}" Width="100" />
                                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_PID_dde57e82}" Binding="{Binding Pid}" Width="52" />
                                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_Service_329cb8b6}" Binding="{Binding Service}" Width="110" />
                                                <DataGridTextColumn Header="{svc:Loc Key=ListenerExposure_Package}" Binding="{Binding Package}" Width="110" />
                                                <DataGridTextColumn Header="{svc:Loc Key=ListenerExposure_BindScope}" Binding="{Binding BindScope}" Width="92" />
                                                <DataGridTextColumn Header="{svc:Loc Key=ListenerExposure_Profiles}" Binding="{Binding ActiveProfiles}" Width="110" />
                                                <DataGridTextColumn Header="{svc:Loc Key=ListenerExposure_Coverage}" Binding="{Binding Coverage}" Width="120" />
                                                <DataGridTextColumn Header="{svc:Loc Key=ListenerExposure_Reason}" Binding="{Binding Reason}" Width="1.5*">
                                                    <DataGridTextColumn.ElementStyle>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                                                            <Setter Property="ToolTip" Value="{Binding Reason}" />
                                                        </Style>
                                                    </DataGridTextColumn.ElementStyle>
                                                </DataGridTextColumn>
                                            </DataGrid.Columns>
                                        </DataGrid>
                                        <Border Visibility="{Binding Listeners.Count, Converter={StaticResource EmptyStateVis}}"
                                                Style="{StaticResource Hg.EmptyState}" IsHitTestVisible="False">
                                            <TextBlock Text="{svc:Loc Key=ListenerExposure_Empty}"
                                                       Style="{StaticResource Hg.EmptyText}" />
                                        </Border>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </Expander>

                        <!-- Persistent connection history + per-app bandwidth (NET-070) -->
                        <Expander Grid.Row="5" Margin="0,8,0,0"
                                  AutomationProperties.Name="{svc:Loc Key=Xaml_Connection_history_and_bandwidth_90795564}">
                            <Expander.Header>
                                <TextBlock Text="{svc:Loc Key=Xaml_History_bandwidth_fb9e4b19}" TextWrapping="Wrap" />
                            </Expander.Header>
                            <Border Style="{StaticResource Hg.Card}">
                                <StackPanel>
                                    <WrapPanel>
                                        <Button Content="{svc:Loc Key=Xaml_Load_ddcb77ff}" Padding="10,2" Margin="0,0,6,6"
                                                Command="{Binding LoadHistoryCommand}" />
                                        <Button Content="{svc:Loc Key=Xaml_Prev_e96fea52}" Padding="10,2" Margin="0,0,6,6"
                                                Command="{Binding PreviousHistoryPageCommand}" />
                                        <Button Content="{svc:Loc Key=Xaml_Next_bc981983}" Padding="10,2" Margin="0,0,6,6"
                                                Command="{Binding NextHistoryPageCommand}" />
                                        <Button Content="{svc:Loc Key=Xaml_Export_CSV_5755f9ac}" Padding="10,2" Margin="0,0,6,6"
                                                Command="{Binding ExportHistoryCsvCommand}"
                                                ToolTip="{svc:Loc Key=Xaml_Save_the_loaded_connection_history_to_91aec34f}" />
                                        <Button Content="{svc:Loc Key=TrafficProfile_Export}" Padding="10,2" Margin="0,0,6,6"
                                                Command="{Binding ExportTrafficProfileCommand}"
                                                ToolTip="{svc:Loc Key=TrafficProfile_ExportTip}" />
                                        <Button Content="{svc:Loc Key=Xaml_Set_retention_ab73848f}" Padding="10,2" Margin="0,0,6,6"
                                                Command="{Binding SaveRetentionCommand}" />
                                        <Button Content="{svc:Loc Key=History_Clear}" Padding="10,2" Margin="0,0,6,6"
                                                Command="{Binding ClearHistoryCommand}"
                                                AutomationProperties.Name="{svc:Loc Key=History_ClearName}" />
                                        <TextBox Width="46" Margin="0,0,6,6" VerticalAlignment="Center"
                                                 Text="{Binding RetentionDays, UpdateSourceTrigger=PropertyChanged}"
                                                 AutomationProperties.Name="{svc:Loc Key=Xaml_History_retention_days_5a7034c6}"
                                                 ToolTip="{svc:Loc Key=Xaml_History_retention_in_days_1_365_5b9947ca}" />
                                        <TextBox Width="54" Margin="0,0,6,6" VerticalAlignment="Center"
                                                 Text="{Binding HistoryLimit, UpdateSourceTrigger=PropertyChanged}"
                                                 AutomationProperties.Name="{svc:Loc Key=History_LimitName}" />
                                        <TextBox Width="66" Margin="0,0,6,6" VerticalAlignment="Center"
                                                 Text="{Binding HistoryOffset, UpdateSourceTrigger=PropertyChanged}"
                                                 AutomationProperties.Name="{svc:Loc Key=History_OffsetName}" />
                                        <TextBox Width="160" Margin="0,0,6,6"
                                                 Text="{Binding HistorySearch, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=Xaml_SearchAllHistoryHint}"
                                                 AutomationProperties.Name="{svc:Loc Key=Xaml_Search_connection_history_7c35164f}"
                                                 ToolTip="{svc:Loc Key=Xaml_Substring_match_on_process_remote_address_ed5726b7}" />
                                        <TextBox Width="130" Margin="0,0,6,6"
                                                 Text="{Binding HistoryProcess, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=Xaml_HistoryProcessHint}"
                                                 AutomationProperties.Name="{svc:Loc Key=History_ProcessName}" />
                                        <TextBox Width="150" Margin="0,0,6,6"
                                                 Text="{Binding HistoryHost, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=Xaml_HistoryHostHint}"
                                                 AutomationProperties.Name="{svc:Loc Key=History_HostName}" />
                                        <TextBox Width="130" Margin="0,0,6,6"
                                                 Text="{Binding HistoryRemoteAddr, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=Xaml_RemoteIpHint}"
                                                 AutomationProperties.Name="{svc:Loc Key=History_RemoteName}" />
                                        <TextBox Width="96" Margin="0,0,6,6"
                                                 Text="{Binding HistoryStatusFilter, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=Xaml_Action_97c89a4d}"
                                                 AutomationProperties.Name="{svc:Loc Key=History_StatusName}" />
                                        <TextBox Width="74" Margin="0,0,6,6"
                                                 Text="{Binding HistoryProtocol, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=Xaml_TcpUdpHint}"
                                                 AutomationProperties.Name="{svc:Loc Key=History_ProtocolName}" />
                                        <TextBox Width="148" Margin="0,0,6,6"
                                                 Text="{Binding HistorySince, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=Xaml_SinceIsoHint}"
                                                 AutomationProperties.Name="{svc:Loc Key=History_SinceName}" />
                                        <TextBox Width="148" Margin="0,0,6,6"
                                                 Text="{Binding HistoryUntil, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=Xaml_UntilIsoHint}"
                                                 AutomationProperties.Name="{svc:Loc Key=History_UntilName}" />
                                    </WrapPanel>
                                    <DataGrid ItemsSource="{Binding HistoryRows}" MaxHeight="180" Margin="0,6,0,0"
                                              AutomationProperties.Name="{svc:Loc Key=Xaml_Connection_history_f48a01d3}">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="{svc:Loc Key=Xaml_When_769bb19e}" Binding="{Binding TsText}" Width="120" />
                                            <DataGridTextColumn Header="{svc:Loc Key=Xaml_Process_12f4c13a}" Binding="{Binding Process}" Width="150" />
                                            <DataGridTextColumn Header="{svc:Loc Key=Xaml_PID_dde57e82}" Binding="{Binding Pid}" Width="56" />
                                            <DataGridTextColumn Header="{svc:Loc Key=Xaml_Proto_24131f3e}" Binding="{Binding Protocol}" Width="56" />
                                            <DataGridTextColumn Header="{svc:Loc Key=History_ColumnHost}" Binding="{Binding Host}" Width="150" />
                                            <DataGridTextColumn Header="{svc:Loc Key=Xaml_Remote_address_209fa9f4}" Binding="{Binding RemoteAddr}" Width="*" />
                                            <DataGridTextColumn Header="{svc:Loc Key=Xaml_Port_fe035157}" Binding="{Binding RemotePort}" Width="56" />
                                            <DataGridTextColumn Header="{svc:Loc Key=Xaml_Country_d523ebbd}" Binding="{Binding Country}" Width="70" />
                                            <DataGridTextColumn Header="{svc:Loc Key=Xaml_ASN_b4d90f17}" Binding="{Binding Asn}" Width="160" />
                                            <DataGridTextColumn Header="{svc:Loc Key=Xaml_FW_913a8d78}" Binding="{Binding FwStatus}" Width="80" />
                                        </DataGrid.Columns>
                                    </DataGrid>
                                    <TextBlock Text="{Binding HistoryStatus}" Style="{StaticResource Hg.DimText}" Margin="0,4,0,0"
                                               AutomationProperties.LiveSetting="Polite" />

                                    <Border Style="{StaticResource Hg.StatusStrip}" Margin="0,10,0,0">
                                        <StackPanel>
                                            <TextBlock Text="{svc:Loc Key=HistoryPrivacy_Title}" FontWeight="SemiBold" />
                                            <TextBlock Text="{svc:Loc Key=HistoryPrivacy_Disclosure}" Style="{StaticResource Hg.DimText}"
                                                       TextWrapping="Wrap" Margin="0,3,0,6" />
                                            <WrapPanel>
                                                <Button Content="{svc:Loc Key=HistoryPrivacy_Load}" Command="{Binding LoadHistoryPrivacyCommand}" Margin="0,0,6,6" />
                                                <ComboBox Width="110" ItemsSource="{Binding HistoryPrivacyScopes}" SelectedItem="{Binding HistoryPrivacyScope}"
                                                          AutomationProperties.Name="{svc:Loc Key=HistoryPrivacy_Scope}" Margin="0,0,6,6" />
                                                <TextBox Width="240" Text="{Binding HistoryPrivacyMatch, UpdateSourceTrigger=PropertyChanged}"
                                                         Style="{StaticResource Hg.HintBox}" Tag="{svc:Loc Key=HistoryPrivacy_MatchHint}"
                                                         AutomationProperties.Name="{svc:Loc Key=HistoryPrivacy_MatchHint}" Margin="0,0,6,6" />
                                                <Button Content="{svc:Loc Key=HistoryPrivacy_Save}" Command="{Binding SaveHistoryPrivacyCommand}" Margin="0,0,6,6" />
                                            </WrapPanel>
                                            <ItemsControl ItemsSource="{Binding HistoryPrivacyExclusions}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <DockPanel Margin="0,2">
                                                            <Button DockPanel.Dock="Right" Content="{svc:Loc Key=HistoryPrivacy_Delete}"
                                                                    Command="{Binding DataContext.DeleteHistoryPrivacyCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                                    CommandParameter="{Binding}" Padding="8,2" />
                                                            <TextBlock Text="{Binding Scope}" FontWeight="SemiBold" Margin="0,0,8,0" />
                                                            <TextBlock Text="{Binding Match}" />
                                                        </DockPanel>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                            <TextBlock Text="{Binding HistoryPrivacyStatus}" Style="{StaticResource Hg.DimText}"
                                                       TextWrapping="Wrap" AutomationProperties.LiveSetting="Polite" />
                                        </StackPanel>
                                    </Border>

                                    <DockPanel LastChildFill="False" Margin="0,12,0,0">
                                        <TextBlock DockPanel.Dock="Left" Text="{svc:Loc Key=Xaml_Event_ledger_fe4c6b16}" FontWeight="SemiBold" />
                                        <TextBlock DockPanel.Dock="Right" Text="{Binding EventStatus}"
                                                   Style="{StaticResource Hg.DimText}" />
                                    </DockPanel>
                                    <WrapPanel Margin="0,6,0,0">
                                        <Button Content="{svc:Loc Key=Xaml_Load_events_6b8e35ba}" Padding="10,2" Margin="0,0,6,6"
                                                Command="{Binding LoadEventsCommand}" />
                                        <Button Content="{svc:Loc Key=Xaml_Prev_e96fea52}" Padding="10,2" Margin="0,0,6,6"
                                                Command="{Binding PreviousEventsPageCommand}" />
                                        <Button Content="{svc:Loc Key=Xaml_Next_bc981983}" Padding="10,2" Margin="0,0,6,6"
                                                Command="{Binding NextEventsPageCommand}" />
                                        <TextBox Width="54" Margin="0,0,6,6"
                                                 Text="{Binding EventLimit, UpdateSourceTrigger=PropertyChanged}"
                                                 AutomationProperties.Name="{svc:Loc Key=Xaml_Event_page_size_168440bd}"
                                                 ToolTip="{svc:Loc Key=Xaml_Event_page_size_1_2000_2f55649d}" />
                                        <TextBox Width="66" Margin="0,0,6,6"
                                                 Text="{Binding EventOffset, UpdateSourceTrigger=PropertyChanged}"
                                                 AutomationProperties.Name="{svc:Loc Key=Xaml_Event_page_offset_5b573d88}"
                                                 ToolTip="{svc:Loc Key=Xaml_Zero_based_event_page_offset_ec48ffd4}" />
                                        <TextBox Width="150" Margin="0,0,6,6"
                                                 Text="{Binding EventSearch, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=Xaml_SearchEventsHint}"
                                                 AutomationProperties.Name="{svc:Loc Key=Xaml_Search_event_ledger_3c9d9d9d}" />
                                        <ComboBox Width="112" Margin="0,0,6,6"
                                                  ItemsSource="{x:Static vm:FwActivityViewModel.EventCategories}"
                                                  SelectedItem="{Binding EventCategory, UpdateSourceTrigger=PropertyChanged}"
                                                  AutomationProperties.Name="{svc:Loc Key=Xaml_Event_category_filter_0686527b}"
                                                  ToolTip="{svc:Loc Key=Xaml_Event_category_fcd26e04}" />
                                        <TextBox Width="112" Margin="0,0,6,6"
                                                 Text="{Binding EventAction, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=Xaml_Action_97c89a4d}"
                                                 AutomationProperties.Name="{svc:Loc Key=Xaml_Event_action_filter_a44d0147}" />
                                        <TextBox Width="100" Margin="0,0,6,6"
                                                 Text="{Binding EventReason, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=Xaml_Reason_f219cc06}"
                                                 AutomationProperties.Name="{svc:Loc Key=Xaml_Event_reason_filter_b375e7c7}" />
                                        <TextBox Width="120" Margin="0,0,6,6"
                                                 Text="{Binding EventDomain, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=Xaml_Domain_9b10914d}"
                                                 AutomationProperties.Name="{svc:Loc Key=Xaml_Event_domain_filter_338ec470}" />
                                        <TextBox Width="120" Margin="0,0,6,6"
                                                 Text="{Binding EventProcess, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=Xaml_Process_12f4c13a}"
                                                 AutomationProperties.Name="{svc:Loc Key=Xaml_Event_process_filter_75ccb70d}" />
                                        <TextBox Width="148" Margin="0,0,6,6"
                                                 Text="{Binding EventSince, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=Xaml_SinceIsoHint}"
                                                 AutomationProperties.Name="{svc:Loc Key=Xaml_Event_since_filter_833b82e2}" />
                                        <TextBox Width="148" Margin="0,0,6,6"
                                                 Text="{Binding EventUntil, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=Xaml_UntilIsoHint}"
                                                 AutomationProperties.Name="{svc:Loc Key=Xaml_Event_until_filter_9d11b765}" />
                                    </WrapPanel>
                                    <DataGrid ItemsSource="{Binding EventRows}" MaxHeight="180" Margin="0,2,0,0"
                                              AutomationProperties.Name="{svc:Loc Key=Xaml_Persisted_event_ledger_0238df0e}">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="{svc:Loc Key=Xaml_When_769bb19e}" Binding="{Binding TsText}" Width="118" />
                                            <DataGridTextColumn Header="{svc:Loc Key=Xaml_Category_a3c686e7}" Binding="{Binding Category}" Width="82" />
                                            <DataGridTextColumn Header="{svc:Loc Key=Xaml_Action_97c89a4d}" Binding="{Binding Action}" Width="130" />
                                            <DataGridTextColumn Header="{svc:Loc Key=Xaml_Reason_f219cc06}" Binding="{Binding Reason}" Width="92" />
                                            <DataGridTextColumn Header="{svc:Loc Key=Xaml_Subject_8d183dbd}" Binding="{Binding Domain}" Width="1.2*" />
                                            <DataGridTextColumn Header="{svc:Loc Key=Xaml_Process_12f4c13a}" Binding="{Binding Process}" Width="1*" />
                                            <DataGridTextColumn Header="{svc:Loc Key=Xaml_Details_dc3decbb}" Binding="{Binding Details}" Width="1.6*" />
                                        </DataGrid.Columns>
                                    </DataGrid>

                                    <DockPanel LastChildFill="False" Margin="0,10,0,0">
                                        <TextBlock DockPanel.Dock="Left" Text="{svc:Loc Key=UsageRollups_Title}" FontWeight="SemiBold" />
                                        <TextBlock DockPanel.Dock="Right" Text="{Binding UsageStatus}"
                                                   Style="{StaticResource Hg.DimText}" />
                                    </DockPanel>
                                    <WrapPanel Margin="0,6,0,0">
                                        <Button Content="{svc:Loc Key=UsageRollups_Load}" Padding="10,2" Margin="0,0,6,6"
                                                Command="{Binding LoadUsageCommand}" />
                                        <TextBox Width="54" Margin="0,0,6,6"
                                                 Text="{Binding UsageDays, UpdateSourceTrigger=PropertyChanged}"
                                                 AutomationProperties.Name="{svc:Loc Key=UsageRollups_DaysName}"
                                                 ToolTip="{svc:Loc Key=UsageRollups_DaysTip}" />
                                        <TextBox Width="54" Margin="0,0,6,6"
                                                 Text="{Binding UsageLimit, UpdateSourceTrigger=PropertyChanged}"
                                                 AutomationProperties.Name="{svc:Loc Key=UsageRollups_LimitName}"
                                                 ToolTip="{svc:Loc Key=UsageRollups_LimitTip}" />
                                        <TextBox Width="150" Margin="0,0,6,6"
                                                 Text="{Binding UsageSearch, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=UsageRollups_SearchHint}"
                                                 AutomationProperties.Name="{svc:Loc Key=UsageRollups_SearchName}" />
                                        <TextBox Width="120" Margin="0,0,6,6"
                                                 Text="{Binding UsageProcess, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=UsageRollups_ProcessHint}"
                                                 AutomationProperties.Name="{svc:Loc Key=UsageRollups_ProcessName}" />
                                        <TextBox Width="120" Margin="0,0,6,6"
                                                 Text="{Binding UsageDomain, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=UsageRollups_DomainHint}"
                                                 AutomationProperties.Name="{svc:Loc Key=UsageRollups_DomainName}" />
                                    </WrapPanel>
                                    <DataGrid ItemsSource="{Binding UsageRows}" MaxHeight="180" Margin="0,2,0,0"
                                              AutomationProperties.Name="{svc:Loc Key=UsageRollups_TableName}">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="{svc:Loc Key=UsageRollups_Day}" Binding="{Binding Day}" Width="90" />
                                            <DataGridTextColumn Header="{svc:Loc Key=Xaml_Process_12f4c13a}" Binding="{Binding Process}" Width="1.1*" />
                                            <DataGridTextColumn Header="{svc:Loc Key=Xaml_Domain_9b10914d}" Binding="{Binding Domain}" Width="1.4*" />
                                            <DataGridTextColumn Header="{svc:Loc Key=UsageRollups_Sent}" Binding="{Binding SentText}" Width="82" />
                                            <DataGridTextColumn Header="{svc:Loc Key=UsageRollups_Received}" Binding="{Binding RecvText}" Width="82" />
                                            <DataGridTextColumn Header="{svc:Loc Key=UsageRollups_Total}" Binding="{Binding TotalText}" Width="82" />
                                        </DataGrid.Columns>
                                    </DataGrid>

                                    <DockPanel LastChildFill="False" Margin="0,10,0,0">
                                        <TextBlock DockPanel.Dock="Left" Text="{svc:Loc Key=UsageQuota_Title}" FontWeight="SemiBold" />
                                        <TextBlock DockPanel.Dock="Right" Text="{Binding UsageQuotaStatus}"
                                                   Style="{StaticResource Hg.DimText}" />
                                    </DockPanel>
                                    <WrapPanel Margin="0,6,0,0">
                                        <Button Content="{svc:Loc Key=UsageQuota_Load}" Padding="10,2" Margin="0,0,6,6"
                                                Command="{Binding LoadUsageQuotasCommand}" />
                                        <Button Content="{svc:Loc Key=UsageQuota_Save}" Padding="10,2" Margin="0,0,6,6"
                                                Command="{Binding SaveUsageQuotaCommand}" />
                                        <Button Content="{svc:Loc Key=UsageQuota_Reset}" Padding="10,2" Margin="0,0,6,6"
                                                Command="{Binding ResetUsageQuotaHistoryCommand}" />
                                        <Button Content="{svc:Loc Key=UsageQuota_Export}" Padding="10,2" Margin="0,0,6,6"
                                                Command="{Binding ExportUsageQuotaHistoryCommand}" />
                                        <TextBox Width="72" Margin="0,0,6,6"
                                                 Text="{Binding UsageQuotaScope, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=UsageQuota_ScopeHint}"
                                                 AutomationProperties.Name="{svc:Loc Key=UsageQuota_ScopeName}" />
                                        <TextBox Width="150" Margin="0,0,6,6"
                                                 Text="{Binding UsageQuotaMatch, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=UsageQuota_MatchHint}"
                                                 AutomationProperties.Name="{svc:Loc Key=UsageQuota_MatchName}" />
                                        <TextBox Width="86" Margin="0,0,6,6"
                                                 Text="{Binding UsageQuotaLimitText, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=UsageQuota_LimitHint}"
                                                 AutomationProperties.Name="{svc:Loc Key=UsageQuota_LimitName}" />
                                        <TextBox Width="54" Margin="0,0,6,6"
                                                 Text="{Binding UsageQuotaWindowDays, UpdateSourceTrigger=PropertyChanged}"
                                                 AutomationProperties.Name="{svc:Loc Key=UsageQuota_WindowName}"
                                                 ToolTip="{svc:Loc Key=UsageQuota_WindowTip}" />
                                        <CheckBox Content="{svc:Loc Key=UsageQuota_Enabled}" Margin="0,2,6,6"
                                                  IsChecked="{Binding UsageQuotaEnabled}"
                                                  AutomationProperties.Name="{svc:Loc Key=UsageQuota_EnabledName}" />
                                        <CheckBox Content="{svc:Loc Key=UsageQuota_BlockOnExceed}" Margin="0,2,6,6"
                                                  IsChecked="{Binding UsageQuotaBlockOnExceed}"
                                                  AutomationProperties.Name="{svc:Loc Key=UsageQuota_BlockOnExceedName}"
                                                  ToolTip="{svc:Loc Key=UsageQuota_BlockOnExceedTip}" />
                                    </WrapPanel>
                                    <DataGrid ItemsSource="{Binding UsageQuotaRules}" MaxHeight="170" Margin="0,2,0,0"
                                              AutomationProperties.Name="{svc:Loc Key=UsageQuota_TableName}">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="{svc:Loc Key=UsageQuota_Scope}" Binding="{Binding Scope}" Width="74" />
                                            <DataGridTextColumn Header="{svc:Loc Key=UsageQuota_Match}" Binding="{Binding Match}" Width="1.4*" />
                                            <DataGridCheckBoxColumn Header="{svc:Loc Key=UsageQuota_EnabledShort}" Binding="{Binding Enabled}" Width="70" IsReadOnly="True" />
                                            <DataGridTextColumn Header="{svc:Loc Key=UsageQuota_Window}" Binding="{Binding WindowDays}" Width="70" />
                                            <DataGridTextColumn Header="{svc:Loc Key=UsageQuota_Used}" Binding="{Binding UsedText}" Width="86" />
                                            <DataGridTextColumn Header="{svc:Loc Key=UsageQuota_Limit}" Binding="{Binding LimitText}" Width="86" />
                                            <DataGridTextColumn Header="{svc:Loc Key=UsageQuota_Percent}" Binding="{Binding PercentText}" Width="70" />
                                            <DataGridTextColumn Header="{svc:Loc Key=UsageQuota_Block}" Binding="{Binding BlockText}" Width="70" />
                                            <DataGridTextColumn Header="{svc:Loc Key=UsageQuota_LastAlert}" Binding="{Binding LastAlertedText}" Width="1.2*" />
                                            <DataGridTemplateColumn Header="{svc:Loc Key=UsageQuota_Actions}" Width="82">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <Button Content="{svc:Loc Key=UsageQuota_Delete}" Padding="8,1"
                                                                Command="{Binding DataContext.DeleteUsageQuotaCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                                CommandParameter="{Binding}" />
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                        </DataGrid.Columns>
                                    </DataGrid>

                                    <DockPanel LastChildFill="False" Margin="0,10,0,0">
                                        <TextBlock DockPanel.Dock="Left" Text="{svc:Loc Key=Xaml_Per_app_bandwidth_cbf1f3bd}" FontWeight="SemiBold" />
                                        <TextBlock DockPanel.Dock="Right" Text="{Binding BandwidthStatus}"
                                                   Style="{StaticResource Hg.DimText}" />
                                    </DockPanel>
                                    <Grid Height="86" Margin="0,6,0,0" ClipToBounds="True">
                                        <ItemsControl ItemsSource="{Binding Bandwidth}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <Grid />
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Viewbox Stretch="Fill">
                                                        <Canvas Width="600" Height="100">
                                                            <Polyline Points="{Binding PointsText, Converter={StaticResource PointsConverter}}"
                                                                      Stroke="{Binding ColorIndex, Converter={StaticResource SeriesBrush}}"
                                                                      StrokeThickness="1.6" StrokeLineJoin="Round" />
                                                        </Canvas>
                                                    </Viewbox>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Grid>
                                    <ItemsControl ItemsSource="{Binding Bandwidth}" Margin="0,4,0,0">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel Orientation="Horizontal" />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" Margin="0,0,14,0">
                                                    <Rectangle Width="10" Height="3" VerticalAlignment="Center" Margin="0,0,5,0"
                                                               Fill="{Binding ColorIndex, Converter={StaticResource SeriesBrush}}" />
                                                    <TextBlock Text="{Binding Name}" Style="{StaticResource Hg.DimText}" />
                                                    <TextBlock Text="{Binding LegendText}" Style="{StaticResource Hg.DimText}" Margin="5,0,0,0" />
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </Expander>

                        <TextBlock Grid.Row="6" Margin="2,6,0,0"
                                   Text="{Binding StatusText}"
                                   Style="{StaticResource Hg.DimText}"
                                   AutomationProperties.LiveSetting="Polite" />
                    </Grid>
                </TabItem>

                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <Path Style="{StaticResource Hg.MainTabIconPath}"
                                  Data="M 3 4 H 13 M 3 8 H 13 M 3 12 H 13 M 5 2 V 14 M 11 2 V 14"
                                  Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=TabItem}}" />
                            <TextBlock Text="{svc:Loc Key=Tab_FirewallRules, Default=Firewall Rules}" />
                        </StackPanel>
                    </TabItem.Header>
                    <Grid Margin="4" DataContext="{Binding FwRules}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Style="{StaticResource Hg.CommandBar}" Margin="0,0,0,8">
                        <DockPanel>
                            <Button DockPanel.Dock="Right" Content="{svc:Loc Key=Xaml_Refresh_56e3badc}"
                                    Command="{Binding RefreshCommand}" Margin="8,0,0,0" />
                            <Button DockPanel.Dock="Right"
                                    Command="{Binding AdoptRulesCommand}" Margin="8,0,0,0"
                                    ToolTip="{svc:Loc Key=FwRules_IncludeWindowsRulesTip, Default=Index current Windows Firewall rules in this view. Read-only; nothing on the live firewall changes.}">
                                <TextBlock Text="{svc:Loc Key=FwRules_IncludeWindowsRules, Default=Include Windows rules}" TextWrapping="Wrap" />
                            </Button>
                            <CheckBox DockPanel.Dock="Right" Margin="8,0,0,0"
                                      VerticalAlignment="Center" IsChecked="{Binding HostsGuardOnly}"
                                      ToolTip="{svc:Loc Key=FwRules_HostsGuardOnlyTip, Default=Show only rules created and managed by HostsGuard.}">
                                <TextBlock Text="{svc:Loc Key=Xaml_HostsGuard_rules_only_a5afd2dd}" TextWrapping="Wrap" />
                            </CheckBox>
                            <CheckBox DockPanel.Dock="Right" Content="{svc:Loc Key=Xaml_Drift_only_d7e80af4}" Margin="8,0,0,0"
                                      VerticalAlignment="Center" IsChecked="{Binding DriftOnly}"
                                      ToolTip="{svc:Loc Key=Xaml_Show_only_firewall_rules_that_changed_7d356f72}" />
                            <TextBox Text="{Binding Filter, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource Hg.HintBox}"
                                      Tag="{svc:Loc Key=Xaml_SearchRulesHint}"
                                     AutomationProperties.Name="{svc:Loc Key=Xaml_Search_firewall_rules_bbf5460e}"
                                     ToolTip="{svc:Loc Key=Xaml_Filters_as_you_type_â_matches_4dd5c1e7}" />
                        </DockPanel>
                        </Border>

                        <DataGrid x:Name="FwRulesGrid" Grid.Row="1" ItemsSource="{Binding Rules}"
                                  AutomationProperties.Name="{svc:Loc Key=Xaml_Firewall_rules_1e019fbf}"
                                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                  IsVisibleChanged="OnGridVisible">
                            <DataGrid.Resources>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                                    <Setter Property="ToolTip" Value="{Binding Text, RelativeSource={RelativeSource Self}}" />
                                </Style>
                            </DataGrid.Resources>
                            <DataGrid.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="{svc:Loc Key=Xaml_Enable_disable_37f81b0c}"
                                              Command="{Binding PlacementTarget.DataContext.ToggleRuleCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                    <MenuItem Header="{svc:Loc Key=FwRules_EditManaged}"
                                              Command="{Binding PlacementTarget.DataContext.EditRuleCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              ToolTip="{svc:Loc Key=FwRules_EditManagedTip}" />
                                    <MenuItem Header="{svc:Loc Key=Xaml_Delete_f6fdbe48}"
                                              Command="{Binding PlacementTarget.DataContext.DeleteRuleCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.SelectedItem.Name, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                    <MenuItem Header="{svc:Loc Key=Xaml_Delete_selected_8516a714}"
                                              Command="{Binding PlacementTarget.DataContext.DeleteSelectedCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.SelectedItems, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                    <Separator />
                                    <MenuItem Header="{svc:Loc Key=Xaml_Assign_to_groupâ_0952229e}"
                                              Command="{Binding PlacementTarget.DataContext.AssignToGroupCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.SelectedItems, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              ToolTip="{svc:Loc Key=Xaml_Assign_the_selected_HG_rules_to_490e2bd6}" />
                                    <MenuItem Header="{svc:Loc Key=Xaml_Suggest_rebind_programâ_00cd0094}"
                                              Command="{Binding PlacementTarget.DataContext.RebindRuleCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                </ContextMenu>
                            </DataGrid.ContextMenu>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_Name_709a2322}" Binding="{Binding Name}" Width="1.5*" />
                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_Dir_84c6b9ac}" Binding="{Binding Direction}" Width="42" />
                                 <DataGridTextColumn Header="{svc:Loc Key=Xaml_Action_97c89a4d}" Binding="{Binding Action}" Width="58" />
                                 <DataGridTextColumn Header="{svc:Loc Key=Xaml_Origin_44ab85a2}" Binding="{Binding Origin}" Width="68" IsReadOnly="True" />
                                 <DataGridTextColumn Header="{svc:Loc Key=FwRules_TargetKind, Default=Kind}" Binding="{Binding TargetKind}" Width="64" IsReadOnly="True" />
                                 <DataGridCheckBoxColumn Header="{svc:Loc Key=Xaml_On_e0049a66}" Binding="{Binding Enabled}" Width="38" IsReadOnly="True" />
                                 <DataGridTextColumn Header="{svc:Loc Key=Xaml_Remote_c93f6536}" Binding="{Binding RemoteAddr}" Width="0.8*" />
                                 <DataGridTextColumn Header="{svc:Loc Key=Xaml_Proto_24131f3e}" Binding="{Binding Protocol}" Width="52" />
                                 <DataGridTextColumn Header="{svc:Loc Key=FwRules_Ports, Default=Ports}" Binding="{Binding Ports}" Width="64" IsReadOnly="True" />
                                 <DataGridTextColumn Header="{svc:Loc Key=FwRules_Target, Default=Target}" Binding="{Binding Target}" Width="1.8*" IsReadOnly="True" />
                                 <DataGridTextColumn Header="{svc:Loc Key=FwRules_Interfaces, Default=Interfaces}" Binding="{Binding Interfaces}" Width="72" />
                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_Service_329cb8b6}" Binding="{Binding ServiceName}" Width="80" />
                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_Drift_0c1f6e7a}" Binding="{Binding DriftStatus}" Width="64">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                                            <Setter Property="ToolTip" Value="{Binding DriftDetail}" />
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_Flags_5d728758}" Binding="{Binding Flags}" Width="86">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                                            <Setter Property="ToolTip" Value="{Binding Flags}" />
                                            <Setter Property="Foreground" Value="{DynamicResource Hg.Peach}" />
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>

                        <Border Grid.Row="1"
                                Style="{StaticResource Hg.EmptyState}"
                                Visibility="{Binding Rules.Count, Converter={StaticResource EmptyStateVis}}">
                            <StackPanel>
                                <TextBlock Text="{svc:Loc Key=Xaml_No_firewall_rules_match_6d4b1891}"
                                           Style="{StaticResource Hg.SectionTitle}"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,4" />
                                <TextBlock Text="{svc:Loc Key=Xaml_Clear_the_filter_turn_off_HostsGuard_811239c1}"
                                           Style="{StaticResource Hg.EmptyText}" />
                            </StackPanel>
                        </Border>

                        <!-- Rule groups (NET-103): enable/disable a named set atomically -->
                        <ItemsControl Grid.Row="2" ItemsSource="{Binding RuleGroups}" Margin="0,8,0,0">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource Hg.Card}" Padding="8,4" Margin="0,0,8,0">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Label}" VerticalAlignment="Center" Margin="0,0,8,0" />
                                            <Button Content="{svc:Loc Key=Xaml_On_e0049a66}" Padding="8,1" Margin="0,0,4,0"
                                                    Command="{Binding DataContext.EnableGroupCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                    CommandParameter="{Binding}" />
                                            <Button Content="{svc:Loc Key=Xaml_Off_e3de5ab0}" Padding="8,1"
                                                    Command="{Binding DataContext.DisableGroupCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                    CommandParameter="{Binding}" />
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- Inline custom-rule form -->
                        <Border Grid.Row="3" Style="{StaticResource Hg.Card}" Margin="0,8,0,0">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <DockPanel Grid.Row="0" LastChildFill="True">
                                    <Button DockPanel.Dock="Right" Content="{Binding AuthoringActionText}"
                                            Style="{StaticResource Hg.AccentButton}"
                                            Command="{Binding CreateRuleCommand}" Margin="12,0,0,0"
                                            ToolTip="{Binding CreateRuleHelpText}" />
                                    <Button DockPanel.Dock="Right" Content="{svc:Loc Key=FwRules_CancelEdit}"
                                            Command="{Binding CancelRuleEditCommand}" Margin="12,0,0,0"
                                            Visibility="{Binding IsEditingRule, Converter={StaticResource BoolToVis}}" />
                                    <CheckBox DockPanel.Dock="Right" Content="{svc:Loc Key=FwRules_EnabledLabel}"
                                              IsChecked="{Binding NewRuleEnabled}" Margin="12,0,0,0"
                                              VerticalAlignment="Center"
                                              AutomationProperties.Name="{svc:Loc Key=FwRules_EnabledName}" />
                                    <StackPanel>
                                        <TextBlock Text="{svc:Loc Key=Xaml_Create_HostsGuard_rule_0516d184}"
                                                   Style="{StaticResource Hg.SectionTitle}"
                                                   Visibility="{Binding IsEditingRule, Converter={StaticResource InverseBoolToVis}}" />
                                        <TextBlock Text="{svc:Loc Key=FwRules_EditTitle}"
                                                   Style="{StaticResource Hg.SectionTitle}"
                                                   Visibility="{Binding IsEditingRule, Converter={StaticResource BoolToVis}}" />
                                        <TextBlock Text="{svc:Loc Key=Xaml_Rules_created_here_use_the_HG_b7f8e3c1}"
                                                   Style="{StaticResource Hg.HelperText}" Margin="0,3,0,0" />
                                    </StackPanel>
                                </DockPanel>

                                <Grid Grid.Row="1" Margin="0,12,0,0">
                                     <Grid.ColumnDefinitions>
                                         <ColumnDefinition Width="160" />
                                         <ColumnDefinition Width="76" />
                                         <ColumnDefinition Width="92" />
                                         <ColumnDefinition Width="82" />
                                         <ColumnDefinition Width="150" />
                                         <ColumnDefinition Width="220" />
                                         <ColumnDefinition Width="*" />
                                     </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="{svc:Loc Key=Xaml_Name_709a2322}" Style="{StaticResource Hg.FieldLabel}" />
                                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{svc:Loc Key=Xaml_Dir_84c6b9ac}" Style="{StaticResource Hg.FieldLabel}" Margin="0,0,8,4" />
                                    <TextBlock Grid.Row="0" Grid.Column="2" Text="{svc:Loc Key=Xaml_Action_97c89a4d}" Style="{StaticResource Hg.FieldLabel}" Margin="0,0,8,4" />
                                     <TextBlock Grid.Row="0" Grid.Column="3" Text="{svc:Loc Key=Xaml_Protocol_1ed77c3f}" Style="{StaticResource Hg.FieldLabel}" Margin="0,0,8,4" />
                                     <TextBlock Grid.Row="0" Grid.Column="4" Text="{svc:Loc Key=Xaml_Remote_c93f6536}" Style="{StaticResource Hg.FieldLabel}" Margin="0,0,8,4" />
                                     <TextBlock Grid.Row="0" Grid.Column="5" Text="{svc:Loc Key=FwRules_Package, Default=Package}" Style="{StaticResource Hg.FieldLabel}" Margin="0,0,8,4" />
                                     <TextBlock Grid.Row="0" Grid.Column="6" Text="{svc:Loc Key=Xaml_Program_path_223d2c44}" Style="{StaticResource Hg.FieldLabel}" />

                                    <TextBox Grid.Row="1" Grid.Column="0" Margin="0,0,8,0"
                                             Text="{Binding NewRuleName, UpdateSourceTrigger=PropertyChanged}"
                                             Tag="{svc:Loc Key=Xaml_RequiredHint}"
                                             AutomationProperties.Name="{svc:Loc Key=Xaml_New_firewall_rule_name_025d122b}"
                                             ToolTip="{svc:Loc Key=Xaml_Rule_name_HG_prefix_added_automatically_80fd16a4}">
                                        <TextBox.Style>
                                            <Style TargetType="TextBox" BasedOn="{StaticResource Hg.HintBox}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsEditingRule}" Value="True">
                                                        <Setter Property="IsReadOnly" Value="True" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBox.Style>
                                    </TextBox>
                                    <ComboBox Grid.Row="1" Grid.Column="1" Margin="0,0,8,0"
                                              ItemsSource="{x:Static vm:FwRulesViewModel.Directions}"
                                              AutomationProperties.Name="{svc:Loc Key=Xaml_New_firewall_rule_direction_d8bbd16c}"
                                              SelectedItem="{Binding NewRuleDirection}" />
                                    <ComboBox Grid.Row="1" Grid.Column="2" Margin="0,0,8,0"
                                              ItemsSource="{x:Static vm:FwRulesViewModel.Actions}"
                                              AutomationProperties.Name="{svc:Loc Key=Xaml_New_firewall_rule_action_86b8dc92}"
                                              SelectedItem="{Binding NewRuleAction}" />
                                    <ComboBox Grid.Row="1" Grid.Column="3" Margin="0,0,8,0"
                                              ItemsSource="{x:Static vm:FwRulesViewModel.Protocols}"
                                              AutomationProperties.Name="{svc:Loc Key=Xaml_New_firewall_rule_protocol_ffde13a2}"
                                              SelectedItem="{Binding NewRuleProtocol}" />
                                    <TextBox Grid.Row="1" Grid.Column="4" Margin="0,0,8,0"
                                             Text="{Binding NewRuleRemoteAddr, UpdateSourceTrigger=PropertyChanged}"
                                             AutomationProperties.Name="{svc:Loc Key=Xaml_New_firewall_rule_remote_address_80867949}"
                                             ToolTip="{svc:Loc Key=Xaml_Remote_address_IP_CIDR_or_range_3aeffde3}" />
                                     <ComboBox Grid.Row="1" Grid.Column="5" Margin="0,0,8,0"
                                               ItemsSource="{Binding AppPackages}"
                                               DisplayMemberPath="Label"
                                               SelectedValuePath="PackageFamilyName"
                                               SelectedValue="{Binding NewRulePackageFamily, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                               Text="{Binding NewRulePackageFamily, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                               IsEditable="True"
                                               IsTextSearchEnabled="True"
                                               AutomationProperties.Name="{svc:Loc Key=FwRules_NewRulePackage, Default=New firewall rule package}"
                                               ToolTip="{svc:Loc Key=FwRules_PackageTip, Default=Optional MSIX package family name or package SID}" />
                                     <TextBox Grid.Row="1" Grid.Column="6"
                                              Text="{Binding NewRuleProgram, UpdateSourceTrigger=PropertyChanged}"
                                              AutomationProperties.Name="{svc:Loc Key=Xaml_New_firewall_rule_program_path_1032f360}"
                                             ToolTip="{svc:Loc Key=Xaml_Program_path_optional_c4107d4a}" />
                                </Grid>

                                <Grid Grid.Row="2" Margin="0,10,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="170" />
                                        <ColumnDefinition Width="170" />
                                        <ColumnDefinition Width="240" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="{svc:Loc Key=FwRules_LocalPortsLabel}"
                                               Style="{StaticResource Hg.FieldLabel}" />
                                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{svc:Loc Key=FwRules_RemotePortsLabel}"
                                               Style="{StaticResource Hg.FieldLabel}" />
                                    <TextBlock Grid.Row="0" Grid.Column="2" Text="{svc:Loc Key=FwRules_ManualInterfacesLabel}"
                                               Style="{StaticResource Hg.FieldLabel}" />
                                    <DockPanel Grid.Row="0" Grid.Column="3" LastChildFill="True">
                                        <Button DockPanel.Dock="Right" Content="{svc:Loc Key=Xaml_Refresh_56e3badc}"
                                                Command="{Binding LoadInterfaceAliasesCommand}" Padding="8,1" />
                                        <TextBlock Text="{svc:Loc Key=FwRules_InterfaceAliasesLabel}"
                                                   Style="{StaticResource Hg.FieldLabel}" />
                                    </DockPanel>
                                    <TextBox Grid.Row="1" Grid.Column="0" Margin="0,0,8,0"
                                             Text="{Binding NewRuleLocalPorts, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource Hg.HintBox}" Tag="{svc:Loc Key=FwRules_PortsHint}"
                                             AutomationProperties.Name="{svc:Loc Key=FwRules_LocalPortsName}"
                                             ToolTip="{svc:Loc Key=FwRules_PortsTip}" />
                                    <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,8,0"
                                             Text="{Binding NewRuleRemotePorts, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource Hg.HintBox}" Tag="{svc:Loc Key=FwRules_PortsHint}"
                                             AutomationProperties.Name="{svc:Loc Key=FwRules_RemotePortsName}"
                                             ToolTip="{svc:Loc Key=FwRules_PortsTip}" />
                                    <TextBox Grid.Row="1" Grid.Column="2" Margin="0,0,8,0"
                                             Text="{Binding NewRuleInterfaces, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource Hg.HintBox}" Tag="{svc:Loc Key=FwRules_InterfacesHint}"
                                             AutomationProperties.Name="{svc:Loc Key=FwRules_ManualInterfacesName}"
                                             ToolTip="{svc:Loc Key=FwRules_ManualInterfacesTip}" />
                                    <ScrollViewer Grid.Row="1" Grid.Column="3" MaxHeight="72"
                                                  VerticalScrollBarVisibility="Auto">
                                        <ItemsControl ItemsSource="{Binding InterfaceAliases}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <CheckBox Content="{Binding Name}" IsChecked="{Binding IsSelected}"
                                                              Margin="0,0,12,4" ToolTip="{Binding Description}"
                                                              AutomationProperties.Name="{Binding Name}" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                    <TextBlock Grid.Row="2" Grid.Column="3" Text="{Binding InterfaceAliasStatus}"
                                               Style="{StaticResource Hg.DimText}" Margin="0,3,0,0"
                                               TextWrapping="Wrap"
                                               AutomationProperties.LiveSetting="Polite" />
                                </Grid>

                                <Border Grid.Row="3" Style="{StaticResource Hg.StatusStrip}" Margin="0,8,0,0">
                                    <TextBlock Text="{Binding CreateRulePreview}" TextWrapping="Wrap"
                                               AutomationProperties.Name="{svc:Loc Key=FwRules_PreviewName}" />
                                </Border>

                                <TextBlock Grid.Row="4" Margin="0,8,0,0"
                                           Text="{Binding CreateRuleHelpText}"
                                           Style="{StaticResource Hg.HelperText}"
                                           AutomationProperties.LiveSetting="Polite" />
                            </Grid>
                        </Border>

                        <Expander Grid.Row="4" Margin="0,8,0,0" IsExpanded="False"
                                  AutomationProperties.Name="{svc:Loc Key=RuleAnalysis_Name}">
                            <Expander.Header>
                                <TextBlock Text="{svc:Loc Key=RuleAnalysis_Title}" TextWrapping="Wrap" />
                            </Expander.Header>
                            <Border Style="{StaticResource Hg.Card}">
                                <StackPanel>
                                    <DockPanel LastChildFill="False">
                                        <Button DockPanel.Dock="Right" Content="{svc:Loc Key=RuleAnalysis_Apply}"
                                                Command="{Binding ApplyRuleCleanupCommand}" Padding="10,2" Margin="6,0,0,0"
                                                AutomationProperties.Name="{svc:Loc Key=RuleAnalysis_ApplyName}"
                                                ToolTip="{svc:Loc Key=RuleAnalysis_ApplyTip}" />
                                        <Button DockPanel.Dock="Right" Content="{svc:Loc Key=RuleAnalysis_Preview}"
                                                Command="{Binding PreviewRuleCleanupCommand}" Padding="10,2" Margin="6,0,0,0"
                                                AutomationProperties.Name="{svc:Loc Key=RuleAnalysis_PreviewName}" />
                                        <Button DockPanel.Dock="Right" Content="{svc:Loc Key=RuleAnalysis_Run}"
                                                Command="{Binding AnalyzeRulesCommand}" Padding="10,2" Margin="6,0,0,0"
                                                AutomationProperties.Name="{svc:Loc Key=RuleAnalysis_RunName}" />
                                        <CheckBox DockPanel.Dock="Right" Content="{svc:Loc Key=RuleAnalysis_GroupProvenance}"
                                                  IsChecked="{Binding GroupAnalysisByProvenance}" Margin="8,0,4,0"
                                                  VerticalAlignment="Center"
                                                  AutomationProperties.Name="{svc:Loc Key=RuleAnalysis_GroupProvenanceName}" />
                                        <TextBox Width="260" Text="{Binding AnalysisFilter, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=RuleAnalysis_SearchHint}"
                                                 AutomationProperties.Name="{svc:Loc Key=RuleAnalysis_SearchName}"
                                                 ToolTip="{svc:Loc Key=RuleAnalysis_SearchTip}" />
                                    </DockPanel>
                                    <DockPanel Margin="0,6,0,4">
                                        <TextBlock DockPanel.Dock="Right" Text="{Binding AnalysisContext}"
                                                   Style="{StaticResource Hg.DimText}" />
                                        <TextBlock Text="{Binding AnalysisStatus}" Style="{StaticResource Hg.DimText}"
                                                   AutomationProperties.LiveSetting="Polite" />
                                    </DockPanel>
                                    <TextBlock Text="{svc:Loc Key=RuleAnalysis_Disclaimer}"
                                               Style="{StaticResource Hg.HelperText}" Margin="0,0,0,5"
                                               TextWrapping="Wrap" />
                                    <Grid>
                                        <DataGrid ItemsSource="{Binding AnalysisView}" MaxHeight="220"
                                                  IsReadOnly="True" CanUserSortColumns="True"
                                                  AutomationProperties.Name="{svc:Loc Key=RuleAnalysis_TableName}">
                                            <DataGrid.GroupStyle>
                                                <GroupStyle>
                                                    <GroupStyle.ContainerStyle>
                                                        <Style TargetType="{x:Type GroupItem}">
                                                            <Setter Property="Template">
                                                                <Setter.Value>
                                                                    <ControlTemplate TargetType="{x:Type GroupItem}">
                                                                        <Expander IsExpanded="True" Margin="0,2,0,0">
                                                                            <Expander.Header>
                                                                                <StackPanel Orientation="Horizontal">
                                                                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                                                                                    <TextBlock Text="{Binding ItemCount, StringFormat=' ({0})'}"
                                                                                               Style="{StaticResource Hg.DimText}" />
                                                                                </StackPanel>
                                                                            </Expander.Header>
                                                                            <ItemsPresenter />
                                                                        </Expander>
                                                                    </ControlTemplate>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </Style>
                                                    </GroupStyle.ContainerStyle>
                                                </GroupStyle>
                                            </DataGrid.GroupStyle>
                                            <DataGrid.Columns>
                                                <DataGridTemplateColumn Header="{svc:Loc Key=RuleAnalysis_Select}" Width="58">
                                                    <DataGridTemplateColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <CheckBox IsChecked="{Binding Selected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                      IsEnabled="{Binding CleanupEligible}"
                                                                      ToolTip="{Binding SelectionHelp}"
                                                                      AutomationProperties.Name="{svc:Loc Key=RuleAnalysis_SelectName}" />
                                                        </DataTemplate>
                                                    </DataGridTemplateColumn.CellTemplate>
                                                </DataGridTemplateColumn>
                                                <DataGridTextColumn Header="{svc:Loc Key=RuleAnalysis_Kind}" Binding="{Binding Kind}" Width="124" />
                                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_Name_709a2322}" Binding="{Binding RuleName}" Width="1.2*" />
                                                <DataGridTextColumn Header="{svc:Loc Key=RuleAnalysis_Related}" Binding="{Binding RelatedRuleName}" Width="1.1*" />
                                                <DataGridTextColumn Header="{svc:Loc Key=Xaml_Origin_44ab85a2}" Binding="{Binding Provenance}" Width="104" />
                                                <DataGridTextColumn Header="{svc:Loc Key=ListenerExposure_Reason}" Binding="{Binding Reason}" Width="1.6*">
                                                    <DataGridTextColumn.ElementStyle>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                                                            <Setter Property="ToolTip" Value="{Binding Reason}" />
                                                        </Style>
                                                    </DataGridTextColumn.ElementStyle>
                                                </DataGridTextColumn>
                                                <DataGridTextColumn Header="{svc:Loc Key=RuleAnalysis_Remediation}" Binding="{Binding Remediation}" Width="116" />
                                            </DataGrid.Columns>
                                        </DataGrid>
                                        <Border Visibility="{Binding AnalysisFindings.Count, Converter={StaticResource EmptyStateVis}}"
                                                Style="{StaticResource Hg.EmptyState}" IsHitTestVisible="False">
                                            <TextBlock Text="{svc:Loc Key=RuleAnalysis_Empty}" Style="{StaticResource Hg.EmptyText}" />
                                        </Border>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </Expander>

                        <TextBlock Grid.Row="5" Margin="2,6,0,0"
                                   Text="{Binding StatusText}"
                                   Style="{StaticResource Hg.DimText}"
                                   AutomationProperties.LiveSetting="Polite" />
                    </Grid>
                </TabItem>

                <TabItem>
                    <TabItem.Resources>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                            <Setter Property="TextWrapping" Value="Wrap" />
                        </Style>
                        <DataTemplate x:Key="ToolsWrappedContent">
                            <TextBlock Text="{Binding}" TextWrapping="Wrap" TextAlignment="Center" />
                        </DataTemplate>
                        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                            <Setter Property="ContentTemplate" Value="{StaticResource ToolsWrappedContent}" />
                        </Style>
                        <Style TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
                            <Setter Property="ContentTemplate" Value="{StaticResource ToolsWrappedContent}" />
                        </Style>
                        <Style TargetType="ComboBoxItem" BasedOn="{StaticResource {x:Type ComboBoxItem}}">
                            <Setter Property="ContentTemplate" Value="{StaticResource ToolsWrappedContent}" />
                        </Style>
                    </TabItem.Resources>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <Path Style="{StaticResource Hg.MainTabIconPath}"
                                  Data="M 8 2 V 5 M 8 11 V 14 M 2 8 H 5 M 11 8 H 14 M 4.5 4.5 L 6.5 6.5 M 9.5 9.5 L 11.5 11.5 M 11.5 4.5 L 9.5 6.5 M 6.5 9.5 L 4.5 11.5"
                                  Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=TabItem}}" />
                            <TextBlock Text="{svc:Loc Key=Tab_Tools, Default=Tools}" />
                        </StackPanel>
                    </TabItem.Header>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="ToolsSurface" Margin="8" DataContext="{Binding Tools}">

                            <!-- Service-health glance (NET-186) -->
                            <Border Style="{StaticResource Hg.Card}" Margin="0,0,0,10">
                                <StackPanel>
                                    <DockPanel LastChildFill="False">
                                        <TextBlock DockPanel.Dock="Left"
                                                   Text="{svc:Loc Key=Health_Title}"
                                                   FontWeight="SemiBold"
                                                   Margin="0,0,0,4" />
                                        <Button DockPanel.Dock="Right"
                                                Content="{svc:Loc Key=Xaml_Refresh_56e3badc}"
                                                Command="{Binding LoadHealthCommand}"
                                                Padding="10,2" />
                                    </DockPanel>
                                    <TextBlock Text="{svc:Loc Key=Health_Help}"
                                               Style="{StaticResource Hg.HelperText}"
                                               Margin="0,0,0,8"
                                               TextWrapping="Wrap" />
                                    <DataGrid ItemsSource="{Binding HealthRows}"
                                              AutoGenerateColumns="False"
                                              IsReadOnly="True"
                                              CanUserAddRows="False"
                                              HeadersVisibility="Column"
                                              MaxHeight="230"
                                              AutomationProperties.Name="{svc:Loc Key=Health_TableName}">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="{svc:Loc Key=Health_ColAspect}" Binding="{Binding Aspect}" Width="1.2*" />
                                            <DataGridTextColumn Header="{svc:Loc Key=Health_ColState}" Binding="{Binding State}" Width="110">
                                                <DataGridTextColumn.ElementStyle>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="{DynamicResource Hg.Green}" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Healthy}" Value="False">
                                                                <Setter Property="Foreground" Value="{DynamicResource Hg.Peach}" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </DataGridTextColumn.ElementStyle>
                                            </DataGridTextColumn>
                                            <DataGridTextColumn Header="{svc:Loc Key=Health_ColDetail}" Binding="{Binding Detail}" Width="2.4*" />
                                        </DataGrid.Columns>
                                    </DataGrid>
                                    <Border Style="{StaticResource Hg.EmptyState}"
                                            Margin="0,8,0,0"
                                            Visibility="{Binding HealthRows.Count, Converter={StaticResource EmptyStateVis}}">
                                        <TextBlock Text="{svc:Loc Key=Health_Empty}"
                                                   Style="{StaticResource Hg.EmptyText}" />
                                    </Border>
                                    <Border Style="{StaticResource Hg.StatusStrip}" Margin="0,8,0,0">
                                        <TextBlock Text="{Binding HealthStatusText}"
                                                   Style="{StaticResource Hg.SubText}"
                                                   TextWrapping="Wrap"
                                                   AutomationProperties.LiveSetting="Polite" />
                                    </Border>
                                </StackPanel>
                            </Border>

                            <!-- Proxy/PAC tamper baseline -->
                            <Border Style="{StaticResource Hg.Card}" Margin="0,0,0,10">
                                <StackPanel>
                                    <DockPanel LastChildFill="False">
                                        <TextBlock DockPanel.Dock="Left"
                                                   Text="{svc:Loc Key=Proxy_Title}"
                                                   FontWeight="SemiBold" />
                                        <Button DockPanel.Dock="Right"
                                                Content="{svc:Loc Key=Proxy_Accept}"
                                                Command="{Binding AcceptProxyBaselineCommand}"
                                                AutomationProperties.Name="{svc:Loc Key=Proxy_AcceptName}"
                                                ToolTip="{svc:Loc Key=Proxy_AcceptTip}"
                                                Margin="8,0,0,0"
                                                Padding="10,2" />
                                        <Button DockPanel.Dock="Right"
                                                Content="{svc:Loc Key=Proxy_Check}"
                                                Command="{Binding InspectProxyBaselineCommand}"
                                                AutomationProperties.Name="{svc:Loc Key=Proxy_CheckName}"
                                                Padding="10,2" />
                                    </DockPanel>
                                    <TextBlock Text="{svc:Loc Key=Proxy_Help}"
                                               Style="{StaticResource Hg.HelperText}"
                                               Margin="0,4,0,8"
                                               TextWrapping="Wrap" />
                                    <DataGrid ItemsSource="{Binding ProxyBaselineRows}"
                                              AutoGenerateColumns="False"
                                              IsReadOnly="True"
                                              CanUserAddRows="False"
                                              CanUserDeleteRows="False"
                                              HeadersVisibility="Column"
                                              MaxHeight="190"
                                              AutomationProperties.Name="{svc:Loc Key=Proxy_TableName}">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="{svc:Loc Key=Proxy_ColScope}" Binding="{Binding Scope}" Width="88" />
                                            <DataGridTextColumn Header="{svc:Loc Key=Proxy_ColIdentity}" Binding="{Binding Identity}" Width="1.1*" />
                                            <DataGridTextColumn Header="{svc:Loc Key=Proxy_ColSetting}" Binding="{Binding Setting}" Width="1.1*" />
                                            <DataGridTextColumn Header="{svc:Loc Key=Proxy_ColState}" Binding="{Binding State}" Width="82" />
                                            <DataGridTextColumn Header="{svc:Loc Key=Proxy_ColBaseline}" Binding="{Binding Baseline}" Width="1.4*" />
                                            <DataGridTextColumn Header="{svc:Loc Key=Proxy_ColCurrent}" Binding="{Binding Current}" Width="1.4*" />
                                        </DataGrid.Columns>
                                    </DataGrid>
                                    <Border Style="{StaticResource Hg.EmptyState}"
                                            Margin="0,8,0,0"
                                            Visibility="{Binding ProxyBaselineRows.Count, Converter={StaticResource EmptyStateVis}}">
                                        <TextBlock Text="{svc:Loc Key=Proxy_Empty}" Style="{StaticResource Hg.EmptyText}" />
                                    </Border>
                                    <Border Style="{StaticResource Hg.StatusStrip}" Margin="0,8,0,0">
                                        <TextBlock Text="{Binding ProxyBaselineStatus}"
                                                   Style="{StaticResource Hg.SubText}"
                                                   TextWrapping="Wrap"
                                                   AutomationProperties.LiveSetting="Polite" />
                                    </Border>
                                </StackPanel>
                            </Border>

                            <!-- SHA-256-verified self-update (NET-187) -->
                            <Border Style="{StaticResource Hg.Card}" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="{svc:Loc Key=Update_Title}" FontWeight="SemiBold" Margin="0,0,0,4" />
                                    <TextBlock Text="{svc:Loc Key=Update_Help}"
                                               Style="{StaticResource Hg.HelperText}"
                                               Margin="0,0,0,8"
                                               TextWrapping="Wrap" />
                                    <WrapPanel>
                                        <Button Content="{svc:Loc Key=Update_Check}"
                                                Command="{Binding CheckUpdateCommand}"
                                                Margin="0,0,8,8" />
                                        <Button Style="{StaticResource Hg.AccentButton}"
                                                Command="{Binding StageUpdateCommand}"
                                                ToolTip="{svc:Loc Key=Update_StageTip}"
                                                Margin="0,0,8,8">
                                            <TextBlock Text="{svc:Loc Key=Update_Stage}" TextWrapping="Wrap" TextAlignment="Center" />
                                        </Button>
                                    </WrapPanel>
                                    <Border Style="{StaticResource Hg.StatusStrip}">
                                        <TextBlock Text="{Binding UpdateStatusText}"
                                                   Style="{StaticResource Hg.SubText}"
                                                   TextWrapping="Wrap"
                                                   AutomationProperties.LiveSetting="Polite" />
                                    </Border>
                                </StackPanel>
                            </Border>

                            <!-- Settings lock (NET-079) -->
                            <Border Style="{StaticResource Hg.Card}" Margin="0,0,0,10">
                                <StackPanel>
                                    <DockPanel LastChildFill="False">
                                        <TextBlock DockPanel.Dock="Left" Text="{svc:Loc Key=Xaml_Settings_lock_424ef42a}" FontWeight="SemiBold" />
                                        <TextBlock DockPanel.Dock="Right" Text="{Binding LockStatus}" Style="{StaticResource Hg.DimText}" />
                                    </DockPanel>
                                    <TextBlock Text="{svc:Loc Key=Xaml_When_locked_changing_filtering_mode_firewall_234ed002}"
                                               Style="{StaticResource Hg.DimText}" TextWrapping="Wrap" Margin="0,4,0,8" />
                                    <TextBlock Text="{svc:Loc Key=Xaml_Lock_password_4cd91ac2}" Style="{StaticResource Hg.FieldLabel}" />
                                    <DockPanel LastChildFill="True">
                                        <Button DockPanel.Dock="Right" Content="{svc:Loc Key=Xaml_Refresh_56e3badc}" Command="{Binding LoadLockStateCommand}" Margin="6,0,0,0" Padding="10,2" />
                                        <Button DockPanel.Dock="Right" Content="{svc:Loc Key=Xaml_Protect_hosts_file_41db1809}" Command="{Binding ProtectHostsCommand}" Margin="6,0,0,0" Padding="10,2"
                                                ToolTip="{svc:Loc Key=Xaml_Enforce_a_SYSTEM_Admins_only_DACL_680f041b}" />
                                        <PasswordBox x:Name="LockPasswordBox" VerticalAlignment="Center"
                                                     Style="{StaticResource Hg.LockPasswordBox}"
                                                     svc:PasswordBoxHelper.Watermark="Enter lock password"
                                                     AutomationProperties.Name="{svc:Loc Key=Xaml_Settings_lock_password_c745c4ae}" />
                                    </DockPanel>
                                    <DockPanel LastChildFill="False" Margin="0,8,0,0">
                                        <Button DockPanel.Dock="Left" Content="{svc:Loc Key=Xaml_Arm_lock_82cc9364}" Command="{Binding EnableLockCommand}" Margin="0,0,6,0" Padding="10,2"
                                                CommandParameter="{Binding ElementName=LockPasswordBox}" Click="OnLockPasswordSync"
                                                ToolTip="{svc:Loc Key=Xaml_Set_the_password_that_protects_filtering_1de59e66}" />
                                        <Button DockPanel.Dock="Left" Content="{svc:Loc Key=Xaml_Disarm_f26d27b0}" Command="{Binding DisableLockCommand}" Margin="0,0,6,0" Padding="10,2"
                                                Click="OnLockPasswordSync"
                                                ToolTip="{svc:Loc Key=Xaml_Remove_the_settings_lock_after_entering_3792232c}" />
                                        <Button DockPanel.Dock="Left" Content="{svc:Loc Key=Xaml_Unlock_1526a17e}" Command="{Binding UnlockCommand}" Margin="0,0,6,0" Padding="10,2"
                                                Click="OnLockPasswordSync"
                                                ToolTip="{svc:Loc Key=Xaml_Temporarily_unlock_protected_actions_for_the_5440dfe8}" />
                                        <TextBox DockPanel.Dock="Left" Width="46" VerticalAlignment="Center"
                                                 Text="{Binding UnlockMinutes, UpdateSourceTrigger=PropertyChanged}"
                                                 AutomationProperties.Name="{svc:Loc Key=Xaml_Unlock_minutes_12cfd14d}" ToolTip="{svc:Loc Key=Xaml_Timed_unlock_window_in_minutes_0caa0a13}" />
                                        <TextBlock DockPanel.Dock="Left" Text="{svc:Loc Key=Xaml_min_b6c935d4}" Style="{StaticResource Hg.DimText}"
                                                   VerticalAlignment="Center" Margin="5,0,0,0" />
                                    </DockPanel>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource Hg.Card}" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="{svc:Loc Key=Xaml_DNS_3ddc21b1}" FontWeight="SemiBold" Margin="0,0,0,8" />
                                    <TextBlock Text="{svc:Loc Key=Xaml_Flush_resolver_state_switch_Windows_DNS_dbece2f0}"
                                               Style="{StaticResource Hg.HelperText}" Margin="0,0,0,10" />
                                    <DockPanel LastChildFill="False">
                                        <Button DockPanel.Dock="Left" Content="{svc:Loc Key=Xaml_Flush_DNS_cache_58856a23}"
                                                Command="{Binding FlushDnsCommand}" Margin="0,0,10,0" />
                                        <ComboBox DockPanel.Dock="Left" Width="180" Margin="0,0,10,0"
                                                  ItemsSource="{x:Static vm:ToolsViewModel.ResolverNames}"
                                                  ItemTemplate="{StaticResource ToolsWrappedContent}"
                                                  AutomationProperties.Name="{svc:Loc Key=Xaml_DNS_resolver_preset_df5e62bd}"
                                                  SelectedItem="{Binding SelectedResolver}" />
                                        <Button DockPanel.Dock="Left" Content="{svc:Loc Key=Xaml_Apply_resolver_2382d7f6}"
                                                Command="{Binding ApplyResolverCommand}" />
                                    </DockPanel>
                                    <DockPanel Margin="0,10,0,0">
                                        <Button DockPanel.Dock="Right" Content="{svc:Loc Key=Xaml_Refresh_56e3badc}"
                                                Command="{Binding LoadDnsAdaptersCommand}"
                                                AutomationProperties.Name="{svc:Loc Key=Xaml_Refresh_56e3badc}"
                                                Padding="10,2" />
                                        <TextBlock Text="{svc:Loc Key=DnsAdapters_Help}"
                                                   Style="{StaticResource Hg.HelperText}"
                                                   TextWrapping="Wrap" VerticalAlignment="Center" />
                                    </DockPanel>
                                    <ItemsControl ItemsSource="{Binding DnsAdapters}" Margin="0,6,0,0">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <CheckBox IsChecked="{Binding IsSelected}"
                                                          Content="{Binding Label}"
                                                          AutomationProperties.Name="{Binding Label}"
                                                          Margin="0,3,0,0" />
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <Border Style="{StaticResource Hg.StatusStrip}" Margin="0,10,0,0">
                                        <StackPanel>
                                            <DockPanel LastChildFill="True">
                                                <Button DockPanel.Dock="Right"
                                                        Content="{svc:Loc Key=ResolverHealth_Run}"
                                                        Command="{Binding RunResolverHealthCommand}"
                                                        AutomationProperties.Name="{svc:Loc Key=ResolverHealth_RunName}"
                                                        ToolTip="{svc:Loc Key=ResolverHealth_RunTip}"
                                                        Margin="8,0,0,0"
                                                        Padding="10,2" />
                                                <Button DockPanel.Dock="Right"
                                                        Content="{svc:Loc Key=ResolverHealth_Load}"
                                                        Command="{Binding LoadResolverHealthCommand}"
                                                        AutomationProperties.Name="{svc:Loc Key=ResolverHealth_LoadName}"
                                                        ToolTip="{svc:Loc Key=ResolverHealth_LoadTip}"
                                                        Margin="8,0,0,0"
                                                        Padding="10,2" />
                                                <StackPanel>
                                                    <TextBlock Text="{svc:Loc Key=ResolverHealth_Title}" FontWeight="SemiBold" />
                                                    <TextBlock Text="{svc:Loc Key=ResolverHealth_Help}"
                                                               Style="{StaticResource Hg.HelperText}"
                                                               TextWrapping="Wrap" />
                                                </StackPanel>
                                            </DockPanel>
                                            <TextBox Text="{Binding ResolverHealthHost, UpdateSourceTrigger=PropertyChanged}"
                                                     Style="{StaticResource Hg.HintBox}"
                                                     Tag="{svc:Loc Key=ResolverHealth_HostHint}"
                                                     AutomationProperties.Name="{svc:Loc Key=ResolverHealth_HostName}"
                                                     ToolTip="{svc:Loc Key=ResolverHealth_HostTip}"
                                                     Margin="0,8,0,6" />
                                            <DockPanel LastChildFill="False" Margin="0,0,0,6">
                                                <CheckBox DockPanel.Dock="Left"
                                                          IsChecked="{Binding ResolverHealthScheduleEnabled}"
                                                          Content="{svc:Loc Key=ResolverHealth_ScheduleEnabled}"
                                                          AutomationProperties.Name="{svc:Loc Key=ResolverHealth_ScheduleEnabledName}"
                                                          VerticalAlignment="Center"
                                                          Margin="0,0,8,0" />
                                                <TextBox DockPanel.Dock="Left"
                                                         Width="58"
                                                         Text="{Binding ResolverHealthScheduleIntervalMinutes, UpdateSourceTrigger=PropertyChanged}"
                                                         AutomationProperties.Name="{svc:Loc Key=ResolverHealth_ScheduleIntervalName}"
                                                         ToolTip="{svc:Loc Key=ResolverHealth_ScheduleIntervalTip}"
                                                         VerticalAlignment="Center" />
                                                <TextBlock DockPanel.Dock="Left"
                                                           Text="{svc:Loc Key=ResolverHealth_Minutes}"
                                                           Style="{StaticResource Hg.DimText}"
                                                           VerticalAlignment="Center"
                                                           Margin="5,0,8,0" />
                                                <Button DockPanel.Dock="Left"
                                                        Content="{svc:Loc Key=ResolverHealth_ApplySchedule}"
                                                        Command="{Binding ApplyResolverHealthScheduleCommand}"
                                                        AutomationProperties.Name="{svc:Loc Key=ResolverHealth_ApplyScheduleName}"
                                                        ToolTip="{svc:Loc Key=ResolverHealth_ApplyScheduleTip}"
                                                        Padding="10,2" />
                                            </DockPanel>
                                            <DataGrid ItemsSource="{Binding ResolverHealthEntries}"
                                                      AutoGenerateColumns="False"
                                                      IsReadOnly="True"
                                                      CanUserAddRows="False"
                                                      CanUserDeleteRows="False"
                                                      HeadersVisibility="Column"
                                                      MaxHeight="190"
                                                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                                      AutomationProperties.Name="{svc:Loc Key=ResolverHealth_TableName}">
                                                <DataGrid.Columns>
                                                    <DataGridTextColumn Header="{svc:Loc Key=ResolverHealth_Adapter}" Binding="{Binding Adapter}" Width="150" />
                                                    <DataGridTextColumn Header="{svc:Loc Key=ResolverHealth_Endpoint}" Binding="{Binding Endpoint}" Width="145" />
                                                    <DataGridTextColumn Header="{svc:Loc Key=ResolverHealth_Protocol}" Binding="{Binding Protocol}" Width="76" />
                                                    <DataGridTextColumn Header="{svc:Loc Key=ResolverHealth_A}" Binding="{Binding AResult}" Width="96" />
                                                    <DataGridTextColumn Header="{svc:Loc Key=ResolverHealth_Aaaa}" Binding="{Binding AaaaResult}" Width="96" />
                                                    <DataGridTextColumn Header="{svc:Loc Key=ResolverHealth_Rtt}" Binding="{Binding RttText}" Width="78" />
                                                    <DataGridTextColumn Header="{svc:Loc Key=ResolverHealth_Tls}" Binding="{Binding TlsStatus}" Width="110" />
                                                    <DataGridTextColumn Header="{svc:Loc Key=ResolverHealth_Certificate}" Binding="{Binding CertificateStatus}" Width="120" />
                                                    <DataGridTextColumn Header="{svc:Loc Key=ResolverHealth_Result}" Binding="{Binding ResultText}" Width="220" />
                                                </DataGrid.Columns>
                                            </DataGrid>
                                            <TextBlock Text="{Binding ResolverHealthStatusText}"
                                                       Style="{StaticResource Hg.DimText}"
                                                       TextWrapping="Wrap"
                                                       Margin="0,6,0,0"
                                                       AutomationProperties.Name="{Binding ResolverHealthStatusText}"
                                                       AutomationProperties.LiveSetting="Polite" />
                                        </StackPanel>
                                    </Border>
                                    <DockPanel Margin="0,10,0,0">
                                        <Button DockPanel.Dock="Right" Content="{svc:Loc Key=Xaml_Inspect_18ca87af}"
                                                Command="{Binding InspectCommand}" Margin="8,0,0,0"
                                                ToolTip="{svc:Loc Key=Xaml_Enter_a_domain_to_resolve_and_bdf20a48}" />
                                        <TextBox Text="{Binding InspectDomain, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=Xaml_InspectDomainHint}"
                                                 AutomationProperties.Name="{svc:Loc Key=Xaml_Domain_to_inspect_ea69d8b4}"
                                                 ToolTip="{svc:Loc Key=Xaml_Domain_to_resolve_and_check_block_be2f0bc0}" />
                                    </DockPanel>
                                    <TextBlock Text="{Binding InspectResult}" Style="{StaticResource Hg.SubText}" Margin="0,6,0,0"
                                               TextWrapping="Wrap" />
                                    <TextBlock Text="{svc:Loc Key=ServiceBinding_Title}" FontWeight="SemiBold" Margin="0,10,0,0" />
                                    <TextBlock Text="{Binding ServiceBindingStatusText}"
                                               Style="{StaticResource Hg.HelperText}"
                                               TextWrapping="Wrap"
                                               Margin="0,2,0,4"
                                               AutomationProperties.LiveSetting="Polite" />
                                    <DataGrid ItemsSource="{Binding ServiceBindings}"
                                              AutoGenerateColumns="False"
                                              IsReadOnly="True"
                                              CanUserAddRows="False"
                                              CanUserDeleteRows="False"
                                              HeadersVisibility="Column"
                                              MaxHeight="154"
                                              ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                              AutomationProperties.Name="{svc:Loc Key=ServiceBinding_TableName}">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="{svc:Loc Key=ServiceBinding_Owner}" Binding="{Binding OwnerName}" Width="180" />
                                            <DataGridTextColumn Header="{svc:Loc Key=ServiceBinding_Type}" Binding="{Binding DnsType}" Width="68" />
                                            <DataGridTextColumn Header="{svc:Loc Key=ServiceBinding_Priority}" Binding="{Binding Priority}" Width="72" />
                                            <DataGridTextColumn Header="{svc:Loc Key=ServiceBinding_Target}" Binding="{Binding Target}" Width="180" />
                                            <DataGridTextColumn Header="{svc:Loc Key=ServiceBinding_Mode}" Binding="{Binding ModeText}" Width="76" />
                                            <DataGridTextColumn Header="{svc:Loc Key=ServiceBinding_Parameters}" Binding="{Binding ParametersText}" Width="320" />
                                            <DataGridTextColumn Header="{svc:Loc Key=ServiceBinding_Ech}" Binding="{Binding EchText}" Width="104" />
                                            <DataGridTextColumn Header="{svc:Loc Key=ServiceBinding_Ttl}" Binding="{Binding TtlSeconds}" Width="68" />
                                            <DataGridTextColumn Header="{svc:Loc Key=ServiceBinding_Health}" Binding="{Binding HealthText}" Width="240" />
                                        </DataGrid.Columns>
                                    </DataGrid>

                                    <Border Style="{StaticResource Hg.StatusStrip}" Margin="0,10,0,0">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>
                                            <DockPanel Grid.Row="0" LastChildFill="True">
                                                <Button DockPanel.Dock="Right"
                                                        Content="{svc:Loc Key=DnsCache_FlushSelected}"
                                                        Command="{Binding FlushDnsCacheEntryCommand}"
                                                        AutomationProperties.Name="{svc:Loc Key=DnsCache_FlushSelectedName}"
                                                        ToolTip="{svc:Loc Key=DnsCache_FlushSelectedTip}"
                                                        Padding="10,2" />
                                                <Button DockPanel.Dock="Right"
                                                        Content="{svc:Loc Key=DnsCache_Load}"
                                                        Command="{Binding LoadDnsCacheCommand}"
                                                        AutomationProperties.Name="{svc:Loc Key=DnsCache_LoadName}"
                                                        ToolTip="{svc:Loc Key=DnsCache_LoadTip}"
                                                        Margin="0,0,8,0"
                                                        Padding="10,2" />
                                                <StackPanel>
                                                    <TextBlock Text="{svc:Loc Key=DnsCache_Title}" FontWeight="SemiBold" />
                                                    <TextBlock Text="{svc:Loc Key=DnsCache_Help}"
                                                               Style="{StaticResource Hg.HelperText}"
                                                               Margin="0,2,12,0"
                                                               TextWrapping="Wrap" />
                                                </StackPanel>
                                            </DockPanel>
                                            <Grid Grid.Row="1" Margin="0,8,0,6">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="84" />
                                                </Grid.ColumnDefinitions>
                                                <TextBox Grid.Column="0"
                                                         Text="{Binding DnsCacheSearch, UpdateSourceTrigger=PropertyChanged}"
                                                         Style="{StaticResource Hg.HintBox}"
                                                         Tag="{svc:Loc Key=DnsCache_SearchHint}"
                                                         AutomationProperties.Name="{svc:Loc Key=DnsCache_SearchName}"
                                                         ToolTip="{svc:Loc Key=DnsCache_SearchTip}" />
                                                <TextBox Grid.Column="1"
                                                         Margin="8,0,0,0"
                                                         Text="{Binding DnsCacheLimit, UpdateSourceTrigger=PropertyChanged}"
                                                         AutomationProperties.Name="{svc:Loc Key=DnsCache_LimitName}"
                                                         ToolTip="{svc:Loc Key=DnsCache_LimitTip}" />
                                            </Grid>
                                            <DataGrid Grid.Row="2"
                                                      ItemsSource="{Binding DnsCacheEntries}"
                                                      SelectedItem="{Binding SelectedDnsCacheEntry}"
                                                      AutoGenerateColumns="False"
                                                      IsReadOnly="True"
                                                      CanUserAddRows="False"
                                                      CanUserDeleteRows="False"
                                                      HeadersVisibility="Column"
                                                      SelectionMode="Single"
                                                      MaxHeight="178"
                                                      AutomationProperties.Name="{svc:Loc Key=DnsCache_TableName}">
                                                <DataGrid.Columns>
                                                    <DataGridTextColumn Header="{svc:Loc Key=DnsCache_ColumnName}" Binding="{Binding Name}" Width="1.3*" />
                                                    <DataGridTextColumn Header="{svc:Loc Key=DnsCache_ColumnType}" Binding="{Binding Type}" Width="72" />
                                                    <DataGridTextColumn Header="{svc:Loc Key=DnsCache_ColumnPrivacyRole}" Binding="{Binding PrivacyRole}" Width="1.2*" />
                                                    <DataGridTextColumn Header="{svc:Loc Key=DnsCache_ColumnDataLength}" Binding="{Binding DataLengthText}" Width="82" />
                                                    <DataGridTextColumn Header="{svc:Loc Key=DnsCache_ColumnFlags}" Binding="{Binding FlagsText}" Width="92" />
                                                </DataGrid.Columns>
                                            </DataGrid>
                                            <TextBlock Grid.Row="3"
                                                       Text="{Binding DnsCacheStatusText}"
                                                       Style="{StaticResource Hg.DimText}"
                                                       Margin="0,6,0,0"
                                                       AutomationProperties.LiveSetting="Polite" />
                                        </Grid>
                                    </Border>

                                    <Separator Margin="0,10" Background="{DynamicResource Hg.S0}" />
                                    <TextBlock Text="{svc:Loc Key=Xaml_Close_DNS_bypass_b36af939}" FontWeight="SemiBold" Margin="0,0,0,2" />
                                    <TextBlock TextWrapping="Wrap" Style="{StaticResource Hg.HelperText}" Margin="0,0,0,6"
                                               Text="{svc:Loc Key=Xaml_Browsers_using_their_own_Secure_DNS_97632c3b}" />
                                    <DockPanel LastChildFill="False" Margin="0,0,0,6">
                                        <Button DockPanel.Dock="Left"
                                                Command="{Binding ToggleSeeEverythingCommand}"
                                                AutomationProperties.Name="{svc:Loc Key=Xaml_See_everything_â_force_browser_DNS_11a79a3d}"
                                                ToolTip="{svc:Loc Key=Xaml_One_click_block_QUIC_UDP_443_edd53016}">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource Hg.AccentButton}">
                                                    <Setter Property="Content" Value="{svc:Loc Key=Xaml_SeeEverythingOffButton}" />
                                                    <Setter Property="ContentTemplate" Value="{StaticResource ToolsWrappedContent}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding SeeEverythingActive}" Value="True">
                                                            <Setter Property="Content" Value="{svc:Loc Key=Xaml_SeeEverythingOnButton}" />
                                                            <Setter Property="Background" Value="{DynamicResource Hg.Danger2}" />
                                                            <Setter Property="Foreground" Value="{DynamicResource Hg.OnDanger}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </DockPanel>
                                    <DockPanel LastChildFill="False">
                                        <Button DockPanel.Dock="Left" Margin="0,0,10,0"
                                                Command="{Binding ToggleEncryptedDnsCommand}"
                                                ToolTip="{svc:Loc Key=Xaml_Firewall_block_known_DoH_resolver_IPs_d179fd3d}">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                                    <Setter Property="Content" Value="{svc:Loc Key=Xaml_BlockEncryptedDnsButton}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding DohBlockingActive}" Value="True">
                                                            <Setter Property="Content" Value="{svc:Loc Key=Xaml_UnblockEncryptedDnsButton}" />
                                                            <Setter Property="Background" Value="{DynamicResource Hg.Danger2}" />
                                                            <Setter Property="Foreground" Value="{DynamicResource Hg.OnDanger}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                        <TextBox DockPanel.Dock="Left" Width="240" Margin="0,0,8,0"
                                                 Text="{Binding DohUrl, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=Xaml_DohUrlHint}"
                                                 AutomationProperties.Name="{svc:Loc Key=Xaml_DoH_intelligence_URL_04daa00f}"
                                                 ToolTip="{svc:Loc Key=Xaml_Optional_remote_resolver_list_URL_https_0024c563}" />
                                        <TextBox DockPanel.Dock="Left" Width="200" Margin="0,0,8,0"
                                                 Text="{Binding DohSha256, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=Xaml_DohSha256Hint}"
                                                 AutomationProperties.Name="{svc:Loc Key=Xaml_DoH_intelligence_SHA_256_24601ac7}"
                                                 ToolTip="{svc:Loc Key=Xaml_Expected_SHA_256_of_the_remote_5d820f42}" />
                                        <Button DockPanel.Dock="Left" Content="{svc:Loc Key=Xaml_Refresh_DoH_intelligence_b3df1a97}"
                                                Command="{Binding RefreshDohCommand}" />
                                    </DockPanel>
                                    <DockPanel LastChildFill="False" Margin="0,8,0,0">
                                        <Button DockPanel.Dock="Left" Margin="0,0,10,0"
                                                Command="{Binding ToggleQuicCommand}"
                                                ToolTip="{svc:Loc Key=Xaml_Block_outbound_QUIC_HTTP3_UDP_port_6ce0d298}">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                                    <Setter Property="Content" Value="{svc:Loc Key=Xaml_BlockQuicButton}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding QuicBlockingActive}" Value="True">
                                                            <Setter Property="Content" Value="{svc:Loc Key=Xaml_UnblockQuicButton}" />
                                                            <Setter Property="Background" Value="{DynamicResource Hg.Danger2}" />
                                                            <Setter Property="Foreground" Value="{DynamicResource Hg.OnDanger}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                        <TextBlock DockPanel.Dock="Left" VerticalAlignment="Center"
                                                   Style="{StaticResource Hg.DimText}"
                                                   TextWrapping="Wrap"
                                                   Text="{svc:Loc Key=Xaml_Forces_TCP_fallback_so_DoH3_QUIC_1377802e}" />
                                    </DockPanel>
                                    <DockPanel LastChildFill="False" Margin="0,8,0,0">
                                        <Button DockPanel.Dock="Left" Margin="0,0,10,0"
                                                Command="{Binding ToggleCnameCloakCommand}"
                                                ToolTip="{svc:Loc Key=Xaml_CNAME_cloak_defense_when_a_first_6bce35c8}">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                                    <Setter Property="Content" Value="{svc:Loc Key=Xaml_ArmCnameCloakButton}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding CnameCloakActive}" Value="True">
                                                            <Setter Property="Content" Value="{svc:Loc Key=Xaml_DisarmCnameCloakButton}" />
                                                            <Setter Property="Background" Value="{DynamicResource Hg.Danger2}" />
                                                            <Setter Property="Foreground" Value="{DynamicResource Hg.OnDanger}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                        <TextBlock DockPanel.Dock="Left" VerticalAlignment="Center"
                                                   Style="{StaticResource Hg.DimText}"
                                                   TextWrapping="Wrap"
                                                   Text="{svc:Loc Key=Xaml_Blocks_first_party_hosts_that_alias_700a14d5}" />
                                    </DockPanel>
                                    <DockPanel LastChildFill="False" Margin="0,8,0,0">
                                        <Button DockPanel.Dock="Left" Margin="0,0,10,0"
                                                Command="{Binding ToggleIdnHomographCommand}"
                                                AutomationProperties.Name="{svc:Loc Key=IdnHomograph_ToggleName}"
                                                ToolTip="{svc:Loc Key=IdnHomograph_ToggleTip}">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                                    <Setter Property="Content" Value="{svc:Loc Key=IdnHomograph_EnableButton}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IdnHomographEnabled}" Value="True">
                                                            <Setter Property="Content" Value="{svc:Loc Key=IdnHomograph_DisableButton}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                        <StackPanel>
                                            <TextBlock Text="{svc:Loc Key=IdnHomograph_ToolsHelp}"
                                                       Style="{StaticResource Hg.DimText}" TextWrapping="Wrap" />
                                            <TextBlock Text="{Binding IdnHomographStatus}"
                                                       Style="{StaticResource Hg.SubText}" TextWrapping="Wrap"
                                                       Margin="0,3,0,0"
                                                       AutomationProperties.LiveSetting="Polite" />
                                        </StackPanel>
                                    </DockPanel>
                                    <DockPanel LastChildFill="False" Margin="0,8,0,0">
                                        <Button DockPanel.Dock="Left" Margin="0,0,10,0"
                                                Command="{Binding ToggleSniCaptureCommand}"
                                                ToolTip="{svc:Loc Key=Xaml_TLS_SNI_capture_NET_109_recover_f333e6fd}">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                                    <Setter Property="Content" Value="{svc:Loc Key=Xaml_CaptureSniButton}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding SniCaptureActive}" Value="True">
                                                            <Setter Property="Content" Value="{svc:Loc Key=Xaml_StopSniButton}" />
                                                            <Setter Property="Background" Value="{DynamicResource Hg.Danger2}" />
                                                            <Setter Property="Foreground" Value="{DynamicResource Hg.OnDanger}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                        <TextBlock DockPanel.Dock="Left" VerticalAlignment="Center"
                                                   Style="{StaticResource Hg.DimText}"
                                                   TextWrapping="Wrap"
                                                   Text="{svc:Loc Key=Xaml_Names_HTTPS_dials_the_OS_resolver_79149379}" />
                                    </DockPanel>
                                    <Border Style="{StaticResource Hg.StatusStrip}" Margin="0,8,0,0">
                                        <StackPanel>
                                            <TextBlock Text="{Binding DohStatusText}"
                                                       Style="{StaticResource Hg.SubText}"
                                                       TextWrapping="Wrap"
                                                       AutomationProperties.LiveSetting="Polite" />
                                            <TextBlock Text="{Binding EchPostureText}"
                                                       Style="{StaticResource Hg.DimText}"
                                                       TextWrapping="Wrap"
                                                       Margin="0,4,0,0"
                                                       AutomationProperties.LiveSetting="Polite" />
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource Hg.Card}" Margin="0,0,0,10">
                                <StackPanel>
                                    <DockPanel LastChildFill="False">
                                        <TextBlock DockPanel.Dock="Left"
                                                   Text="{svc:Loc Key=LanSurface_Title, Default=LAN attack surface}"
                                                   FontWeight="SemiBold" />
                                        <Button DockPanel.Dock="Right"
                                                Content="{svc:Loc Key=Xaml_Refresh_56e3badc}"
                                                Command="{Binding LoadLanAttackSurfaceCommand}"
                                                Padding="10,2" />
                                    </DockPanel>
                                    <TextBlock Text="{svc:Loc Key=LanSurface_Intro, Default=Reversible one-click controls for chatty LAN discovery and legacy name lookup plus auto-proxy and inbound file sharing.}"
                                               Style="{StaticResource Hg.HelperText}" Margin="0,4,0,8" TextWrapping="Wrap" />
                                    <ItemsControl ItemsSource="{Binding LanAttackSurface}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Style="{StaticResource Hg.StatusStrip}"
                                                        Width="335"
                                                        MinHeight="122"
                                                        Margin="0,0,8,8">
                                                    <Grid>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="*" />
                                                        </Grid.RowDefinitions>
                                                        <DockPanel Grid.Row="0" LastChildFill="True">
                                                            <Button DockPanel.Dock="Right"
                                                                    Content="{Binding ActionText}"
                                                                    Command="{Binding DataContext.ToggleLanAttackSurfaceCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                                    CommandParameter="{Binding}"
                                                                    AutomationProperties.Name="{Binding Label}">
                                                                <Button.Style>
                                                                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                                                        <Setter Property="Padding" Value="10,2" />
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding Blocked}" Value="True">
                                                                                <Setter Property="Background" Value="{DynamicResource Hg.Danger2}" />
                                                                                <Setter Property="Foreground" Value="{DynamicResource Hg.OnDanger}" />
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </Button.Style>
                                                            </Button>
                                                            <StackPanel>
                                                                <TextBlock Text="{Binding Label}"
                                                                           FontWeight="SemiBold"
                                                                           TextTrimming="CharacterEllipsis" />
                                                                <TextBlock Text="{Binding StateText}"
                                                                           Style="{StaticResource Hg.DimText}"
                                                                           Margin="0,2,0,0" />
                                                            </StackPanel>
                                                        </DockPanel>
                                                        <TextBlock Grid.Row="1"
                                                                   Text="{svc:Loc Key=LanSurface_Breaks, Default=What breaks}"
                                                                   Style="{StaticResource Hg.FieldLabel}"
                                                                   Margin="0,10,0,2" />
                                                        <TextBlock Grid.Row="2"
                                                                   Text="{Binding BreakNote}"
                                                                   Style="{StaticResource Hg.HelperText}"
                                                                   TextWrapping="Wrap" />
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource Hg.Card}" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="{svc:Loc Key=Xaml_Blocked_services_dea80c2c}" FontWeight="SemiBold" Margin="0,0,0,4" />
                                    <TextBlock Text="{svc:Loc Key=Xaml_One_click_hosts_blocking_of_a_d41913d9}"
                                               Style="{StaticResource Hg.DimText}" Margin="0,0,0,8" TextWrapping="Wrap" />
                                    <ItemsControl ItemsSource="{Binding Services}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Button Content="{Binding Label}" Margin="0,0,8,8" Padding="10,5"
                                                        ToolTip="{Binding Note}"
                                                        Command="{Binding DataContext.ToggleServiceCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                        CommandParameter="{Binding}">
                                                    <Button.Style>
                                                        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Blocked}" Value="True">
                                                                    <Setter Property="Background" Value="{DynamicResource Hg.Danger2}" />
                                                                    <Setter Property="Foreground" Value="{DynamicResource Hg.OnDanger}" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Button.Style>
                                                </Button>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>

                            <!-- Blocklist intelligence (reference index for candidate flagging) -->
                            <Border Style="{StaticResource Hg.Card}" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="{svc:Loc Key=Xaml_Blocklist_intelligence_d54b451c}" FontWeight="SemiBold" Margin="0,0,0,4" />
                                    <TextBlock Text="{svc:Loc Key=Xaml_Every_catalog_blocklist_is_downloaded_into_875a299b}"
                                               Style="{StaticResource Hg.HelperText}" Margin="0,0,0,8" />
                                    <DockPanel LastChildFill="True">
                                        <Button DockPanel.Dock="Right" Content="{svc:Loc Key=Xaml_Refresh_reference_lists_49c91c6d}"
                                                Command="{Binding RefreshIntelCommand}" Margin="8,0,0,0"
                                                ToolTip="{svc:Loc Key=Xaml_Re_download_every_catalog_list_into_7d99cc69}" />
                                        <Border Style="{StaticResource Hg.StatusStrip}">
                                            <TextBlock Text="{Binding IntelStatusText}"
                                                       Style="{StaticResource Hg.SubText}"
                                                       TextWrapping="Wrap"
                                                       AutomationProperties.LiveSetting="Polite" />
                                        </Border>
                                    </DockPanel>
                                </StackPanel>
                            </Border>

                            <!-- Manual-edit adoption (NET-188) -->
                            <Border Style="{StaticResource Hg.Card}" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="{svc:Loc Key=Xaml_Adopt_manual_hosts_edits, Default=Adopt manual hosts edits}" FontWeight="SemiBold" Margin="0,0,0,4" />
                                    <TextBlock Text="{svc:Loc Key=Xaml_Adopt_manual_helper}"
                                               Style="{StaticResource Hg.HelperText}" Margin="0,0,0,8" />
                                    <DockPanel LastChildFill="False">
                                        <CheckBox DockPanel.Dock="Left" Content="{svc:Loc Key=Xaml_Auto_adopt_hand_edits, Default=Auto-import hand edits on change}" VerticalAlignment="Center"
                                                  IsChecked="{Binding AdoptManualEdits}" Margin="0,0,12,0"
                                                  ToolTip="{svc:Loc Key=Xaml_Auto_adopt_tooltip}" />
                                        <Button DockPanel.Dock="Left" Content="{svc:Loc Key=Xaml_Save_adoption, Default=Save}"
                                                Command="{Binding SaveAdoptionConfigCommand}" Margin="0,0,12,0" />
                                        <Button DockPanel.Dock="Left" Content="{svc:Loc Key=Xaml_Adopt_and_organize_now, Default=Adopt and organize now}"
                                                Command="{Binding AdoptHostsNowCommand}"
                                                ToolTip="{svc:Loc Key=Xaml_Adopt_now_tooltip}" />
                                    </DockPanel>
                                    <Border Style="{StaticResource Hg.StatusStrip}" Margin="0,8,0,0">
                                        <TextBlock Text="{Binding AdoptionStatusText}"
                                                   Style="{StaticResource Hg.SubText}"
                                                   TextWrapping="Wrap"
                                                   AutomationProperties.LiveSetting="Polite" />
                                    </Border>
                                </StackPanel>
                            </Border>

                            <!-- AI domain categorization (DeepSeek) -->
                            <Border Style="{StaticResource Hg.Card}" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="{svc:Loc Key=Xaml_AI_categorization_DeepSeek_f61d8c50}" FontWeight="SemiBold" Margin="0,0,0,4" />
                                    <TextBlock Text="{svc:Loc Key=Xaml_Uses_the_DeepSeek_API_to_bucket_45490066}"
                                               Style="{StaticResource Hg.HelperText}" Margin="0,0,0,8" />
                                    <TextBlock Text="{svc:Loc Key=Xaml_DeepSeek_API_key_aef39533}" Style="{StaticResource Hg.FieldLabel}" />
                                    <DockPanel LastChildFill="True">
                                        <Button DockPanel.Dock="Right" Content="{svc:Loc Key=Xaml_Save_AI_settings_6ce9c264}"
                                                Style="{StaticResource Hg.AccentButton}"
                                                Command="{Binding SaveAiConfigCommand}" Click="OnAiKeySync"
                                                Margin="8,0,0,0"
                                                ToolTip="{svc:Loc Key=Xaml_Store_the_key_in_the_service_1493e7be}" />
                                        <TextBox DockPanel.Dock="Right" Width="150" Margin="8,0,0,0" VerticalAlignment="Center"
                                                 Text="{Binding AiModel, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}" Tag="{svc:Loc Key=Xaml_ModelHint}"
                                                 AutomationProperties.Name="{svc:Loc Key=Xaml_AI_model_6572e8b8}"
                                                 ToolTip="{svc:Loc Key=Xaml_DeepSeek_model_deepseek_chat_is_the_ca4c0112}" />
                                        <PasswordBox x:Name="AiKeyBox" VerticalAlignment="Center"
                                                     AutomationProperties.Name="{svc:Loc Key=Xaml_DeepSeek_API_key_aef39533}" />
                                    </DockPanel>
                                    <DockPanel LastChildFill="False" Margin="0,8,0,0">
                                        <CheckBox DockPanel.Dock="Left" Content="{svc:Loc Key=Xaml_Auto_categorize_new_blocks_909f7f79}" VerticalAlignment="Center"
                                                  IsChecked="{Binding AiEnabled}" Margin="0,0,12,0"
                                                  ToolTip="{svc:Loc Key=Xaml_File_every_new_manual_block_under_47dee3d1}" />
                                        <Button DockPanel.Dock="Left" Content="{svc:Loc Key=Xaml_Categorize_uncategorized_domains_2705b2d4}"
                                                Command="{Binding CategorizeAllCommand}" Margin="0,0,12,0"
                                                ToolTip="{svc:Loc Key=Xaml_Send_every_uncategorized_blocked_domain_to_72e7027a}" />
                                        <Button DockPanel.Dock="Left" Content="{svc:Loc Key=Xaml_Export_AI_knowledge_c58d27a3}"
                                                Command="{Binding ExportAiKnowledgeCommand}"
                                                ToolTip="{svc:Loc Key=Xaml_Save_everything_the_AI_has_learned_ed995f2f}" />
                                    </DockPanel>
                                    <Border Style="{StaticResource Hg.StatusStrip}" Margin="0,8,0,0">
                                        <TextBlock Text="{Binding AiStatusText}"
                                                   Style="{StaticResource Hg.SubText}"
                                                   TextWrapping="Wrap"
                                                   AutomationProperties.LiveSetting="Polite" />
                                    </Border>
                                </StackPanel>
                            </Border>

                            <!-- AI knowledge review & promote (NET-107) -->
                            <Border Style="{StaticResource Hg.Card}" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="{svc:Loc Key=Xaml_AI_knowledge_review_c036069a}" FontWeight="SemiBold" Margin="0,0,0,4" />
                                    <TextBlock Text="{svc:Loc Key=Xaml_Review_what_the_AI_has_learned_c10f330b}"
                                               Style="{StaticResource Hg.HelperText}" Margin="0,0,0,8" />
                                    <DockPanel LastChildFill="False" Margin="0,0,0,8">
                                        <Button DockPanel.Dock="Left" Content="{svc:Loc Key=Xaml_Load_learned_40f26ad2}" Command="{Binding LoadKnowledgeCommand}"
                                                Margin="0,0,8,0" ToolTip="{svc:Loc Key=Xaml_List_what_the_AI_has_learned_3e46adb3}" />
                                        <Button DockPanel.Dock="Left" Content="{svc:Loc Key=Xaml_Mark_all_reviewed_40153e6d}" Command="{Binding MarkKnowledgeReviewedCommand}"
                                                Margin="0,0,8,0" ToolTip="{svc:Loc Key=Xaml_Advance_the_new_since_last_review_fbe3d1a0}" />
                                        <CheckBox DockPanel.Dock="Left" Content="{svc:Loc Key=Xaml_Only_new_since_last_review_a443fd1e}" VerticalAlignment="Center"
                                                  IsChecked="{Binding KnowledgeOnlyNew}"
                                                  ToolTip="{svc:Loc Key=Xaml_Show_only_entries_the_AI_learned_ecfbe106}" />
                                    </DockPanel>
                                    <DataGrid ItemsSource="{Binding Knowledge}" AutoGenerateColumns="False"
                                              MaxHeight="240" IsReadOnly="False" CanUserAddRows="False"
                                              HeadersVisibility="Column" Margin="0,0,0,8"
                                              AutomationProperties.Name="{svc:Loc Key=Xaml_Learned_AI_knowledge_22d7f148}">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="{svc:Loc Key=Xaml_Kind_e00ac23f}" Binding="{Binding Kind}" IsReadOnly="True" Width="80" />
                                            <DataGridTextColumn Header="{svc:Loc Key=Xaml_Domain_key_60671553}" Binding="{Binding Key}" IsReadOnly="True" Width="1.4*" />
                                            <DataGridTextColumn Header="{svc:Loc Key=Xaml_AI_label_a2ea7244}" Binding="{Binding Value}" IsReadOnly="True" Width="1.4*" />
                                            <DataGridTextColumn Header="{svc:Loc Key=Xaml_Promote_as_433a4f10}" Binding="{Binding EditValue, UpdateSourceTrigger=PropertyChanged}" Width="1.4*" />
                                            <DataGridTemplateColumn Header="{svc:Loc Key=Xaml_Override_84219262}" Width="1*">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding UserOverride}" VerticalAlignment="Center"
                                                                   Foreground="{DynamicResource Hg.Mauve}" />
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                            <DataGridTemplateColumn Header="{svc:Loc Key=Xaml_Actions_c3cd636a}" Width="Auto">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Horizontal">
                                                            <Button Content="{svc:Loc Key=Xaml_Promote_61c57736}" Margin="2,1"
                                                                    Command="{Binding DataContext.PromoteKnowledgeCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                                    CommandParameter="{Binding}" />
                                                            <Button Content="{svc:Loc Key=Xaml_Discard_36fff63c}" Margin="2,1"
                                                                    Command="{Binding DataContext.DiscardKnowledgeCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                                    CommandParameter="{Binding}" />
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                    <TextBlock Text="{svc:Loc Key=Xaml_Correct_a_domain_directly_remembered_override_3f0323e4}" Style="{StaticResource Hg.FieldLabel}" />
                                    <DockPanel LastChildFill="False">
                                        <ComboBox DockPanel.Dock="Left" Width="110" VerticalAlignment="Center"
                                                  ItemsSource="{Binding CorrectionKinds}" SelectedItem="{Binding CorrectKind}"
                                                  AutomationProperties.Name="{svc:Loc Key=Xaml_Correction_kind_688a781b}" />
                                        <TextBox DockPanel.Dock="Left" Width="190" Margin="8,0,0,0" VerticalAlignment="Center"
                                                 Text="{Binding CorrectDomain, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}" Tag="{svc:Loc Key=Xaml_CorrectionDomainHint}"
                                                 AutomationProperties.Name="{svc:Loc Key=Xaml_Correction_domain_1067f9bb}" />
                                        <TextBox DockPanel.Dock="Left" Width="220" Margin="8,0,0,0" VerticalAlignment="Center"
                                                 Text="{Binding CorrectValue, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}" Tag="{svc:Loc Key=Xaml_CorrectionValueHint}"
                                                 AutomationProperties.Name="{svc:Loc Key=Xaml_Correction_value_3225de74}" />
                                        <Button DockPanel.Dock="Left" Content="{svc:Loc Key=Xaml_Apply_cfea419c}" Margin="8,0,0,0"
                                                Style="{StaticResource Hg.AccentButton}" Command="{Binding CorrectDomainCommand}" />
                                    </DockPanel>
                                    <Border Style="{StaticResource Hg.StatusStrip}" Margin="0,8,0,0">
                                        <TextBlock Text="{Binding KnowledgeStatusText}"
                                                   Style="{StaticResource Hg.SubText}"
                                                   TextWrapping="Wrap"
                                                   AutomationProperties.LiveSetting="Polite" />
                                    </Border>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource Hg.Card}" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="{svc:Loc Key=Xaml_Scheduled_blocking_5a4effa0}" FontWeight="SemiBold" Margin="0,0,0,4" />
                                    <TextBlock Text="{svc:Loc Key=Xaml_Use_a_domain_service_target_or_59499d73}"
                                               Style="{StaticResource Hg.HelperText}" Margin="0,0,0,8" />
                                    <DataGrid ItemsSource="{Binding Schedules}" IsReadOnly="False" MinHeight="80" MaxHeight="220"
                                              CanUserAddRows="False"
                                              AutomationProperties.Name="{svc:Loc Key=Xaml_Scheduled_blocking_windows_06fcbe8c}">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="{svc:Loc Key=Xaml_Domain_target_a5cc1a02}" Binding="{Binding Target, UpdateSourceTrigger=PropertyChanged}" Width="2*" />
                                            <DataGridTextColumn Header="{svc:Loc Key=Xaml_Days_f6bb0f46}" Binding="{Binding DaysText, UpdateSourceTrigger=PropertyChanged}" Width="2*" />
                                            <DataGridTextColumn Header="{svc:Loc Key=Xaml_Start_952f3754}" Binding="{Binding Start, UpdateSourceTrigger=PropertyChanged}" Width="80" />
                                            <DataGridTextColumn Header="{svc:Loc Key=Xaml_End_a2bb9d34}" Binding="{Binding End, UpdateSourceTrigger=PropertyChanged}" Width="80" />
                                            <DataGridTemplateColumn Header="{svc:Loc Key=Xaml_Actions_c3cd636a}" Width="90">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <Button Content="{svc:Loc Key=Xaml_Remove_e963907d}" Padding="8,2"
                                                                Style="{StaticResource Hg.DangerButton}"
                                                                Command="{Binding DataContext.RemoveScheduleCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                                CommandParameter="{Binding}" />
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                    <Border Style="{StaticResource Hg.EmptyState}"
                                            Margin="0,8,0,0"
                                            Visibility="{Binding Schedules.Count, Converter={StaticResource EmptyStateVis}}">
                                        <TextBlock Text="{svc:Loc Key=Xaml_No_schedules_yet_Add_a_blocking_346560fe}"
                                                   Style="{StaticResource Hg.EmptyText}" />
                                    </Border>
                                    <DockPanel Margin="0,8,0,0" LastChildFill="False">
                                        <Button DockPanel.Dock="Left" Content="{svc:Loc Key=Xaml_Add_window_840e8f46}"
                                                Command="{Binding AddScheduleCommand}" Margin="0,0,10,0" />
                                        <Button DockPanel.Dock="Left" Content="{svc:Loc Key=Xaml_Save_schedules_cb3a4a50}"
                                                Style="{StaticResource Hg.AccentButton}"
                                                Command="{Binding SaveSchedulesCommand}" />
                                    </DockPanel>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource Hg.Card}" Margin="0,0,0,10">
                                <StackPanel>
                                    <DockPanel LastChildFill="False">
                                        <TextBlock DockPanel.Dock="Left"
                                                   Text="{svc:Loc Key=PolicySub_Title}"
                                                   FontWeight="SemiBold"
                                                   Margin="0,0,0,4" />
                                        <Button DockPanel.Dock="Right"
                                                Content="{svc:Loc Key=Xaml_Refresh_56e3badc}"
                                                Command="{Binding LoadPolicySubscriptionsCommand}"
                                                Padding="10,2" />
                                    </DockPanel>
                                    <TextBlock Text="{svc:Loc Key=PolicySub_Help}"
                                               Style="{StaticResource Hg.HelperText}"
                                               Margin="0,0,0,8"
                                               TextWrapping="Wrap" />

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="170" />
                                            <ColumnDefinition Width="2*" />
                                            <ColumnDefinition Width="1.4*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <TextBox Grid.Row="0" Grid.Column="0" Margin="0,0,8,8"
                                                 Text="{Binding PolicySubscriptionName, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=PolicySub_NameHint}"
                                                 AutomationProperties.Name="{svc:Loc Key=PolicySub_NameName}"
                                                 ToolTip="{svc:Loc Key=PolicySub_NameTip}" />
                                        <TextBox Grid.Row="0" Grid.Column="1" Margin="0,0,8,8"
                                                 Text="{Binding PolicySubscriptionUrl, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=PolicySub_UrlHint}"
                                                 AutomationProperties.Name="{svc:Loc Key=PolicySub_UrlName}"
                                                 ToolTip="{svc:Loc Key=PolicySub_UrlTip}" />
                                        <TextBox Grid.Row="0" Grid.Column="2" Margin="0,0,0,8"
                                                 Text="{Binding PolicySubscriptionPinHash, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=PolicySub_PinHashHint}"
                                                 AutomationProperties.Name="{svc:Loc Key=PolicySub_PinHashName}"
                                                 ToolTip="{svc:Loc Key=PolicySub_PinHashTip}" />
                                        <WrapPanel Grid.Row="1" Grid.ColumnSpan="3">
                                            <CheckBox Content="{svc:Loc Key=PolicySub_Enabled}"
                                                      IsChecked="{Binding PolicySubscriptionEnabled}"
                                                      VerticalAlignment="Center"
                                                      Margin="0,0,14,8"
                                                      ToolTip="{svc:Loc Key=PolicySub_EnabledTip}" />
                                            <CheckBox Content="{svc:Loc Key=PolicySub_AutoApply}"
                                                      IsChecked="{Binding PolicySubscriptionAutoApply}"
                                                      VerticalAlignment="Center"
                                                      Margin="0,0,14,8"
                                                      ToolTip="{svc:Loc Key=PolicySub_AutoApplyTip}" />
                                            <CheckBox Content="{svc:Loc Key=PolicySub_PinFetched}"
                                                      IsChecked="{Binding PolicySubscriptionPinCurrentHash}"
                                                      VerticalAlignment="Center"
                                                      Margin="0,0,14,8"
                                                      ToolTip="{svc:Loc Key=PolicySub_PinFetchedTip}" />
                                            <Button Content="{svc:Loc Key=PolicySub_Preview}"
                                                    Command="{Binding PreviewPolicySubscriptionCommand}"
                                                    Margin="0,0,8,8" />
                                            <Button Content="{svc:Loc Key=PolicySub_Apply}"
                                                    Style="{StaticResource Hg.AccentButton}"
                                                    Command="{Binding ApplyPolicySubscriptionCommand}"
                                                    Margin="0,0,8,8" />
                                            <Button Content="{svc:Loc Key=PolicySub_Save}"
                                                    Command="{Binding SavePolicySubscriptionCommand}"
                                                    Margin="0,0,8,8" />
                                            <Button Content="{svc:Loc Key=PolicySub_ApplyAuto}"
                                                    Command="{Binding RefreshPolicySubscriptionsCommand}"
                                                    Margin="0,0,8,8" />
                                            <Button Content="{svc:Loc Key=PolicySub_Rollback}"
                                                    Style="{StaticResource Hg.DangerButton}"
                                                    Command="{Binding RollbackPolicySubscriptionCommand}"
                                                    Margin="0,0,8,8" />
                                            <Button Content="{svc:Loc Key=Xaml_Remove_e963907d}"
                                                    Style="{StaticResource Hg.DangerButton}"
                                                    Command="{Binding DeletePolicySubscriptionCommand}"
                                                    Margin="0,0,8,8" />
                                        </WrapPanel>
                                    </Grid>

                                    <DataGrid ItemsSource="{Binding PolicySubscriptions}"
                                              SelectedItem="{Binding SelectedPolicySubscription}"
                                              AutoGenerateColumns="False"
                                              IsReadOnly="True"
                                              CanUserAddRows="False"
                                              HeadersVisibility="Column"
                                              MaxHeight="180"
                                              Margin="0,4,0,0"
                                              AutomationProperties.Name="{svc:Loc Key=PolicySub_TableName}">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="{svc:Loc Key=Xaml_Name_709a2322}" Binding="{Binding Name}" Width="1.2*" />
                                            <DataGridTextColumn Header="{svc:Loc Key=PolicySub_State}" Binding="{Binding StateText}" Width="84" />
                                            <DataGridTextColumn Header="{svc:Loc Key=PolicySub_Trust}" Binding="{Binding TrustText}" Width="84" />
                                            <DataGridTextColumn Header="{svc:Loc Key=PolicySub_Mode}" Binding="{Binding ApplyModeText}" Width="118" />
                                            <DataGridTextColumn Header="{svc:Loc Key=PolicySub_LastApplied}" Binding="{Binding LastAppliedText}" Width="112" />
                                            <DataGridTextColumn Header="{svc:Loc Key=PolicySub_LastHash}" Binding="{Binding LastHash}" Width="1.4*" />
                                            <DataGridTextColumn Header="{svc:Loc Key=PolicySub_Error}" Binding="{Binding ErrorText}" Width="1.4*" />
                                        </DataGrid.Columns>
                                    </DataGrid>
                                    <Border Style="{StaticResource Hg.EmptyState}"
                                            Margin="0,8,0,0"
                                            Visibility="{Binding PolicySubscriptions.Count, Converter={StaticResource EmptyStateVis}}">
                                        <TextBlock Text="{svc:Loc Key=PolicySub_Empty}"
                                                   Style="{StaticResource Hg.EmptyText}" />
                                    </Border>
                                    <Border Style="{StaticResource Hg.StatusStrip}" Margin="0,8,0,0">
                                        <TextBlock Text="{Binding PolicySubscriptionStatusText}"
                                                   Style="{StaticResource Hg.SubText}"
                                                   TextWrapping="Wrap"
                                                   AutomationProperties.LiveSetting="Polite" />
                                    </Border>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource Hg.Card}" Margin="0,0,0,10">
                                <StackPanel>
                                    <DockPanel LastChildFill="False">
                                        <TextBlock DockPanel.Dock="Left"
                                                   Text="{svc:Loc Key=IpBlock_Title}"
                                                   FontWeight="SemiBold"
                                                   Margin="0,0,0,4" />
                                        <Button DockPanel.Dock="Right"
                                                Content="{svc:Loc Key=Xaml_Refresh_56e3badc}"
                                                Command="{Binding LoadIpBlocklistsCommand}"
                                                Padding="10,2" />
                                    </DockPanel>
                                    <TextBlock Text="{svc:Loc Key=IpBlock_Help}"
                                               Style="{StaticResource Hg.HelperText}"
                                               Margin="0,0,0,8"
                                               TextWrapping="Wrap" />

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="170" />
                                            <ColumnDefinition Width="2*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <TextBox Grid.Row="0" Grid.Column="0" Margin="0,0,8,8"
                                                 Text="{Binding IpBlocklistName, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=IpBlock_NameHint}"
                                                 AutomationProperties.Name="{svc:Loc Key=IpBlock_NameName}"
                                                 ToolTip="{svc:Loc Key=IpBlock_NameTip}" />
                                        <TextBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,8"
                                                 Text="{Binding IpBlocklistUrl, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=IpBlock_UrlHint}"
                                                 AutomationProperties.Name="{svc:Loc Key=IpBlock_UrlName}"
                                                 ToolTip="{svc:Loc Key=IpBlock_UrlTip}" />
                                        <WrapPanel Grid.Row="1" Grid.ColumnSpan="2">
                                            <Button Content="{svc:Loc Key=IpBlock_Import}"
                                                    Style="{StaticResource Hg.AccentButton}"
                                                    Command="{Binding ImportIpBlocklistCommand}"
                                                    Margin="0,0,8,8" />
                                            <Button Content="{svc:Loc Key=IpBlock_RefreshNow}"
                                                    Command="{Binding RefreshIpBlocklistsCommand}"
                                                    Margin="0,0,8,8" />
                                            <Button Content="{svc:Loc Key=IpBlock_Toggle}"
                                                    Command="{Binding ToggleIpBlocklistCommand}"
                                                    Margin="0,0,8,8" />
                                            <Button Content="{svc:Loc Key=IpBlock_Rollback}"
                                                    Style="{StaticResource Hg.DangerButton}"
                                                    Command="{Binding RollbackIpBlocklistCommand}"
                                                    Margin="0,0,8,8" />
                                            <Button Content="{svc:Loc Key=Xaml_Remove_e963907d}"
                                                    Style="{StaticResource Hg.DangerButton}"
                                                    Command="{Binding RemoveIpBlocklistCommand}"
                                                    Margin="0,0,8,8" />
                                        </WrapPanel>
                                    </Grid>

                                    <DataGrid ItemsSource="{Binding IpBlocklists}"
                                              SelectedItem="{Binding SelectedIpBlocklist}"
                                              AutoGenerateColumns="False"
                                              IsReadOnly="True"
                                              CanUserAddRows="False"
                                              HeadersVisibility="Column"
                                              MaxHeight="180"
                                              Margin="0,4,0,0"
                                              AutomationProperties.Name="{svc:Loc Key=IpBlock_TableName}">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="{svc:Loc Key=Xaml_Name_709a2322}" Binding="{Binding Name}" Width="1.2*" />
                                            <DataGridTextColumn Header="{svc:Loc Key=IpBlock_ColState}" Binding="{Binding EnabledText}" Width="84" />
                                            <DataGridTextColumn Header="{svc:Loc Key=IpBlock_ColHealth}" Binding="{Binding HealthText}" Width="110" />
                                            <DataGridTextColumn Header="{svc:Loc Key=IpBlock_ColAddresses}" Binding="{Binding AddressCount}" Width="92" />
                                            <DataGridTextColumn Header="{svc:Loc Key=IpBlock_ColRules}" Binding="{Binding RuleCount}" Width="70" />
                                            <DataGridTextColumn Header="{svc:Loc Key=IpBlock_ColRefreshed}" Binding="{Binding LastRefreshText}" Width="112" />
                                            <DataGridTextColumn Header="{svc:Loc Key=IpBlock_ColError}" Binding="{Binding ErrorText}" Width="1.4*" />
                                        </DataGrid.Columns>
                                    </DataGrid>
                                    <Border Style="{StaticResource Hg.EmptyState}"
                                            Margin="0,8,0,0"
                                            Visibility="{Binding IpBlocklists.Count, Converter={StaticResource EmptyStateVis}}">
                                        <TextBlock Text="{svc:Loc Key=IpBlock_Empty}"
                                                   Style="{StaticResource Hg.EmptyText}" />
                                    </Border>
                                    <Border Style="{StaticResource Hg.StatusStrip}" Margin="0,8,0,0">
                                        <TextBlock Text="{Binding IpBlocklistStatusText}"
                                                   Style="{StaticResource Hg.SubText}"
                                                   TextWrapping="Wrap"
                                                   AutomationProperties.LiveSetting="Polite" />
                                    </Border>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource Hg.Card}" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="{svc:Loc Key=Xaml_Network_profiles_04269420}" FontWeight="SemiBold" Margin="0,0,0,4" />
                                    <TextBlock Text="{svc:Loc Key=Xaml_Save_named_snapshots_of_the_current_305311f0}"
                                               Style="{StaticResource Hg.HelperText}" Margin="0,0,0,4" />
                                    <TextBlock Text="{Binding ActiveProfile}" Style="{StaticResource Hg.DimText}" Margin="0,0,0,8" />
                                    <WrapPanel>
                                        <ComboBox Width="180" Margin="0,0,8,8"
                                                  ItemsSource="{Binding Profiles}"
                                                  AutomationProperties.Name="{svc:Loc Key=Xaml_Saved_network_profiles_8671dcb5}"
                                                  SelectedItem="{Binding SelectedProfile}" />
                                        <Button Content="{svc:Loc Key=Xaml_Switch_3e44c920}"
                                                Style="{StaticResource Hg.AccentButton}"
                                                Command="{Binding SwitchProfileCommand}" Margin="0,0,8,8"
                                                ToolTip="{svc:Loc Key=Xaml_Choose_a_saved_profile_first_8f0db999}" />
                                        <Button Content="{svc:Loc Key=Xaml_Delete_f6fdbe48}"
                                                Style="{StaticResource Hg.DangerButton}"
                                                Command="{Binding DeleteProfileCommand}" Margin="0,0,16,8"
                                                ToolTip="{svc:Loc Key=Xaml_Choose_a_saved_profile_first_8f0db999}" />
                                        <TextBox Width="180" Margin="0,0,8,8"
                                                 Text="{Binding NewProfileName, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=Xaml_New_profile_name_393313b6}"
                                                 AutomationProperties.Name="{svc:Loc Key=Xaml_New_profile_name_393313b6}"
                                                 ToolTip="{svc:Loc Key=Xaml_Name_for_a_snapshot_of_the_196c42ae}" />
                                        <Button Content="{svc:Loc Key=Xaml_Save_current_as_profile_8e02fa9c}"
                                                Command="{Binding SaveProfileCommand}"
                                                Margin="0,0,8,8"
                                                ToolTip="{svc:Loc Key=Xaml_Enter_a_profile_name_before_saving_a706bf49}" />
                                    </WrapPanel>
                                    <Separator Margin="0,4,0,10" />
                                    <TextBlock Text="{svc:Loc Key=ProfileMatch_Title}" FontWeight="SemiBold" Margin="0,0,0,4" />
                                    <TextBlock Text="{svc:Loc Key=ProfileMatch_Help}"
                                               Style="{StaticResource Hg.HelperText}" TextWrapping="Wrap" Margin="0,0,0,6" />
                                    <TextBlock Text="{Binding CurrentNetworkText}" Style="{StaticResource Hg.DimText}"
                                               TextWrapping="Wrap" Margin="0,0,0,8"
                                               AutomationProperties.LiveSetting="Polite" />
                                    <WrapPanel>
                                        <TextBox Width="170" Margin="0,0,8,8" Text="{Binding NetworkRuleLabel, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}" Tag="{svc:Loc Key=ProfileMatch_Label}"
                                                 AutomationProperties.Name="{svc:Loc Key=ProfileMatch_Label}" />
                                        <ComboBox Width="150" Margin="0,0,8,8" ItemsSource="{Binding Profiles}"
                                                  SelectedItem="{Binding SelectedProfile}"
                                                  AutomationProperties.Name="{svc:Loc Key=ProfileMatch_Profile}" />
                                        <TextBox Width="150" Margin="0,0,8,8" Text="{Binding NetworkRuleSsid, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}" Tag="{svc:Loc Key=ProfileMatch_Ssid}"
                                                 AutomationProperties.Name="{svc:Loc Key=ProfileMatch_Ssid}" />
                                        <TextBox Width="170" Margin="0,0,8,8" Text="{Binding NetworkRuleInterface, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}" Tag="{svc:Loc Key=ProfileMatch_Interface}"
                                                 AutomationProperties.Name="{svc:Loc Key=ProfileMatch_Interface}" />
                                        <TextBox Width="170" Margin="0,0,8,8" Text="{Binding NetworkRuleDnsSuffix, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}" Tag="{svc:Loc Key=ProfileMatch_DnsSuffix}"
                                                 AutomationProperties.Name="{svc:Loc Key=ProfileMatch_DnsSuffix}" />
                                        <TextBox Width="160" Margin="0,0,8,8" Text="{Binding NetworkRuleGatewayMac, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}" Tag="{svc:Loc Key=ProfileMatch_Gateway}"
                                                 AutomationProperties.Name="{svc:Loc Key=ProfileMatch_Gateway}" />
                                        <ComboBox Width="240" Margin="0,0,8,8" ItemsSource="{Binding NetworkVpnOptions}"
                                                  ItemTemplate="{StaticResource ToolsWrappedContent}"
                                                  SelectedItem="{Binding SelectedNetworkVpnOption}"
                                                  AutomationProperties.Name="{svc:Loc Key=ProfileMatch_Vpn}" />
                                    </WrapPanel>
                                    <WrapPanel>
                                        <Button Content="{svc:Loc Key=ProfileMatch_UseCurrent}" Command="{Binding UseCurrentNetworkCommand}"
                                                MinWidth="180" Margin="0,0,8,8" AutomationProperties.Name="{svc:Loc Key=ProfileMatch_UseCurrentName}" />
                                        <Button Content="{svc:Loc Key=ProfileMatch_Save}" Command="{Binding SaveNetworkProfileRuleCommand}"
                                                Style="{StaticResource Hg.AccentButton}" MinWidth="240" Margin="0,0,8,8"
                                                ContentTemplate="{StaticResource ToolsWrappedContent}"
                                                AutomationProperties.Name="{svc:Loc Key=ProfileMatch_Save}" />
                                        <Button Content="{svc:Loc Key=ProfileMatch_Delete}" Command="{Binding DeleteNetworkProfileRuleCommand}"
                                                Style="{StaticResource Hg.DangerButton}" MinWidth="180" Margin="0,0,8,8"
                                                ContentTemplate="{StaticResource ToolsWrappedContent}"
                                                AutomationProperties.Name="{svc:Loc Key=ProfileMatch_Delete}" />
                                    </WrapPanel>
                                    <DataGrid ItemsSource="{Binding NetworkProfileRules}" SelectedItem="{Binding SelectedNetworkProfileRule}"
                                              AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                                              HeadersVisibility="Column" MaxHeight="170" Margin="0,2,0,0"
                                              AutomationProperties.Name="{svc:Loc Key=ProfileMatch_TableName}">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="{svc:Loc Key=ProfileMatch_Label}" Binding="{Binding Label}" Width="1.1*" />
                                            <DataGridTextColumn Header="{svc:Loc Key=ProfileMatch_Profile}" Binding="{Binding Profile}" Width="1*" />
                                            <DataGridTextColumn Header="{svc:Loc Key=ProfileMatch_Criteria}" Binding="{Binding CriteriaText}" Width="2.5*" />
                                        </DataGrid.Columns>
                                    </DataGrid>
                                    <Border Style="{StaticResource Hg.EmptyState}" Margin="0,8,0,0"
                                            Visibility="{Binding NetworkProfileRules.Count, Converter={StaticResource EmptyStateVis}}">
                                        <TextBlock Text="{svc:Loc Key=ProfileMatch_Empty}" Style="{StaticResource Hg.EmptyText}" />
                                    </Border>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource Hg.Card}" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="{svc:Loc Key=CaptivePortal_Title}" FontWeight="SemiBold" Margin="0,0,0,4" />
                                    <TextBlock Text="{svc:Loc Key=CaptivePortal_Help}" Style="{StaticResource Hg.HelperText}"
                                               TextWrapping="Wrap" Margin="0,0,0,8" />
                                    <TextBlock Text="{Binding CaptivePortalStatusText}" Style="{StaticResource Hg.DimText}"
                                               TextWrapping="Wrap" Margin="0,0,0,8"
                                               AutomationProperties.LiveSetting="Polite" />
                                    <WrapPanel>
                                        <Button Content="{svc:Loc Key=CaptivePortal_Check}" Command="{Binding CheckCaptivePortalCommand}"
                                                MinWidth="150" Margin="0,0,8,8" AutomationProperties.Name="{svc:Loc Key=CaptivePortal_CheckName}" />
                                        <Button Content="{svc:Loc Key=CaptivePortal_Pause5}" Command="{Binding PauseForCaptivePortalCommand}"
                                                CommandParameter="5" IsEnabled="{Binding CaptivePortalPauseAvailable}" Margin="0,0,8,8"
                                                AutomationProperties.Name="{svc:Loc Key=CaptivePortal_Pause5}" />
                                        <Button Content="{svc:Loc Key=CaptivePortal_Pause15}" Command="{Binding PauseForCaptivePortalCommand}"
                                                CommandParameter="15" IsEnabled="{Binding CaptivePortalPauseAvailable}" Margin="0,0,8,8"
                                                AutomationProperties.Name="{svc:Loc Key=CaptivePortal_Pause15}" />
                                        <Button Content="{svc:Loc Key=CaptivePortal_Pause60}" Command="{Binding PauseForCaptivePortalCommand}"
                                                CommandParameter="60" IsEnabled="{Binding CaptivePortalPauseAvailable}" Margin="0,0,8,8"
                                                AutomationProperties.Name="{svc:Loc Key=CaptivePortal_Pause60}" />
                                    </WrapPanel>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource Hg.Card}" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="{svc:Loc Key=Xaml_Maintenance_94de303b}" FontWeight="SemiBold" Margin="0,0,0,4" />
                                    <TextBlock Text="{svc:Loc Key=Xaml_Back_up_harden_export_diagnostics_refresh_5026f167}"
                                               Style="{StaticResource Hg.HelperText}" Margin="0,0,0,8" />
                                    <WrapPanel>
                                        <Button DockPanel.Dock="Left" Content="{svc:Loc Key=Xaml_Backup_hosts_file_b631b0d3}"
                                                Command="{Binding BackupHostsCommand}" Margin="0,0,10,8" />
                                        <Button DockPanel.Dock="Left" Content="{svc:Loc Key=Xaml_Harden_hosts_ACL_c68950b7}"
                                                Command="{Binding HardenAclCommand}" Margin="0,0,10,8" />
                                        <Button DockPanel.Dock="Left" Content="{svc:Loc Key=Xaml_Export_support_bundle_redacted_9362a115}"
                                                Command="{Binding ExportBundleCommand}" Margin="0,0,10,8" />
                                        <Button DockPanel.Dock="Left" Content="{svc:Loc Key=Xaml_Apply_known_safe_baseline_e67182c9}"
                                                Command="{Binding ApplyBaselineCommand}" Margin="0,0,10,8"
                                                ToolTip="{svc:Loc Key=Xaml_Pre_allow_essential_Windows_binaries_Update_22ed5300}" />
                                        <Button DockPanel.Dock="Left"
                                                Command="{Binding ToggleSecureRulesCommand}" Margin="0,0,10,8"
                                                ToolTip="{svc:Loc Key=Xaml_Secure_Rules_the_service_recreates_or_894b4569}">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                                    <Setter Property="Content" Value="{svc:Loc Key=Xaml_ArmSecureRulesButton}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding SecureRulesActive}" Value="True">
                                                            <Setter Property="Content" Value="{svc:Loc Key=Xaml_DisarmSecureRulesButton}" />
                                                            <Setter Property="Background" Value="{DynamicResource Hg.Danger2}" />
                                                            <Setter Property="Foreground" Value="{DynamicResource Hg.OnDanger}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                        <Button DockPanel.Dock="Left" Content="{svc:Loc Key=Xaml_Refresh_threat_intel_a6f40d99}"
                                                Command="{Binding RefreshThreatIntelCommand}" Margin="0,0,10,8" />
                                        <Button DockPanel.Dock="Left" Content="{svc:Loc Key=Xaml_Refresh_GeoIP_0f5c8fef}"
                                                Command="{Binding RefreshGeoIpCommand}" Margin="0,0,10,8" />
                                        <Button DockPanel.Dock="Left" Content="{svc:Loc Key=Xaml_Exclude_hosts_in_Defender_9cf62a6e}"
                                                Command="{Binding AddDefenderExclusionCommand}" Margin="0,0,10,8" />
                                        <Button DockPanel.Dock="Left" Content="{svc:Loc Key=Xaml_Emergency_reset_hosts_87f24c13}"
                                                Style="{StaticResource Hg.DangerButton}"
                                                Command="{Binding EmergencyResetCommand}"
                                                Margin="0,0,10,8" />
                                    </WrapPanel>
                                    <WrapPanel Margin="0,2,0,0">
                                        <TextBlock Text="{svc:Loc Key=Xaml_Restore_backup_581b7174}" VerticalAlignment="Center" Margin="0,0,8,8" />
                                        <ComboBox Width="320" Margin="0,0,8,8"
                                                  ItemsSource="{Binding Backups}"
                                                  SelectedItem="{Binding SelectedBackup}"
                                                  DisplayMemberPath="Label"
                                                  AutomationProperties.Name="{svc:Loc Key=Xaml_Hosts_backup_to_restore_5e191a1c}"
                                                  ToolTip="{svc:Loc Key=Xaml_Hosts_file_backups_in_the_service_4d6477de}" />
                                        <Button Content="{svc:Loc Key=Xaml_Restore_3cbe6d6b}"
                                                Style="{StaticResource Hg.DangerButton}"
                                                Command="{Binding RestoreBackupCommand}" Margin="0,0,10,8"
                                                ToolTip="{svc:Loc Key=Xaml_Choose_a_backup_before_restoring_4c45efa5}" />
                                        <Button Content="{svc:Loc Key=Xaml_Refresh_list_7fe6bbe7}"
                                                Command="{Binding LoadBackupsCommand}"
                                                Margin="0,0,10,8" />
                                    </WrapPanel>
                                    <Border Style="{StaticResource Hg.StatusStrip}" Margin="0,8,0,0">
                                        <StackPanel>
                                            <TextBlock Text="{Binding SecureRulesText}"
                                                       Style="{StaticResource Hg.SubText}"
                                                       AutomationProperties.LiveSetting="Polite" />
                                            <TextBlock Text="{Binding DefenderStatusText}"
                                                       Style="{StaticResource Hg.DimText}"
                                                       Margin="0,3,0,0"
                                                       AutomationProperties.LiveSetting="Polite" />
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource Hg.Card}" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="{svc:Loc Key=Recovery_Title}" FontWeight="SemiBold" Margin="0,0,0,4" />
                                    <TextBlock Text="{svc:Loc Key=Recovery_Help}"
                                               Style="{StaticResource Hg.HelperText}" TextWrapping="Wrap" Margin="0,0,0,8" />
                                    <WrapPanel>
                                        <Button Content="{svc:Loc Key=Recovery_Create}"
                                                Style="{StaticResource Hg.AccentButton}"
                                                Command="{Binding CreateFullStateSnapshotCommand}" Margin="0,0,8,8" />
                                        <Button Content="{svc:Loc Key=Xaml_Refresh_list_7fe6bbe7}"
                                                Command="{Binding LoadFullStateSnapshotsCommand}" Margin="0,0,8,8" />
                                    </WrapPanel>
                                    <WrapPanel>
                                        <ComboBox Width="460" Margin="0,0,8,8"
                                                  ItemsSource="{Binding FullStateSnapshots}"
                                                  SelectedItem="{Binding SelectedFullStateSnapshot}"
                                                  DisplayMemberPath="Label"
                                                  AutomationProperties.Name="{svc:Loc Key=Recovery_SnapshotPicker}" />
                                        <Button Content="{svc:Loc Key=Recovery_Preview}"
                                                Command="{Binding PreviewFullStateRestoreCommand}" Margin="0,0,8,8" />
                                        <Button Content="{svc:Loc Key=Recovery_Restore}"
                                                Style="{StaticResource Hg.DangerButton}"
                                                Command="{Binding RestoreFullStateSnapshotCommand}" Margin="0,0,8,8" />
                                    </WrapPanel>
                                    <Border Style="{StaticResource Hg.StatusStrip}" Margin="0,4,0,0">
                                        <StackPanel>
                                            <TextBlock Text="{Binding FullStateSnapshotStatus}"
                                                       Style="{StaticResource Hg.SubText}"
                                                       TextWrapping="Wrap"
                                                       AutomationProperties.LiveSetting="Polite" />
                                            <TextBlock Text="{Binding FullStateRestorePreview}"
                                                       Style="{StaticResource Hg.DimText}"
                                                       TextWrapping="Wrap" Margin="0,4,0,0"
                                                       AutomationProperties.LiveSetting="Polite" />
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </Border>

                            <!-- Trusted publishers (NET-113) -->
                            <Border Style="{StaticResource Hg.Card}" Margin="0,0,0,10">
                                <StackPanel>
                                    <DockPanel LastChildFill="False">
                                        <TextBlock DockPanel.Dock="Left" Text="{svc:Loc Key=Xaml_Trusted_publishers_cb37fb9f}" FontWeight="SemiBold" />
                                        <Button DockPanel.Dock="Right" Content="{svc:Loc Key=Xaml_Refresh_56e3badc}"
                                                Command="{Binding LoadTrustedPublishersCommand}" Padding="10,2" />
                                    </DockPanel>
                                    <TextBlock Text="{svc:Loc Key=Xaml_Software_signed_by_these_Authenticode_publishers_6fac87af}"
                                               Style="{StaticResource Hg.HelperText}" Margin="0,4,0,8" />
                                    <ItemsControl ItemsSource="{Binding TrustedPublishers}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <DockPanel LastChildFill="False" Margin="0,0,0,4">
                                                    <TextBlock DockPanel.Dock="Left" Text="{Binding}" VerticalAlignment="Center" />
                                                    <Button DockPanel.Dock="Right" Content="{svc:Loc Key=Xaml_Remove_e963907d}" Padding="8,2"
                                                            Command="{Binding DataContext.RemoveTrustedPublisherCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                            CommandParameter="{Binding}" />
                                                </DockPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <TextBlock Text="{svc:Loc Key=Xaml_No_trusted_publishers_yet_826f2f55}" Style="{StaticResource Hg.DimText}"
                                               Visibility="{Binding TrustedPublishers.Count, Converter={StaticResource EmptyStateVis}}" />
                                </StackPanel>
                            </Border>

                            <!-- Trusted folders (NET-117) -->
                            <Border Style="{StaticResource Hg.Card}" Margin="0,0,0,10">
                                <StackPanel>
                                    <DockPanel LastChildFill="False">
                                        <TextBlock DockPanel.Dock="Left" Text="{svc:Loc Key=Xaml_Trusted_folders_7c2bb9d8}" FontWeight="SemiBold" />
                                        <Button DockPanel.Dock="Right" Content="{svc:Loc Key=Xaml_Refresh_56e3badc}"
                                                Command="{Binding LoadTrustedFoldersCommand}" Padding="10,2" />
                                    </DockPanel>
                                    <TextBlock Text="{svc:Loc Key=Xaml_Any_binary_under_these_folders_is_79c0059a}"
                                               Style="{StaticResource Hg.HelperText}" Margin="0,4,0,8" />
                                    <ItemsControl ItemsSource="{Binding TrustedFolders}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="0,0,0,4">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="{Binding}" VerticalAlignment="Center"
                                                               TextWrapping="Wrap" Margin="0,0,8,0" />
                                                    <Button Grid.Column="1" Content="{svc:Loc Key=Xaml_Remove_e963907d}" Padding="8,2"
                                                            Command="{Binding DataContext.RemoveTrustedFolderCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                            CommandParameter="{Binding}" />
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <TextBlock Text="{svc:Loc Key=Xaml_No_trusted_folders_yet_b90572cc}" Style="{StaticResource Hg.DimText}"
                                               Visibility="{Binding TrustedFolders.Count, Converter={StaticResource EmptyStateVis}}" />
                                </StackPanel>
                            </Border>

                            <!-- VPN-presence kill-switch (NET-119) -->
                            <Border Style="{StaticResource Hg.Card}" Margin="0,0,0,10">
                                <StackPanel>
                                    <DockPanel LastChildFill="False">
                                        <TextBlock DockPanel.Dock="Left" Text="{svc:Loc Key=Xaml_VPN_kill_switch_435ed722}" FontWeight="SemiBold" />
                                        <Button DockPanel.Dock="Right" Content="{svc:Loc Key=Xaml_Refresh_56e3badc}"
                                                Command="{Binding LoadKillSwitchCommand}" Padding="10,2" />
                                    </DockPanel>
                                    <TextBlock Text="{svc:Loc Key=Xaml_Block_ALL_outbound_whenever_the_chosen_031f3f4c}"
                                               Style="{StaticResource Hg.HelperText}" Margin="0,4,0,8" TextWrapping="Wrap" />
                                    <WrapPanel>
                                        <TextBlock Text="{svc:Loc Key=Xaml_VPN_adapter_7234153f}" VerticalAlignment="Center" Margin="0,0,8,8" />
                                        <ComboBox Width="360" Margin="0,0,8,8"
                                                  ItemsSource="{Binding Adapters}"
                                                  SelectedItem="{Binding SelectedAdapter}"
                                                  DisplayMemberPath="Label"
                                                  AutomationProperties.Name="{svc:Loc Key=Xaml_VPN_adapter_to_watch_af20301c}"
                                                  ToolTip="{svc:Loc Key=Xaml_Pick_the_network_adapter_your_VPN_771cf729}" />
                                        <Button Command="{Binding ToggleKillSwitchCommand}" Margin="0,0,10,8"
                                                ToolTip="{svc:Loc Key=Xaml_Turn_the_VPN_kill_switch_on_2e253649}">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                                    <Setter Property="Content" Value="{svc:Loc Key=Xaml_EnableKillSwitchButton}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding KillSwitchEnabled}" Value="True">
                                                            <Setter Property="Content" Value="{svc:Loc Key=Xaml_DisableKillSwitchButton}" />
                                                            <Setter Property="Background" Value="{DynamicResource Hg.Danger2}" />
                                                            <Setter Property="Foreground" Value="{DynamicResource Hg.OnDanger}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </WrapPanel>
                                    <Border Style="{StaticResource Hg.StatusStrip}" Margin="0,4,0,0">
                                        <TextBlock Text="{Binding KillSwitchStatusText}"
                                                   Style="{StaticResource Hg.SubText}"
                                                   AutomationProperties.LiveSetting="Polite" />
                                    </Border>
                                </StackPanel>
                            </Border>

                            <!-- Per-app VPN binding (NET-157) -->
                            <Border Style="{StaticResource Hg.Card}" Margin="0,0,0,10">
                                <StackPanel>
                                    <DockPanel LastChildFill="False">
                                        <TextBlock DockPanel.Dock="Left" Text="{svc:Loc Key=Tools_AppVpnBinding_Title, Default=Per-app VPN binding}" FontWeight="SemiBold" />
                                        <Button DockPanel.Dock="Right" Content="{svc:Loc Key=Xaml_Refresh_56e3badc}"
                                                Command="{Binding LoadAppVpnBindingsCommand}" Padding="10,2" />
                                    </DockPanel>
                                    <TextBlock Text="{svc:Loc Key=Tools_AppVpnBinding_Help, Default=Bind one program to an adapter by blocking it on other active interfaces. Default outbound and hosts-file blocks are unchanged.}"
                                               Style="{StaticResource Hg.HelperText}" Margin="0,4,0,8" TextWrapping="Wrap" />
                                    <WrapPanel>
                                        <TextBlock Text="{svc:Loc Key=Tools_AppVpnBinding_Program, Default=Program}" VerticalAlignment="Center" Margin="0,0,8,8" />
                                        <TextBox Width="360" Margin="0,0,8,8"
                                                 Text="{Binding AppVpnProgramPath, UpdateSourceTrigger=PropertyChanged}"
                                                 Style="{StaticResource Hg.HintBox}"
                                                 Tag="{svc:Loc Key=Tools_AppVpnBinding_ProgramHint, Default=Program path}"
                                                 AutomationProperties.Name="{svc:Loc Key=Tools_AppVpnBinding_ProgramName, Default=Program path for VPN binding}" />
                                        <TextBlock Text="{svc:Loc Key=Tools_AppVpnBinding_Adapter, Default=Adapter}" VerticalAlignment="Center" Margin="0,0,8,8" />
                                        <ComboBox Width="300" Margin="0,0,8,8"
                                                  ItemsSource="{Binding Adapters}"
                                                  SelectedItem="{Binding SelectedAppVpnAdapter}"
                                                  DisplayMemberPath="Label"
                                                  AutomationProperties.Name="{svc:Loc Key=Tools_AppVpnBinding_AdapterName, Default=VPN adapter for app binding}" />
                                        <Button Content="{svc:Loc Key=Tools_AppVpnBinding_Bind, Default=Bind app}"
                                                Command="{Binding SaveAppVpnBindingCommand}" Margin="0,0,10,8" />
                                    </WrapPanel>
                                    <ItemsControl ItemsSource="{Binding AppVpnBindings}" Margin="0,2,0,0">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Style="{StaticResource Hg.StatusStrip}" Margin="0,0,0,6">
                                                    <DockPanel LastChildFill="True">
                                                        <Button DockPanel.Dock="Right"
                                                                Content="{svc:Loc Key=Xaml_Remove_e963907d}"
                                                                Padding="8,2"
                                                                Command="{Binding DataContext.RemoveAppVpnBindingCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                                CommandParameter="{Binding}"
                                                                AutomationProperties.Name="{svc:Loc Key=Tools_AppVpnBinding_RemoveName, Default=Remove app VPN binding}" />
                                                        <StackPanel>
                                                            <TextBlock Text="{Binding ProgramPath}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                                            <WrapPanel Margin="0,3,0,0">
                                                                <TextBlock Text="{Binding Adapter}" Style="{StaticResource Hg.DimText}" Margin="0,0,10,0" />
                                                                <TextBlock Text="{Binding AdapterStatus}" Style="{StaticResource Hg.DimText}" Margin="0,0,10,0" />
                                                                <TextBlock Text="{Binding BlockedInterfacesText}" Style="{StaticResource Hg.DimText}" />
                                                            </WrapPanel>
                                                        </StackPanel>
                                                    </DockPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <TextBlock Text="{svc:Loc Key=Tools_AppVpnBinding_Empty, Default=No app VPN bindings yet.}" Style="{StaticResource Hg.DimText}"
                                               Visibility="{Binding AppVpnBindings.Count, Converter={StaticResource EmptyStateVis}}" />
                                    <Border Style="{StaticResource Hg.StatusStrip}" Margin="0,4,0,0">
                                        <TextBlock Text="{Binding AppVpnStatusText}"
                                                   Style="{StaticResource Hg.SubText}"
                                                   AutomationProperties.LiveSetting="Polite" />
                                    </Border>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource Hg.StatusStrip}" Margin="0,0,0,0">
                                <TextBlock Text="{Binding StatusText}"
                                           Style="{StaticResource Hg.SubText}"
                                           AutomationProperties.LiveSetting="Polite" />
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>
            </TabControl>

            <Grid Background="{DynamicResource Hg.Bg}"
                  Visibility="{Binding IsConnected, Converter={StaticResource InverseBoolToVis}}">
                <Border Style="{StaticResource Hg.Card}"
                        MaxWidth="520"
                        Padding="22,18"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                    <StackPanel>
                        <TextBlock Text="{svc:Loc Key=Xaml_Service_connection_needed_35041312}"
                                   Style="{StaticResource Hg.SectionTitle}"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,6" />
                        <TextBlock Text="{Binding ConnectionText}"
                                   Style="{StaticResource Hg.EmptyText}"
                                   AutomationProperties.LiveSetting="Polite" />
                        <Button Content="{svc:Loc Key=Xaml_Reconnect_6988b16a}"
                                Command="{Binding ConnectCommand}"
                                HorizontalAlignment="Center"
                                Margin="0,14,0,0"
                                MinWidth="120"
                                ToolTip="{svc:Loc Key=Xaml_Try_the_service_connection_again_0ea5fded}" />
                    </StackPanel>
                </Border>
            </Grid>
                    </Grid>
                </Border>
            </Grid>
        </DockPanel>
    </Grid>
</Window>
